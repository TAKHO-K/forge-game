# 강화 웹게임 — PRD v2

> 스타크래프트 유즈맵 「강화 키우기」에서 영감을 받은 강화·보스 도전 웹게임.
> 전투는 브라우저에서 완결되고, 서버는 기록 저장만 담당하는 **비동기 멀티** 구조.
>
> **[📄 문서 분리 안내]** 이 문서는 웹 v1의 설계·구현·수치를 담는다. 로블록스 전환
> 설계(19장 v3 구상 — 이제 로블록스에서 구현할 계획, 20장 로블록스 전환 전체)는
> `PRD-forge-game-roblox.md`로 옮겼다 — 로블록스 작업에 웹 전용 절까지 매번 끌고
> 다니지 않기 위함이다. 두 문서에 공통된 내용(예: 4.3 스킬, 9.1 보스 스펙)은 이 문서에
> 원본을 두고, 로블록스 문서는 참조만 한다. **로블록스 설계를 볼 때는 저 파일을 열어라.**

---

## 1. 한 줄 정의

10분 동안 사냥과 강화로 캐릭터를 키워 보스에 도전하고,
그 결과가 전체 플레이어의 기록표에 남는 웹게임.

**핵심 긴장**: "지금 도전할까, 더 키울까"

> **[🔄 설계 변경됨] "10분"의 실체**: 실제 내부 파밍 타이머는 1,800초(30분)로 구현되어 있다
> (8.0-1-a 참조). 대신 **배속(1x/2x/3x) 기능이 새로 생겨서**, 3배속으로 플레이하면
> 실제 걸리는 시간이 정확히 10분이 된다 — 파밍량 자체는 배속과 무관하게 동일하다(8.0-1-a).
> 즉 이 문서 전반의 "10분"은 지금도 **"3배속 기준 실질 플레이 시간"**으로 여전히 유효한
> 서술이며, 굳이 전부 "30분"으로 고치지 않는다. 다만 코드의 타이머 상수 자체를 볼 때는 1,800을 봐야 한다.

---

## 2. 설계 원칙

| 원칙 | 이유 |
|---|---|
| 전투 계산은 브라우저, 서버는 기록만 | 실시간 전투를 서버에 올리면 치팅 방지 부담이 게임을 두 번 만드는 수준이 됨 |
| 죽음에 페널티 없음 | 강화 실패가 이 게임의 유일한 상실감이어야 함 |
| 확률형 아이템 판매 없음 | 게임산업법상 확률 공시·등급분류 대상. 광고 보상으로 대체 |
| 유지보수 월 1~2회 | 밸런스 수치 조정만. 상시 대응이 필요한 구조를 만들지 않음 |
| 한 판 = 보스 1단계 = 10분(3배속 기준, 내부 타이머는 30분) | 출퇴근·쉬는 시간에 한 판. 짧아야 재도전한다 |
| 풀 클리어 = 2판(구현된 1·2단계 기준) = 약 20분 | 이어서 하는 사람만. 한 번에 앉아 있으라고 강요하지 않는다 (전체 6단계 설계 기준으로는 약 1시간, 9.1 참조) |

---

## 3. 핵심 루프

```
무기 선택 → 던전 사냥 (몬스터·상자)
    → 무기 경험치 + 골드 + 재료 + 강화권 획득
    → 무기 강화 (장비는 강화 대신 드랍·아이템 레벨로 성장, 7.5 참조)
    → 딜량 상승 → 상위 몬스터 해금
    → [10분 경과] or [조기 도전] → 보스전
    → 성공: 보상 + 기록 등록 / 실패: 3분 파밍 후 재도전 (최대 2회)
```

---

## 4. 직업 `[✅ 구현 완료 — 4직업 기본 스탯·Q스킬. E스킬은 미구현]`

전체 6직업 중 **v1은 4직업**을 구현한다. 나머지 2종(탱커·CC딜러)은 파티 플레이가
전제이므로 v3로 미룬다.

| # | 직업 | v1 | 역할 |
|---|---|---|---|
| 1 | 대검 딜러 | ⭕ | 느린 고데미지 |
| 2 | 쌍검 딜러 | ⭕ | 빠른 연타 |
| 3 | 활 딜러 | ⭕ | 원거리 고위험 고수익 |
| 4 | **힐러** | ⭕ | 체력을 자원으로 쓰는 변칙 딜러 (+ v3에서 파티 회복) |
| 5 | 탱커 | ❌ v3 | 어그로 · 도발 |
| 6 | CC딜러 | ❌ v3 | 제어 · 시전 차단 |

### 4.1 딜러 3종 (시작 시 1택) `[🔄 밸런스 기준선 변경 — 딜 천장을 활→쌍검으로 이동, 근거는 4.4 참고]`

| 타입 | 공격력 | 공격속도 | 방어력 | 성격 |
|---|---|---|---|---|
| **대검** | 1.85배 | 0.7배 | 높음 (1.3배) | 느리지만 안정적. 실수해도 버틴다 |
| **쌍검** | 0.85배 | 1.6배 | **낮음 (0.6배)** | 타수가 많아 강화 상승분이 즉시 체감된다. **지속 딜·최고점(4.4)** |
| **활** | 1.8배 | 1.0배 | 낮음 (0.6배) | 실력 폭이 가장 넓다. 원거리 + 회피가 곧 실력 |

**쌍검의 위치**: 방어력을 대검·활과 같은 최저 구간(0.6배)까지 낮추는 대신 딜 천장을 가져간다 —
근접인데도 활만큼 위험을 감수하게 만들어 "천장은 항상 원거리"라는 기존 구도를 깬다. 방어력을
0.5배가 아니라 0.6배로 맞춘 이유는 8.1의 1등급 몬스터 무피해 안전 구간(공격력 3 ≤ 최소
방어배율×방어력5)이 정확히 방어배율 0.6에서 성립하기 때문 — 이 문턱을 넘기면 몬스터 데이터
재조정이 필요해진다.

**활의 위치**: 지속 딜 최고점은 쌍검에게 넘겼지만, 스킬 타이밍(Q-E 스냅샷, 4.3·20.8-3 참고)으로
뽑아내는 폭딜과 실력 폭은 4직업 중 가장 넓다. 원거리 공격이지만 방어력이 낮아 한 대만 맞아도
크게 아프고, 대시와 무적 시간을 얼마나 잘 쓰느냐가 실력이 되는 무기.
→ 조기 도전과 궁합이 가장 좋다.

### 4.1-1 힐러 — 체력을 자원으로 쓰는 변칙 딜러 `[🔄 설계 변경됨 — 모드 온/오프만 구현, 딜 환산·체력 환급·부활은 미구현]`

**v1(싱글)에서는 딜링 모드가 사실상 본체다.** 힐할 대상이 자신뿐이기 때문.
v3에서 파티가 붙으면 본래 역할이 열린다.

**기본 스탯**

| 공격력 | 공격속도 | 방어력 |
|---|---|---|
| 0.5배 (매우 약함) | 1.0배 | 중간 (1.0배) |

기본 공격만으로도 사냥은 가능하다. 느릴 뿐.

**모드 전환 — 딜링 모드**

| 항목 | 값 |
|---|---|
| 전환 조작 | 전용 버튼 (모바일: 화면 좌하단) |
| 비용 | **최대 체력 1칸 소모** |
| 지속 | 15초 |
| 효과 | **회복량 계수를 공격력으로 환산해 난사** |
| 재사용 | 체력이 2칸 이상 남아 있으면 즉시 가능 (연속 사용 가능) |
| **체력 환급** | 딜링모드 **활성화 1회당**(매 평타가 아니다) `min(25%, 치명타확률 × 0.5)` 확률로 체력 1칸 회복 |
| **자동 회복·라이프스틸** | 딜링모드가 켜져 있는 동안 **정지**한다 — 꺼야 재개된다 |

```
딜링모드 공격력 = 기본공격력 × (1 + min(회복량계수, dealModeHealPowerCap))
dealModeHealPowerCap = 0.8   // 딜링모드 환산에만 적용 — 치유량(computeHealAmount)의
                              // 회복량계수는 이 상한과 무관하게 그대로 무상한(1+회복량계수)이다
```

`[🔄 설계 변경됨]` **공식을 덧셈에서 곱셈으로 고친다.** 원래 `기본공격력 + 회복량계수×2.5`는
치유량(`1+회복량계수`)·무기경험치(`1+계수×레벨`) 등 이 게임의 다른 모든 스케일링과 다른
유일한 덧셈식이었다 — `기본공격력`이 강화·장비로 수백 단위까지 커지는 값인 반면
회복량계수는 0~1 사이라, 덧셈으로는 후반에 `+회복량계수×2.5`가 사실상 0으로 묻힌다.
"회복량 계수가 딜링모드의 주력 스탯"이라는 이 절의 핵심 전제 자체가 덧셈식으로는
성립하지 않았다.

**체력 환급을 "활성화 1회당"으로 못박은 이유**: "딜링 스킬 사용 시"를 매 평타 판정으로
읽으면, 힐러 기본 치명타율(12%)만으로도 15초 창에 평타 약 67회 × 6% 확률 ≈ 기대 4.0칸
환급이 나온다 — 1칸 쓰고 4칸을 돌려받는 구조라 자원 소모라는 정체성이 무너진다. 활성화
1회당 판정이면 15초당 기대 0.06칸으로 원래 의도한 소량의 보정만 남는다.

**자동 회복·라이프스틸을 정지시키는 이유**: 자동 회복(12초 무피격 후 3초당 1칸 = 0.333칸/초)은
소모율(15초당 1칸 = 0.067칸/초)의 5배다 — 힐러는 원거리라 무피격 유지가 어렵지 않으므로,
정지시키지 않으면 딜링모드를 영구히 켜둬도 체력이 줄지 않아 "체력을 태워 딜을 낸다"는
전제 자체가 사라진다. 라이프스틸(15.2-1)도 같은 이유로 딜링모드 중엔 발동하지 않는다 —
딜링모드 자체가 회복량계수를 딜로 바꿔 쓰는 스킬인데 그 위에 또 다른 회복 효과가 동시에
붙는 건 자기모순이다. 딜링모드를 끄면 즉시 재개된다.

> **[⛔ 미구현]** 실제 구현은 `tryActivateDealMode()`가 체력 1칸을 깎고 15초 타이머를 켜는
> 온/오프 전환까지만 한다. 위 공격력 환산식, 아래 체력 환급, 부활 전부 코드에 없다.
> 대신 이번 랜덤 옵션 작업(15장)에서 `healPower`를 실제로 작동하는 스탯 레지스트리 항목으로
> 편입했다 — `computeHealAmount`가 이미 `healPower`를 받아 치유 스킬(4.3)에 반영하므로,
> 장비로 회복량 계수를 올리는 축 자체는 지금도 작동한다. 다만 그게 "딜로 환산"되지는
> 않는다 — 치유는 치유일 뿐 보스에게 피해를 주지 않는다. 힐러가 활보다 보스를 훨씬
> 느리게 잡는 문제(19.3 이전 회귀 검증에서 34.6초 vs 13.3초로 확인)를 실제로 풀려면
> 이 절의 "딜링모드 공격력" 환산식이 코드로 들어와야 한다 — 아직 그 작업 전이다.

> **핵심**: 힐러의 강화·장비 투자는 전부 `회복량 계수`로 간다.
> 그 계수가 딜링 모드에서 딜량으로 환산되므로, **힐 특화 = 딜 특화**가 성립한다.
> 다른 직업이 "강화 → 딜"이라면 힐러는 "강화 → 회복량 → 딜"이라는 한 단계를 더 거친다.

> **의도**: 체력 10칸을 탄창처럼 쓰는 감각. 체력이 곧 딜 자원이라
> 자동 회복(12초 무피격) 관리가 다른 직업보다 훨씬 중요해진다.
> 잘 쓰면 최상위 딜, 못 쓰면 자기 체력만 태우고 죽는다.

> **밸런스 상한**: 힐러의 딜링모드 최고 딜은 **쌍검(4.4의 새 딜 천장)의 약 80%를 넘지
> 않아야 한다.** 100%에 닿으면 힐도 되는 직업이 딜까지 최상위가 되어 우위가 된다.
> `dealModeHealPowerCap = 0.8`이 구조적 안전장치다 — 회복량계수를 아무리 투자해도
> 딜링모드 배율은 `×1.8`(=1+0.8)을 넘지 못하므로, 극단적으로 투자해도 쌍검 대비
> **약 99.2%가 절대 상한**이다(회복량계수 0.45 확보 시 약 80%, 0.8 이상은 전부 99.2%로
> 수렴). 이 계산의 비교 기준: 장비 배율은 두 직업에 동일하게 걸린다고 가정해 상쇄시키고,
> "쌍검은 4.4 표의 기본 스탯으로 낸 딜 천장(장비 옵션 제외), 힐러는 같은 장비 단계에서
> 회복량계수 0.45를 확보한 상태"를 비교한다 — 나중에 20.9~20.11에서 실제 장비
> 시뮬레이션으로 재검산할 때도 이 정의(동일 장비배율 가정 + 직업별 최적 투자)를 그대로
> 써야 비교가 성립한다. 조정이 필요하면 `dealModeHealPowerCap`을 낮춘다.

**힐러 스킬**

| 스킬 | 효과 | 쿨다운 | v1 동작 |
|---|---|---|---|
| 치유 | 체력 3칸 즉시 회복 (회복량 계수 비례) | 25초 | 자기 자신에게 적용 |
| **부활** `[⛔ 미구현]` | 사망 시 그 자리에서 부활 (체력 50%) | **보스 1단계당 1회** | 자기 부활만 — 실제로는 5.6과 동일하게 스폰 지점에서 풀피 리스폰만 함 |

**v3 확장 시**
- 치유 → 파티 전체 광역 회복
- 부활 → 자신이 죽었을 때 발동, **자신 + 랜덤 최대 2명**까지 동시 부활
- 부활 사용 횟수는 v3에서도 보스 단계당 1회 유지

> 부활을 "자신이 죽었을 때만" 쓸 수 있게 한 것이 좋은 제약이다.
> 힐러가 살아 있는 동안 남을 마음대로 살릴 수 있으면 파티 전멸이라는 긴장이 사라진다.

### 4.2 무기 경험치 — 강화와 별개 축 `[🔄 설계 변경됨 — 계수 0.06/레벨로 상향, 캐릭터 레벨 역할 겸임]`

강화가 "도박"이라면 경험치는 "확정 성장"이다. 둘이 같이 굴러가야 실패해도 진도가 나간다.

- 몬스터 처치 시 무기 경험치 획득 (등급이 높을수록 많이)
- 레벨업 시 기본 공격력 +6%/레벨 (아래 표 참고 — 초안 3%에서 상향, 이유는 아래)
- **10분간 기본 몬스터만 잡고 강화를 전혀 안 했을 때 → 보스 1단계를 아슬아슬하게 못 잡는 수준**으로 조정

**무기 레벨 1~25가 곧 "캐릭터 레벨"이다.** 별도의 캐릭터 레벨 시스템을 만들지 않고
`getWeaponLevelFromExp`가 반환하는 값을 그대로 캐릭터 레벨로 쓴다 — "사냥만 하면 확정으로
오르는 축"이라는 정의가 정확히 같기 때문(7.0-1 참조). 이 레벨이 아이템 드랍 시점에
장비에 각인되는 **아이템 레벨**의 원천이다(7.5 참조).

| 무기 레벨 | 누적 경험치 | 공격력 배율 |
|---|---|---|
| 1 | 0 | 1.00 |
| 5 | 500 | 1.24 |
| 10 | 2,000 | 1.54 |
| 15 | 5,500 | 1.84 |
| 20 | 12,000 | 2.14 |
| 25 | 25,000 | 2.44 |

> `data/balance.js`의 `weaponExpAttackBonusPerLevel = 0.06`. 초안(약 0.03/레벨)에서 두 배로
> 올린 이유는 이 계수를 **아이템 레벨 스탯 계수와 그대로 공유**하기로 했기 때문이다(7.5) —
> 레벨 25에서 무기 공격력과 아이템 스탯이 나란히 2.44배로 정점을 찍도록 대칭을 맞췄다.
> 값을 하나만 만지면 둘 다 같이 움직인다.

> **의도**: "강화 없이도 아깝게 안 되네"가 첫 판의 감상이어야 한다.
> 그래야 두 번째 판에서 강화를 시도한다.

### 4.3 직업별 스킬 (직업당 2종 · Q / E) `[🔄 부분 구현 — Q스킬 4종 전부 구현 완료, E스킬 4종은 전부 미구현]`

**모든 스킬에 치명타가 적용된다.** (힐러의 치유 포함)

#### 대검 — 근접 · 안정

| 키 | 스킬 | 효과 | 쿨다운 |
|---|---|---|---|
| Q `[✅ 구현 완료]` | 관통돌진 | 칼을 내지르며 전방 돌진. 경로상 적 전원 타격 (물리, 데미지 계수 **5.5**) | 10초 |
| E `[⛔ 미구현]` | 회전베기 | 3초간 회전하며 이동(이동속도 `channelMoveSpeedMultiplier=0.5`). 주변 지속 타격(누적 데미지 계수 **5.91**) + **받는 피해 50% 감소** | 16초 |

> E는 회피 대신 정면으로 버티는 선택지. 대검의 "실수해도 버틴다" 성격을 스킬로 구현.

**스킬 데미지 계수 — "평타 5~6대를 치명타로 맞힌 만큼"이 기준선** `[⛔ 미구현, 설계 승인]`

Q/E는 지금까지 `data/skills.js`에 데미지 계수가 없어 사실상 0딜이었다. 기준을
"스킬 1회 = 평타 5~6대 치명타 상당"으로 잡되, 그대로 쓰면 안 된다 — 직업마다
평타/스킬 사용 빈도 비율이 달라 **같은 계수를 4직업에 flat하게 쓰면 4.4의 딜
천장 순서(쌍검>활>대검)가 뒤집힌다**(대검처럼 쿨다운이 짧아 스킬을 자주 쓰는
직업이 flat 계수에서 이득을 독점한다). 대신 **"스킬 데미지가 전체 딜의 26.7%를
차지하도록"**(4.3의 콤보 보너스 +26.7%와 같은 값 재사용 — 조작 축 두 개가 비슷한
무게를 갖게 하려는 것) 직업별로 역산한 뒤, "평타 5~6대 상당"을 **절대 하한선**으로
못박는다(하한 미만이면 스킬을 쿨마다 관리할 이유가 없어 실력 축이 죽는다).

역산 결과 대검은 하한(C≥5.5) 아래로 떨어져 **하한을 그대로 계수로 쓴다**(스킬 비중
26.7% 목표를 넘겨도 허용 — 목표는 상한이 아니라 하한 보정을 위한 기준일 뿐이다).

**채널링 이동불가 위험 비용 — 정량화 완료.** 회전베기는 "제자리 회전"이 아니라
**3초간 이동속도 `channelMoveSpeedMultiplier=0.5`로 느려진 채 회전**한다(완전
고정이 아니다). 완전히 못 움직이면(계수 0) 위험 프리미엄을 다 받아야겠지만,
절반 속도로는 여전히 회피 방향으로 움직일 수 있으므로 그 절반만 인정한다:

```
프리미엄 = 0.15 × (1 − channelMoveSpeedMultiplier) = 0.15 × (1 − 0.5) = 0.075(+7.5%)
C_Q = 5.5 (대시라 이동 제한이 없어 프리미엄 대상 아님)
C_E = 5.5 × 1.075 ≈ 5.91
```

치명타는 이 계수에 미리 곱해두지 않는다 — 계수는 `calcDamage`에 넘기는 기본
데미지 값일 뿐이고, 치명타는 평타와 똑같이 `calcDamage`의 크리 롤을 그대로
통과한다(이중 계산 방지).

**E-Q 연계 — 쿨다운 환급형** `[⛔ 미구현, 설계 승인]`

**회전베기(E) 종료 후 `greatswordComboRefundWindow`초 안에 관통돌진(Q)을 사용하면
관통돌진의 재사용 대기시간이 즉시 초기화된다.** 3안(환급형/공속 버프형/무변경) 중
환급형을 택한 이유: 공속 버프형(E 켜면 자동 적용)은 틀릴 방법이 없어 실력이 아니다.
환급형은 창을 놓칠 수 있어 판단이 되고, "버틴 보상이 반격"이라는 E의 성격과도 맞는다.

```
data/skills.js SKILLS.greatsword.E.effects에 새 조각으로 추가 (기존 effects 배열 방식 그대로):
{ type: "refundOnFollowup", followupSkill: "Q", window: greatswordComboRefundWindow }
```

**리필 성공 시 공격속도 버프 추가** `[⛔ 미구현, 설계 승인 — 실력 폭 확장]`

창을 맞춰 Q 쿨다운을 리필받는 데 성공하면, 그 보상으로 공격속도 버프를 추가로 건다.
값은 새로 만들지 않고 활의 E-Q 스냅샷 배수(4.3 활 절, `1.675/1.225≈1.367`)를 그대로
재사용한다 — 대검과 쌍검 둘 다 "정확한 타이밍"에 같은 보상을 받는다는 원칙을 지키기
위해서다. 지속시간(6초)도 활 Q 버프 지속시간과 같은 값을 재사용한다.

```
{ type: "buff", stat: "attackSpeed", duration: 6, value: 1.367, trigger: "onRefundProc" }
```

이 추가로 대검도 활처럼 "짧은 구간 전체 타격을 배율로 곱하는" 버프형 메커니즘을
갖게 된다 — 근접의 E-Q 연계가 지금까지 활과 구조적으로 달랐던 지점(개별 히트 몇 개
추가 vs 전체 타격 배율)을 좁힌다.

> `greatswordComboRefundWindow`는 값 자체(아래에서 0.5초로 계산)를 가리키는 이름일
> 뿐, 위치는 조정 가능한 데이터값이라는 뜻이다 — 실제 구현 시 `data/skills.js`의
> 이 effects 조각 안에 숫자로 박는다(전용 함수를 만들지 않는다는 원칙 그대로).

**창 길이 계산 — 왜 1초가 아니라 0.5초인가**

대검 평타 간격은 `attackInterval(0.28) / atkSpeed(0.6) ≈ 0.467초`다. 1초 창은 이
간격의 약 2.14배 — E가 끝나고 평타를 한 번 더 흘려보내고도 여유가 남는, "아무 때나
눌러도 되는" 폭이다. 반대로 창을 평타 간격과 같은 **0.5초**로 잡으면 그 안에 평타가
최대 1.07회분만 들어간다 — E가 끝나자마자 사실상 즉시 반응해야 하고, 딴짓(평타를
한 번 더 흘려보내는 것)을 할 여유가 없다. 500ms는 인간 평균 시각 반응속도(약
200~250ms)의 두 배 이상이라 순수 반사신경 테스트는 아니지만, E 종료를 놓치지 않고
주시하고 있어야 맞출 수 있는 폭이다. **`greatswordComboRefundWindow = 0.5`로 정한다.**

**놓쳤을 때의 손해 — 계산으로 확인**

창을 놓치면 그냥 Q가 평소처럼 10초 쿨로 돌아간다 — 이 자체는 손해가 아니라 "환급을
못 받았을 뿐"이다. 진짜 질문은 "Q가 준비됐을 때 그냥 바로 쓰는 것"과 "다음 E까지
들고 있다가 창에 맞춰 쓰는 것" 중 뭐가 더 이득이냐다. E 쿨(16초) > Q 쿨(10초)이므로
언뜻 "그냥 쓰기"가 안전해 보이지만, 실제로 시뮬레이션하면 다르다:

```
그냥 쓰기(Q가 준비되는 즉시 사용, E 타이밍 무시): 60초 보스전 기준 총 8회 캐스트,
  콤보 성립 1/4회 (준비 시점이 매번 16초 주기와 어긋나 버림 — E쿨과 Q쿨이 최소공배수
  관계가 아니라 갈수록 어긋남)
들고 있기(Q가 준비돼도 다음 E 창까지 잠깐 아꼈다가 창 안에서 사용): 60초 기준
  총 9회 캐스트, 콤보 성립 4/4회 (매 E 주기마다 확정 성립)
```

**"놓칠 것 같으면 아예 안 쓴다"가 실제로 더 좋다** — 그냥 쓰면 총 캐스트 수도 적고
(8 < 9) 콤보 성립률도 낮다(25% < 100%). 창을 겨냥해 들고 있는 판단 자체가 순수하게
더 나은 선택이라는 뜻이고, 이게 이 스킬이 만드는 실력 축이다(평타처럼 쿨마다 누르는
게 최선이 아니라, 쿨을 읽고 다음 창을 겨냥해야 최선이 나온다).

**보스전 길이 안에서 몇 번 성립하는가**: E 16초 쿨 기준 60초 보스전에 E 4회 → 콤보
4회 전부 성립(들고 있기 기준). 90초 보스전엔 E 6회 → 콤보 6회 전부 성립. 즉 E 주기
수만큼 "무료" Q 캐스트가 추가된다.

**DPS 환산 — 채널링 3초는 평타 시간에서 뺀다.** 회전베기가 도는 3초 동안은 평타를
칠 수 없으므로, 60초 보스전의 평타 가능 시간은 `60 − (E 3.75회×3초) = 48.75초`다.

```
기본킷(콤보·리필 미고려) 평타 = 48.75 × 2.143 ≈ 104.5회
스킬딜(위 계수, Q 6회·E 3.75회) = 5.5×6.0 + 5.91×3.75 = 55.2
대검 총딜 = 1.85 × (1+0.12×(2.0−1)) × (104.5+55.2) = 2.072 × 159.7 ≈ 330.8
```

4.4의 새 딜 천장(쌍검 436.5) 대비 −24.2%, 활(408.5) 대비 **−19.0%다.** 대검은
방어력 1.3배(4직업 중 최고)로 이미 "안정성" 정체성을 가진 방어형이므로, 순딜에서
15~20% 낮은 건 이 정체성과 정합적이다 — 이 문서 안에 직업 간 딜 격차 상한을
정한 규칙은 따로 없고, 있는 건 아래 스테이지 문턱의 안전마진(10%) 규칙뿐이라
이 이상 계수를 올려 격차를 좁힐 근거는 없다.

**실력 폭(E-Q 리필+공격속도 버프 포함) 재계산**: 리필로 Q가 9회로 늘고, 리필 성공
시 6초간 공격속도 ×1.367 버프가 3.75회 발동한다(총 22.5초, 60초의 37.5%).

```
스킬풀 평타 = (22.5×2.143×1.367) + (26.25×2.143) ≈ 122.2회
스킬풀 스킬딜 = 5.5×9 + 5.91×3.75 = 71.7
E상호작용 배수 = (122.2+71.7) / (104.5+55.2) ≈ 1.214(+21.4%)
콤보와 결합 = 1.267 × 1.214 ≈ 1.538, 스테이지 환산 ≈2.99단계
```

S=2(`k²=1.334`) 마진 +15.3%로 안전마진(10%)을 넘겨 **상한을 +1→+2로 올린다.**
S=3(`k³=1.541`) 마진은 −0.2%로 문턱을 넘지 못해 +3은 아직 아니다(자세한 근거는
PRD-forge-game-roblox.md 20.8-3).

> Q·E 둘 다 개별 히트 추가에 머물던 기존 구조에, 리필 성공 시 전체 타격을 곱하는
> 공격속도 버프를 더해 활의 "버프형" 구조에 가까워졌다 — 그 결과 상한이 +1에서
> +2로 늘었다. 판단의 깊이(창을 놓치면 안 쓴다는 홀드 판단)와 DPS 크기는 원래도
> 별개였고, 이번 추가는 크기 쪽을 끌어올린 것이다.

#### 쌍검 — 연타 · 치명타

| 키 | 스킬 | 효과 | 쿨다운 |
|---|---|---|---|
| Q `[✅ 구현 완료]` | 그림자분신 | 그 자리에 분신 생성 (**분신이 적을 도발해 어그로 유지**, 5초 — 사냥터 몬스터 한정, 아래 참고). **5초간 기본공격 확정 치명타** `[🔄 설계 변경됨 — "다음 기본공격 1타"에서 확장, 아래 참고]` | 14초 |
| E `[⛔ 미구현]` | 독날강화 | 8초간 공격에 도트 데미지 부여 (**마법피해**, 8초 누적 데미지 계수 **14.46**). 지속 중 **치명타 확률 +50%p** | 20초 |

**스킬 데미지 계수 — 14.46, 전액 E(독날강화)에 배분** `[⛔ 미구현, 설계 승인]`

대검과 같은 방식(전체 딜의 26.7%를 스킬이 차지하도록 역산, 하한 C≥5.5는 충족)으로
낸 값이다. Q(그림자분신)는 도발용 분신 소환이라 자체 피해가 없으므로, 계수 전액을
E의 8초 도트에 배분한다(8틱이면 틱당 약 1.8). Q가 향후 자체 피격판정을 갖게 되면
그때 배분을 다시 나눈다 — 지금은 E스킬 자체가 미구현이라 확정할 필요가 없다.

**확정 치명타 지속시간 확장 — "1타"에서 "5초간"으로** `[🔄 설계 변경됨]`

원래 스펙("다음 기본공격 1타만 확정 치명타")을 계산해보니 실효 이득이 무시할
수준이었다 — 쌍검 평타 간격은 `attackInterval(0.28)/atkSpeed(1.35) ≈ 0.207초`라
Q 쿨(14초) 한 주기에 약 67.5타가 들어가는데, 그중 딱 1타에만 붙는 효과였다. 반대로
활 E는 "다음 평타 5발"로 폭이 넓다 — 지속 단위가 직업별 공격속도를 고려하지 않은
채 정해져 있었다.

`data/skills.js SKILLS.dualblade.Q.effects`의 `nextAttack: { count: 1, guaranteedCrit:
true }`를 **지속시간 기반**으로 바꾼다:

```
{ type: "guaranteedCritWindow", duration: 5, guaranteedCrit: true }
```

**"다음 N타"가 아니라 "N초간"을 택한 이유**: N타 방식은 강화 +18 마일스톤
(`ENHANCE_VISUAL_MILESTONES`의 `hitMultiplier: 2`, 명중 횟수 2배)이 붙으면 "타수"의
정의가 흔들린다 — 판정 1회당 히트가 2번이면 "5타"가 실제로는 2.5회 공격 분량이
될 수도 있다. 시간 기반이면 이런 간섭이 없다.

**W=5초를 택한 이유**: 새 상수를 만들지 않는다. 이 스킬 자체에 이미 있는
`summonDecoy.duration=5`(분신 도발 지속시간)를 그대로 재사용한다 — 어차피 분신이
살아있는 5초 동안은 화면에 "지금 강화 구간"이라는 시각 신호(분신)가 이미 떠 있으므로,
확정 치명타 창을 같은 5초로 맞추면 **플레이어가 별도로 외울 숫자가 늘지 않는다.**
(`data/skills.js`에서 `guaranteedCritWindow`의 `duration`을 `summonDecoy`의
`duration`과 같은 값으로 두는 것을 권장 — 두 값을 분리된 상수로 관리하면 이후 밸런스
조정 시 하나만 바꾸고 잊어버릴 위험이 있다.)

**확정 치명타의 초과분 처리** `[✅ 구현 완료 — 발동 범위만 위 변경 적용]`
치명타 확률이 이미 100%에 도달한 경우, 확정 치명타 효과는 **치명타 피해율 +40%**로 전환된다.
(`BALANCE.guaranteedCritOverflowBonus = 0.4` — 기존 치명타 피해율 1.6배에 0.4를
**더해** 2.0배로 만든다는 뜻이지, 1.6배에 40%를 곱하는 게 아니다. 쌍검 기준 상대
이득은 `0.4/1.6 = 25%`.)

```
if (치명타확률 >= 100%) 확정치명타 → 치명타피해율 +40%p (창 안의 매 공격)
else                   확정치명타 → 무조건 치명타 발동 (창 안의 매 공격)
```

> 쌍검은 타수가 많아 치명타 확률을 올리기 쉽다. 100%에 닿았을 때
> 스킬이 무의미해지지 않도록 피해율로 환산해준다. 이 전환은 창이 넓어져도
> 그대로 유지 — 창 안의 매 타격에 개별 적용된다.

**정렬 창 중 공격속도 버프 추가** `[⛔ 미구현, 설계 승인 — 실력 폭 확장]`

Q의 5초 확정치명타 창과 E의 치확+50%p가 겹쳐 치확 100%(치피 +40%p 전환)가 되는
동안, 공격속도도 함께 ×1.367(대검과 같은 값, 활의 E-Q 비율 재사용)로 올린다 —
"정확히 겹치게 맞춘" 판단에 대검과 동일한 보상을 준다.

```
{ type: "conditionalBuff", stat: "attackSpeed", value: 1.367, duration: 5,
  condition: "otherBuffActive:E_critBoost" }
```

**실력 폭 재계산**: Q 주기(14초)의 35.7%(`5/14`)가 정렬 창이다(60초 기준 4.3회,
총 21.4초). 창 안 공격은 공속 ×1.367에 치피 전환 가치(+25%)까지 겹친다.

```
정렬 창 평타 = 21.4×(4.821×1.367) ≈ 141.0,  창 밖 평타 = 38.6×4.821 ≈ 186.1
평타 가치 합 = 141.0×1.25 + 186.1 ≈ 362.4
E상호작용 배수 = (362.4+105.4) / (289.3+105.4) ≈ 1.185(+18.5%)
콤보와 결합 = 1.267 × 1.185 ≈ 1.502, 스테이지 환산 ≈2.82단계
```

S=2(`k²=1.334`) 마진 +12.6%로 안전마진(10%)을 넘겨 **상한을 +1→+2로 올린다**(S=3
마진은 −2.5%로 아직 못 넘는다). 자세한 근거는 PRD-forge-game-roblox.md 20.8-3.

> **[🔄 설계 변경됨] 분신 도발은 보스에게 통하지 않는다.** PRD-forge-game-roblox.md 19.3은 "보스 도발 면역"을
> v3(파티) 전용으로 설계했지만, **v1에서 이미 앞당겨 적용했다** — 분신(`activeTaunt`)은
> 사냥터 몬스터의 어그로만 끈다(`resolveAggroTarget`). 근거: 쿨다운 14초·도발 지속 5초를
> 그대로 보스에 적용하면 보스전 시간의 35%(5/14)를 무피해로 딜만 넣는 구간이 생겨
> 쌍검 혼자 압도적으로 유리해진다 — 다른 3직업엔 없는 우회로다. PRD-forge-game-roblox.md 19.3의 "12초 도발
> 면역" 설계가 v3에서 그대로 재사용 가능한 이유이기도 하다.

#### 활 — 원거리 · 고점

| 키 | 스킬 | 효과 | 쿨다운 |
|---|---|---|---|
| Q `[✅ 구현 완료]` | 속사 | 6초간 공격속도 증가. **치명타 확률에 비례해 최대 2.5배까지** 상승 (아래 표기 정정 참고) | 18초 |
| E `[⛔ 미구현]` | 백스텝샷 | 뒤로 대시 + **다음 평타 5발에 마법피해 추가**, 해당 5발 치명타 확률 +30%p | 15초 |

**Q 공격속도 환산**

```
공속배율 = 100% + (치명타확률 × 1.5)   // 상한 250%
예) 치명타 30% → 145%(=1.45배)  /  치명타 100% → 250%(=2.5배 상한)
```

> **[🔄 표기 정정]** "공속증가율 250%"는 "기존 대비 +250%(3.5배)"가 아니라
> **결과 배율이 2.5배**라는 뜻이다. `computeBuffMagnitude`는 `min(2.5, 1.0 + 치명타확률×1.5)`를
> 그대로 공격 간격에 곱하는 배율로 쓴다(`getEffectiveAttackInterval`) — "100%"는 배율의 기준점
> 1.0을 퍼센트로 쓴 것뿐, "증가분"이 아니다. 계산 자체는 코드와 일치하니 숫자는 안 고치고
> 해석만 명확히 한다.

> **활이 최고점을 갖는 이유**: Q(공속) × 치명타 피해 계수(활이 가장 높음) ×
> E(마법피해 추가)가 겹치면 짧은 구간 폭딜이 나온다.
> 대신 방어력 0.6배 + E가 유일한 회피기라 실수 한 번에 무너진다.

#### 힐러 — 자원 관리 · 지속

| 키 | 스킬 | 효과 | 쿨다운 |
|---|---|---|---|
| Q `[✅ 구현 완료]` | 치유 | 체력 3칸 회복 (회복량 계수 비례). **치명타 시 회복량 2배** | 25초 |
| E `[🔄 부분 구현]` | 딜링모드 | 최대체력 1칸 소모, 15초간 회복량 계수를 딜로 환산해 난사 | 즉시 재사용 가능 |
| — `[⛔ 미구현]` | 부활 | 사망 시 그 자리 부활 (체력 50%) | 보스 1단계당 1회 |

> **[🔄 부분 구현]** E는 체력 1칸 소모 + 15초 타이머 온/오프까지만 구현되어 있다.
> "회복량 계수를 딜로 환산"하는 부분은 없다 — 위 4.1-1의 미구현 노트 참고.

**딜링모드 중 체력 환급** `[⛔ 미구현]`

```
딜링모드 활성화 시(매 평타가 아니다) → 치명타 확률에 비례해 체력 1칸 회복
회복 확률 = min(25%, 치명타확률 × 0.5)
```

> **의도**: 힐러는 체력을 태워 딜을 넣는 구조라 그대로 두면 지속력이 없다.
> 치명타를 올리면 태운 체력이 일부 돌아와 오래 버틴다.
> 단, **상한 25%**·**활성화당 1회**로 자원 소모라는 정체성은 유지된다 — 매 평타 판정으로
> 두면 15초 창에서 기대 환급이 소모량을 크게 웃돌아(4.1-1 참고) 자원 관리라는 축 자체가
> 사라진다.

#### R 키

v1에서는 비워둔다. v2에서 직업별 3번째 스킬(궁극기 성격) 추가 예정.

### 4.3-1 특수옵션이 여는 스킬 변화 `[⛔ 미구현 — 방향만 확정, 수치·확률 없음]`

7.3-2의 고대·태초 특수 옵션 중 일부는 스탯을 올리는 대신 **스킬의 동작·형태
자체를 바꾼다.** 스킬트리·분배 시스템은 만들지 않는다 — 해당 옵션이 붙은
장비를 착용하면 그 스킬이 바뀐다, 그게 전부다. 직업당 Q 1개·E 1개, 총 8개로
제한한다. 수치 상승이 아니라 **동작 변화**만 다루므로 계수·확률은 여기서 정하지
않는다.

#### 대검

| 스킬 | 이름 | 변화(동작) | 시각 효과 |
|---|---|---|---|
| Q | 쇄도의 창 | 직선 관통 돌진 → 제자리에서 넓은 반원을 베는 참격으로 형태 변경(관통 대신 광역) | 창 모양 돌진 이펙트 → 붉은 반원 참격 호 |
| E | 부동의 자세 | 회전하며 이동 → 완전히 제자리에 고정된 채 내려찍는 3연타로 변경(이동 완전 제거) | 회전 이펙트 → 바닥에 금이 가는 3연속 강타 |

**왜 대검다운가**: "실수해도 버틴다"는 안정형 정체성을 더 밀어붙이는 방향 —
위치 노출 위험(돌진)을 줄이거나(Q), 이동을 아예 포기하는 대신 더 단단해지는
선택지(E)를 준다.

#### 쌍검

| 스킬 | 이름 | 변화(동작) | 시각 효과 |
|---|---|---|---|
| Q | 잔영 쇄도 | 제자리 소환 → 적 반대편으로 순간이동해 나타나 앞뒤 협공 구도를 만드는 형태로 변경(도발·확정 치명타는 유지) | 제자리 소환 이펙트 → 순간이동 잔상 |
| E | 역병의 칼날 | 단일 대상 도트 → 적중할 때마다 주변 적으로 옮겨붙는 전염형 도트로 변경 | 녹색 독기운 → 보라색 전염 파티클이 적 사이로 튐 |

**왜 쌍검다운가**: 쌍검은 기동성·타수가 정체성인데 Q가 제자리 소환뿐이라 이동에
관여하지 않았다 — 위치를 바꾸는 옵션이 "치고 빠지기"에 어울리고(Q), 타수가
많은 직업답게 "많이 때릴수록 퍼지는" 전염형이 잘 맞는다(E).

#### 활

| 스킬 | 이름 | 변화(동작) | 시각 효과 |
|---|---|---|---|
| Q | 이도류 사격 | 공격속도 증가 버프 → 화살이 두 갈래로 갈라져 동시에 나가는 투사체 분리형으로 변경 | 단일 연사 → 교차로 갈라지는 두 갈래 화살 |
| E | 되돌아오는 화살 | 뒤로 대시 + 평타 강화 → 쏜 화살이 관통 후 부메랑처럼 되돌아와 왕복 히트하는 형태로 변경 | 직선 화살 → 곡선으로 되돌아오는 궤적 |

**왜 활다운가**: 활은 실력 폭(변수 조작)이 정체성 — 조준 판단이 하나 더 늘어나는
형태 변화(Q)와, 회피 기능은 유지하면서 판정을 왕복으로 늘려 "짧은 구간 폭딜"을
형태로 구현하는 변화(E) 둘 다 고점을 더 높이는 방향이다.

#### 힐러

| 스킬 | 이름 | 변화(동작) | 시각 효과 |
|---|---|---|---|
| Q | 정화의 빛 | 즉시 회복 → 바닥에 지속 치유 장판을 설치(그 자리에 머물러야 회복, 이동하면 못 받음)로 변경 | 즉시 회복 이펙트 → 바닥에 남는 빛 장판 |
| E | 폭주 | 15초 지속 딜링모드 → 즉시 터지는 한 방(순간 폭발)으로 변경(체력 소모 방식은 유지) | 지속 난사 이펙트 → 한 번의 큰 폭발 |

**왜 힐러다운가**: "자원 관리"라는 정체성에 새 판단 축을 얹는다 — Q는 "이동할지
회복할지"를, E는 "천천히 태울지 한 번에 태울지"를 판단하게 만든다.

**딜링모드 상호작용 확인**: Q(정화의 빛)는 장판형이므로 딜링모드 중에는 회복
대신 지속 딜 존으로 자연스럽게 전환될 수 있다(치유↔딜 전환이라는 힐러 고유
구조와 형태가 그대로 맞물린다). E(폭주)는 딜링모드 자체의 발동 형태를 바꾸는
옵션이라 딜링모드가 켜져 있을 때만 의미가 있고, 딜링모드 중 라이프스틸·자동
회복이 비활성화된다는 기존 규칙(4.1-1)과 그대로 호환된다 — 이 옵션이 그 규칙을
바꾸지 않는다.

#### 스킬 자동 사용 (v2 예정, v1에는 넣지 않음)

자동 사용이 켜지면 스킬 쿨다운 관리라는 판단이 사라진다.
"지금 쓸까, 보스전을 위해 아껴둘까"가 조작의 재미인데 자동이면 그냥 계속 돌아간다.

**v2에서 옵션으로** 넣는다 — 사냥터에서는 자동, 보스전에서는 수동.
반복 파밍의 피로를 줄이는 편의 기능으로는 훌륭하다.

#### E 쿨다운은 서로 맞추지 않는다 (구현 시 지킬 것)

현재 수치(대검 16초 / 쌍검 20초 / 활 15초 / 힐러 즉시)는 서로 어긋나 있다.
이 어긋남을 **의도적으로 유지한다** — E스킬 구현 시 "깔끔해 보인다"는 이유로
쿨다운을 통일하지 말 것.

어긋난 쿨다운은 "지금 아꼈다가 다른 스킬(활은 Q, 쌍검은 Q)과 겹칠 때
터뜨릴까"라는 판단을 만든다(PRD-forge-game-roblox.md 20.8-3의 활 사례 — E를 Q보다 먼저 켜서
Q의 치확 스냅샷을 끌어올리는 타이밍 게임이 정확히 이 어긋남에서 나온다).
쿨다운을 맞추면 "돌아올 때마다 누르기"가 되어 이 판단이 조작 실수 방지로
전락한다 — PRD-forge-game-roblox.md 20.8-3이 계산으로 확인했듯 이 타이밍 판단 자체가 무한 모드
"어려움" 난이도를 뚫는 실력 축의 핵심이다.

---

### 4.4 치명타 시스템 `[✅ 구현 완료]`

#### 직업별 치명타 성향 `[🔄 밸런스 기준선 변경 — 딜 천장을 활→쌍검으로 이동]`

| 직업 | 기본 치명타 확률 | 치명타 피해 계수 | 특성 |
|---|---|---|---|
| 대검 | **12%** | **×2.0** | 안정형 — 방어 1.3배 대신 순딜은 4직업 중 가장 낮다 |
| **쌍검** | **30%** | **×2.0** | 타수가 많아 자주 터진다. **지속 딜·딜 천장** |
| 활 | 15% | **×2.3** | 실력 폭이 가장 넓다 — 스킬 타이밍이 곧 고점 |
| 힐러 | 12% | ×1.8 | **치유에도 치명타 적용** (회복량 2배) |

- 치명타 확률은 장비 옵션·강화·스킬로 상승. **상한 100%**
- 치명타 피해 계수는 장비 특수 옵션으로 소폭 상승 가능
  `[✅ 구현 완료]` 랜덤 옵션 시스템(15장)의 `critRate`/`critDmg` 스탯이 이 역할을 한다 —
  `getEffectiveCritRate()`가 직업 기본값 + 장비 합산치를 100%로 클램프한다.

```
치명타 데미지 = 기본데미지 × 치명타피해계수
```

**기본 킷(장비 옵션 제외) 딜 천장 — 4.3의 스킬 데미지 계수 포함**

| 직업 | 60초 딜 총합(atk-단위) | 활 대비 |
|---|---|---|
| **쌍검** | **436.5** | 기준(100%) |
| 활 | 408.5 | −6.4% |
| 대검 | 330.8 | −24.2% |

> **밸런스 기준선**: **지속 딜·최고점은 쌍검**, 실력 폭(변수 조작 여지)은 활, 안정성은
> 대검, 지속력은 힐러. 근접(대검·쌍검)이 사거리를 유지해야 하는 위험을 감수하는데도
> 활만 딜 천장과 가장 넓은 실력 폭을 동시에 갖는 "이중 우위"였던 것을 이번에 교정했다
> — 딜 천장은 근접(쌍검)으로, 실력 폭은 활에 그대로 둔다. 힐러의 딜링모드 상한은
> 쌍검의 약 80%를 넘지 않는다(4.1-1 참고) — 힐도 되는 직업이 딜까지 최상위면
> 우위가 되기 때문이다.

#### 데미지 표시 (LoL 방식)

| 종류 | 색상 | 폰트 |
|---|---|---|
| 기본 공격 | **흰색** | 보통 |
| 물리 피해 (스킬) | **빨강** | 보통 |
| 마법 피해 | **파랑** | 보통 |
| **치명타** | 해당 색상 유지 | **크기 1.5배 + 굵게 + 튀어오르는 모션** |
| 회복 | 초록 | 보통 (치명타 시 동일 강조) |

- 숫자는 피격 지점에서 위로 떠오르며 0.8초간 유지 후 소멸
- 치명타는 폰트 변화 + 살짝 흔들리는 모션으로 **눈으로 즉시 구분**되어야 한다
- 동시 다수 타격 시 숫자가 겹치지 않도록 좌우로 약간 분산

> 강화 게임의 쾌감은 "숫자가 커지는 것"에서 온다.
> 치명타가 시각적으로 도드라져야 강화 투자가 체감된다.

---

## 5. 데미지 · 생존 계산 `[✅ 구현 완료]`

### 5.1 플레이어 → 몬스터

```
기본딜   = 무기공격력 × 무기레벨배율 × (1 + 강화계수) × 장비배율
실제딜   = max(기본딜 × 0.1, 기본딜 - 몬스터방어력)
```

> **[🔄 설계 변경됨] 수동 공격에도 쿨다운이 생겼다.** 예전엔 마우스를 연타하면
> `attackInterval`(0.28초) 없이 무제한 공격이 가능했다 — 그 상태에서는 활 Q(공속 버프,
> 4.3)가 수동 플레이에 아무 의미가 없었다(연타가 이미 최고 속도라 버프로 더 빨라질
> 여지가 없었으므로). `getEffectiveAttackInterval()` 하나를 자동 모드와 수동 클릭(mousedown)
> 양쪽이 공유하도록 고쳐서, 이제 공속 스탯·버프가 두 모드 모두에서 똑같이 유효하다.

**하한선 10%**: 방어력이 아무리 높아도 최소한의 데미지는 들어간다.
"아예 안 깎임"은 버그처럼 느껴지고, "거의 안 깎임"은 성장 동기가 된다.

### 5.2 몬스터 → 플레이어 (죽지 않게 하는 장치) `[🔄 설계 변경됨 — 뺄셈에서 비율 감소로]`

```
피해감소율 = 방어력 / (방어력 + α × 몬스터공격력)     (α = 0.072)
받는피해   = min(최대체력 × 0.3, 몬스터공격력 × (1 - 피해감소율))
```

> **[🔄 설계 변경됨] 기존 `몬스터공격력 - 플레이어방어력` 뺄셈을 폐지했다.**
> 무한 모드에서 몬스터 공격력·플레이어 방어력이 둘 다 지수적으로 커지면 뺄셈은
> 성장률을 아무리 정확히 맞춰도 결과가 0 아니면 전부(완전 무적)로 붕괴한다는 게
> PRD-forge-game-roblox.md 20.11-4에서 확인됐다 — 비율식은 결과가 방어력의 절대값이
> 아니라 "방어력÷몬스터공격력" 비율에만 좌우돼 두 값이 아무리 커져도 [0,1) 사이에
> 머문다(100%에 수학적으로 도달하지 못하므로 완전 무적이 구조적으로 불가능하다).
> α·직업별 방어 배율 적용 위치·등급별 방어 배율 재구축은 전부 PRD-forge-game-roblox.md
> 20.11-4 참고 — 무한 모드 설계 과정에서 나온 값이지만 `calcPlayerDamage`가 하나뿐인
> 공유 함수라 이 공식은 유한 구간(1~6단계)에도 그대로 적용된다.

**상한 30% 고정**: 어떤 공격도 한 번에 최대체력의 30%를 초과해 깎을 수 없다.
장비가 없거나 감소율이 낮은 캐릭터의 **최소 생존 보장**(20.11-4가 이 역할로
재확인)이며, 감소율이 충분히 높아지면(장비를 갖추면) 상한보다 낮은 실제 데미지가
적용되어 상한 자체와는 무관해진다.

> **[🔄 설계 변경됨] `calcPlayerDamage`는 plain `round()`로 정수를 반환한다.**
> 기존엔 원본 데미지가 0보다 크면 반올림이 0으로 내려가도 최소 1칸은 깎이게
> 강제했다(뺄셈 구조에서 완전 무적을 막기 위한 안전장치). 비율식은 그 자체로
> 무적을 막으므로 이 강제가 더 필요 없고, 오히려 1등급 무피해 불변식(8.1)이
> "반올림으로 0"이 되는 걸 전제하므로 그대로 두면 불변식이 깨진다. 이 변경은
> 받는 데미지(`calcPlayerDamage`)에만 적용한다 — 주는 데미지(`calcDamage`,
> 5.1)는 별도 함수라 영향이 없다.

**체력·데미지 표시**: 체력·받은 데미지는 원래 숫자를 그대로 표기한다(축약 표기법은
PRD-forge-game-roblox.md 20.9-1 참고, K/M/B/T + 두 글자 + 세 글자 자연 확장). 동시에
체력바는 **고정 20칸**으로 나눠 표시한다 — 칸 개수는 항상 20이고, **칸 하나의
값이 현재 최대체력에 비례**한다(칸 개수를 최대체력에 비례시키면 무한 모드
후반에 칸이 10³⁰⁰개가 된다). 이렇게 하면 "3칸 깎였다"는 체감이 스테이지1에서나
스테이지3000에서나 동일하게 유지된다. 20칸을 고른 근거: 기존 `hpBarMaxPerRow=10`과
정확히 맞아 두 줄(마인크래프트식, 10+10)로 자연스럽게 쌓이고, `20×0.3=6`으로
피격 상한이 정수 칸(6칸)에 정확히 맞물린다. 한 칸보다 작은 데미지(고감소율
상황 등)는 칸을 통째로 깎지 않고 부분 채움으로 표시한다. 3칸 이하 남으면
게이지 테두리가 붉게 깜빡인다.

### 5.3 무적 시간 (i-frame)

피격 직후 짧은 무적. 연속 피격으로 순식간에 녹는 상황을 방지한다.

| 항목 | 값 |
|---|---|
| 무적 지속 | **0.8초** |
| 시각 표시 | 캐릭터 반투명 깜빡임 |
| 적용 대상 | 몬스터 근접·투사체, 보스 패턴 전부 |

> 보스 패턴이 겹쳐 들어와도 한 번에 두 대를 맞지 않는다.
> 이게 없으면 장판 + 투사체가 동시에 들어올 때 억울한 죽음이 나온다.

### 5.4 대시 (스페이스바)

**회피의 핵심 조작.** 특히 활 유저의 생존이 여기에 달려 있다.

| 항목 | 값 |
|---|---|
| 조작 | 스페이스바 (모바일: 화면 우하단 버튼) |
| 이동 거리 | 캐릭터 4칸 분량 |
| 지속 시간 | 0.3초 |
| 쿨다운 | **8초** (길게 설정 — 남발 방지) |
| 대시 중 피격 | **데미지 50% 감소** |

**의도**: 완전 무적이 아니라 반감이다. 대시로 "완벽 회피"가 되면
패턴을 외울 필요가 없어지고, 아무 의미 없으면 쓸 이유가 없다.
반감은 "일단 대시하고 보자"와 "제대로 피하자" 사이에서 고민하게 만든다.

**쿨다운 감소**: `[🔄 설계 변경됨]` ~~장비 강화 특수 옵션으로~~ **장비 랜덤 옵션**
(`dashCooldownReduction`, 7.3-1)으로 단축 가능 — "강화 특수 옵션"이 아니라 드랍 시
붙는 일반 옵션이다. 원안의 "-1~-3초" 단일 값은 지금은 옵션 하나당 약 0.4초, 여러
부위에 걸쳐 붙으면 누적되는 구조로 바뀌었다(7.3-1 참고).
→ 컨트롤에 자신 있는 유저가 장비 방향을 회피 쪽으로 잡을 수 있다.

### 5.5 체력 자동 회복

| 항목 | 값 |
|---|---|
| 최대 체력 | **10칸** |
| 회복 시작 조건 | **12초간 피격 없음** |
| 회복 속도 | 3초당 1칸 |

> 12초는 "잠깐 물러나면 회복된다"와 "계속 맞으면 못 회복한다"의 경계.
> 활 유저에게는 이 12초를 만들어내는 것이 곧 컨트롤이 된다.

### 5.6 사망 처리

- 체력 0 → **던전에서 나가짐. 그게 전부.**
- 강화 단계·장비·아이템·경험치 **모두 유지**
- 즉시 재입장 가능
- **단, 보스 타이머는 계속 흐른다** → 사망은 시간 손실로만 작용

---

## 6. 강화 시스템 `[🔄 부분 구현 — 무기 강화·부가 효과(6.1-1)는 +10 스킬 해금만 보류. 장비 강화는 7.5 참조(폐지됨)]`

### 6.1 무기 강화 — 데미지 계수 `[✅ 구현 완료 — 수치 일치]`

| 강화 단계 | 단계당 계수 증가 | 누적 계수 (구간 끝) |
|---|---|---|
| +1 ~ +5 | 0.10 | 0.50 |
| +6 ~ +10 | 0.20 | 1.50 |
| +11 ~ +15 | 0.40 | 3.50 |
| +16 ~ +20 | 0.80 | 7.50 |
| +21 ~ +25 | 1.60 | 15.50 |

+20 = 기본 대비 8.5배 / +25 = 16.5배.
**+15→+16 구간에서 체감이 확 뛰도록 의도적으로 설계.**

### 6.1-1 강화 성공 시 따라오는 부가 효과 `[🔄 부분 구현 — 사거리·명중 배가·다중 대상 확장 ✅, +10 스킬 해금 ⛔ 별도 승인 대기]`

데미지만 오르면 숫자 게임이 된다. **눈에 보이는 변화**가 같이 붙어야 강화가 사건이 된다.

| 구간 | 매 강화 시 | 추가로 발생하는 변화 |
|---|---|---|
| +1 ~ +9 | 공격력 · 방어력 · 최대체력 소폭 상승 | — |
| **+10** | 위와 동일 | ~~무기 스킬 1개 해금~~ **⛔ 미구현(별도 승인 대기)** |
| +10 ~ +14 | 위와 동일 | — |
| **+15** | 위와 동일 | **공격 사거리 +20%** (활은 +30%, 대검·쌍검은 +10%) |
| **+18** | 위와 동일 | **단일 대상 명중 배가** — 원거리는 투사체 2발, 근접은 2연타 |
| **+20** | 위와 동일 | **공격 사거리 추가 +20%** (활은 +30%, 대검·쌍검은 +10%) |
| **+23** | 위와 동일 | **다중 대상 확장** — 원거리는 투사체 관통, 근접은 판정 부채꼴 +40% |

- 사거리 증가는 **활에게 특히 크다.** 먼저 때리고 안 맞는 거리가 넓어진다
- 대검·쌍검도 사거리 증가의 혜택을 받되 폭이 작다 (근접 무기 특성) — `data/classes.js`의
  `rangeBonusScale`로 직업별 배분(활 1.5배, 근접 0.5배, 힐러는 활 언급이 없어 기본값 1배)
- **+18을 "단일 대상 배가"로 정한 이유**: 보스는 단일 대상이라, 원거리의 "투사체 2배"는 화살
  두 발이 같은 보스에 다 맞아 보스 DPS가 그대로 2배가 되지만, 근접에서 판정 부채꼴만 넓히면
  다수 몬스터 파밍에는 도움이 돼도 보스 DPS는 전혀 안 오른다 — 그래서 근접도 "2연타"로 판정을
  그대로 반복해 보스 DPS 배가를 맞췄다. 반대로 **+23 "다중 대상 확장"은 원거리 관통도 보스처럼
  고정된 단일 히트박스엔 이득이 없어**(경로가 이미 겹친 지점을 다시 때리지 않음) 근접의 부채꼴
  확대와 정확히 대칭이다 — 두 직업군의 보스 DPS 체감이 +18에서만 오르고 +23에서는 파밍 속도만
  오르도록 의도적으로 맞췄다(설계 승인 시 확인).
- 원거리는 사거리를 상시 표시하지 않는다 — 사거리(최대 활 기준 반경 676px)가 화면을 거의
  덮어 몬스터·드랍을 가릴 수 있어서, `data/balance.js`의 `rangeIndicatorFlashDuration`(4초)만큼
  사거리 마일스톤(+15/+20) 달성 직후에만 옅은 원으로 잠깐 보여준다. 근접의 판정 부채꼴은
  방향성이 있어 상시 표시로 둬도 화면을 가리지 않는다(기존 설계 유지).
- **+1~+9 방어력·최대체력의 "소폭 상승"은 강화 1단당 +1%**(`data/balance.js`의
  `enhanceDefenseBonusPerLevel`/`enhanceHpBonusPerLevel`, 설계 승인)로 구현했다. 최대체력은
  체력바가 정수 칸이라 반올림한다(버림이 아니라 반올림을 쓴 이유: 강화는 성공/강등 양방향으로
  움직이는 값이라 버림만 쓰면 항상 손해 보는 쪽으로 치우친다).

> **의도**: "+17에서 +18 갔더니 화살이 2발로 늘었다"는 게
> 데미지 숫자 200 → 340보다 훨씬 강하게 기억에 남는다. (원문 의도 문단은 "3발"이라 썼지만
> 표의 수치는 "2배"다 — 구현은 표의 수치를 기준으로 삼았다.)

### 6.2 무기 강화 확률 (합계 항상 100%) `[✅ 구현 완료 — data/enhance.js ENHANCE_PROBABILITY와 수치 일치]`

**실패 결과는 4종: 형상유지 / -1강 / -2강 / 리셋(1강).**
리셋은 메이플 파괴처럼 **희귀한 사고**여야 한다. 일상이 되면 진행이 불가능해진다.

| 구간 | 성공 | 형상유지 | -1강 | -2강 | **리셋** |
|---|---|---|---|---|---|
| +1 → +5 | 92% | 8% | 0% | 0% | 0% |
| +5 → +8 | 78% | 12% | 10% | 0% | 0% |
| +8 → +11 | 62% | 13% | 25% | 0% | 0% |
| +11 → +14 | 48% | 12% | 30% | 10% | 0% |
| +14 → +17 | 38% | 12% | 32% | 17% | **1%** |
| +17 → +20 | 28% | 10% | 35% | 26% | **1%** |
| +20 → +23 | 18% | 8% | 38% | 34% | **2%** |
| +23 → +25 | 12% | 5% | 38% | 42% | **3%** |

#### 왜 리셋이 3% 이하여야 하는가 — 기댓값

강화 1회당 평균 상승 단계를 계산하면 문제가 보인다.

```
초기 설계(폐기): +13 구간에서 성공25 / 유지15 / 강등48 / 리셋12
EV = 0.25×(+1) + 0.15×(0) + 0.48×(-1) + 0.12×(-12)
   = 0.25 - 0.48 - 1.44 = -1.67
→ 누를 때마다 평균 1.67단계씩 내려간다. 아무리 눌러도 못 올라간다.
```

**범인은 리셋 확률이다.** 13강에서 1강으로 가면 -12단계이고,
여기에 12%만 곱해도 그 항목 하나가 -1.44다. 나머지를 어떻게 조정해도 이길 수 없다.

리셋을 3% 이하로 낮추고 강등(-1/-2)을 주력 실패로 돌리면 이 구조가 풀린다.

#### 구간별 실제 난이도

| 강화 단계 | 맨손 기댓값 | **파괴방지권 사용 시** |
|---|---|---|
| +12 | -0.02 (거의 0) | +0.48 |
| +15 | -0.42 | **+0.38** |
| +18 | -0.76 | **+0.28** |
| +21 | -1.28 | **+0.18** |

파괴방지권은 모든 실패를 형상유지로 바꾸므로 **기댓값이 항상 양수**가 된다.
시간은 걸려도 반드시 올라간다.

**따라서 구간이 이렇게 나뉜다.**

| 구간 | 필요한 것 |
|---|---|
| +1 ~ +13 | 맨손으로 도달 가능 |
| **+14 ~ +20** | **파괴방지권 필요** (광고로만 획득) |
| **+21 ~ +25** | **파괴방지권 + 확률상승권** (광고 + 골드) |

> 이것이 광고·상점 설계의 근거다. 파괴방지권을 골드로 살 수 없게 한 이유,
> 후반 잉여 골드의 소비처가 확률상승권인 이유가 여기서 나온다.

#### 구현 시 주의

`core/enhance.js`의 `tryEnhance()`는 결과를 5종으로 반환해야 한다.

```js
// 반환 예시
{ result: "success",  newLevel: 15 }
{ result: "maintain", newLevel: 14 }
{ result: "down1",    newLevel: 13 }
{ result: "down2",    newLevel: 12 }   // 신규
{ result: "reset",    newLevel: 1  }
```

- 강등은 **0 밑으로 내려가지 않는다** (-2강인데 1강이면 → 1강 유지)
- 리셋은 항상 **1강** (0강 아님)

### 6.2-1 상급 강화 — 골드를 태워 성공률을 산다 `[✅ 구현 완료]`

**일반 강화와 상급 강화 중 매번 선택한다.**

| | 일반 강화 | **상급 강화** |
|---|---|---|
| 골드 비용 | 기본 | **5배** |
| 성공률 | 표 그대로 | **×1.5 (상한 90%)** |
| 실패 분포 | 표 그대로 | 남은 확률을 **같은 비율로 축소** |
| 재료 | 동일 | 동일 |

```js
// 상급 강화 확률 계산
successHigh = min(0.90, success × 1.5)
scale = (1 - successHigh) / (1 - success)
유지·-1·-2·리셋 = 각각 × scale
```

#### 보정 결과가 기본보다 낮으면 기본을 유지한다 (keepBetter) `[✅ 구현 완료]`

+0 ~ +4 구간의 기본 성공률은 92%다. 상급 배율을 그대로 적용하면
`min(90%, 92%×1.5) = 90%` — **상급 강화를 썼는데 성공률이 오히려 2%p 내려간다.**
상한(90%)이 저단계 기본 확률(92%)보다 낮아서 생기는 문제다.

`resolveProbability`는 이걸 `keepBetter(prob, boosted)`로 막는다 — 보정 결과가 기본보다
낮으면 보정을 버리고 기본 확률을 그대로 쓴다. 그 결과 **+0 ~ +4에서는 상급 강화가
골드 5배를 쓰고도 성공률이 일반 강화와 똑같이 92%다.** 버튼을 비활성화하지는 않는다 —
대신 UI에 `"이득 없음 - 일반 강화와 성공률 동일"`을 표시해 유저가 스스로 판단하게 한다
(6.2-1의 "시스템으로 막지 않아도 갈린다" 원칙과 같은 맥락).

#### 구간별 실효

| 현재 단계 | 일반 성공 | 상급 성공 | 판단 |
|---|---|---|---|
| +5 | 78% | **90%** (상한) | 이득 작음. 쓸 이유 없음 |
| +9 | 62% | **90%** (상한) | 어차피 잘 붙는 구간 |
| **+12** | 48% | **72%** | 여기서부터 체감 |
| **+15** | 38% | **57%** | 시도 횟수가 3분의 2로 |
| **+18** | 28% | **42%** | 실패로 잃는 단계가 절반 |
| **+22** | 18% | **27%** | 사실상 필수 |

**저강화에서 자연스럽게 안 쓰게 되는 이유 (금지하지 않는다)**

1. 성공률이 60%를 넘으면 ×1.5가 **90% 상한에 걸려** 배율이 다 먹지 않는다
2. **초반에는 골드가 없다.** 5배를 낼 여유가 생길 때쯤엔 이미 고강화 구간이다

> 시스템으로 막지 않아도 갈린다. **"할 수는 있는데 할 이유가 없는"** 상태가
> 강제 잠금보다 낫다 — 유저가 스스로 판단해서 안 쓰기 때문이다.

#### 기댓값 비교

| 단계 | 일반 | **상급** | 상급 + 파괴방지권 |
|---|---|---|---|
| +12 | -0.02 | **+0.45** | +0.72 |
| +15 | -0.42 | **+0.02** | +0.57 |
| +18 | -0.76 | **-0.42** | +0.42 |
| +22 | -1.28 | **-1.05** | +0.27 |

> +15·+22의 상급 값은 코드(`resolveProbability`)로 재계산해 수정함 (기존 -0.14, -0.85는 오기).

**고강화에서 실패의 진짜 비용은 골드가 아니라 잃는 단계다.**
여러 번 굴려 올린 단계가 한 번에 날아가는 것 — 상급 강화는 그 횟수를 줄인다.

#### 파괴방지권과의 관계

**겹치지 않는다. 축이 다르다.**

| | 작용 |
|---|---|
| 상급 강화 | **실패 자체를 줄인다** (성공률 상승) |
| 파괴방지권 | **실패의 결과를 바꾼다** (강등·리셋 → 형상유지) |

둘을 같이 쓰면 효과가 곱해진다. **+21 이상의 정석 조합.**
골드(상급)와 광고(파괴방지권)를 모두 요구하므로 수익 구조와도 맞물린다.

#### UI

강화 창에 버튼 두 개를 나란히 둔다.

```
현재 +14 · 강화석 5개 · 골드 12,000

[ 일반 강화 ]        [ 상급 강화 ]
  성공 38%             성공 57%
  1,000골드            5,000골드
```

- 골드가 부족하면 상급 버튼 비활성화 (숨기지 않는다 — 목표가 보여야 한다)
- 파괴방지권 체크박스는 양쪽 모두에 적용 가능

### 6.2-2 긴장감은 확률이 아니라 비용에서 나온다 `[✅ 구현 완료 — ENHANCE_GOLD_COST 존재, 구간 형태는 원안과 다름(5단계 구간이 아니라 레벨별 세분값)]`

**플레이 테스트에서 확인된 것**: 확률을 아무리 가혹하게 만들어도
강화 비용이 0이면 긴장이 생기지 않는다. 실패해도 잃는 것이 클릭 한 번뿐이기 때문이다.

**강화 비용 (골드·재료 도입 후 적용)**

| 강화 단계 | 비용 |
|---|---|
| +1 ~ +5 | 골드 소액 |
| +6 ~ +10 | 골드 + 강화석 1 |
| +11 ~ +15 | 골드 + 강화석 3 |
| +16 ~ +20 | 골드 + 강화석 7 |
| +21 ~ +25 | 골드 + 상급 강화석 3 |

> 비용이 붙는 순간 "한 번 더 눌러볼까"가 진짜 고민이 된다.
> **확률표 조정보다 이쪽이 우선순위가 높다.**

### 6.3 보조 아이템 `[🔄 부분 구현 — 강화권 4종은 구현, 파괴방지권·장비교체권은 미구현]`

**출처가 성격을 결정한다. 확정 진행은 보스에서만, 확률 개선은 사냥터에서.**

| 아이템 | 효과 | 획득 |
|---|---|---|
| **확정 강화권** `[✅]` | 실패 판정 없이 **+1 확정 상승** | **보스 클리어 시 50% 확률** (유일 출처) |
| 확률 강화권 (소) `[✅]` | 성공 확률 **×1.5** | 재료 몬스터 (60%) |
| 확률 강화권 (중) `[✅]` | 성공 확률 **×2.0** (상한 90%) | 재료 몬스터 (30%) |
| **확률 강화권 (대)** `[✅]` | 다음 강화 확률을 **50%로 고정** | 재료 몬스터 (10%) |
| 파괴방지권 `[⛔ 미구현]` | 실패해도 강등·리셋 없음 (형상유지) | 광고 시청 |
| 장비교체권 `[⛔ 미구현]` | 장비 슬롯에 새 장비 장착 | **광고 1회 시청** (원래 "파괴된 장비 슬롯" 용도였으나 장비 파괴 자체가 폐지됨 — 7.5 참조. 이 아이템의 존재 이유를 다시 정의해야 한다) |

> **[⛔ 미구현] 파괴방지권은 무기 강화 전용이다.** 장비 강화·파괴 개념이 없어졌으므로(7.5)
> 헷갈릴 이유는 없지만, 지금 이 절의 EV 표(6.2, 6.2-1)는 전부 "파괴방지권이 있다"고
> 가정하고 계산한 것이다. 실제로는 광고 연동도, 소지·사용 로직도 코드에 없다
> (`index.html`에 비활성화된 체크박스 자리만 있다). 즉 **지금 게임에서 +14 이상 강화는
> 아래 표의 "맨손 기댓값" 열이 곧 현실이고, "파괴방지권 사용 시" 열은 아직 도달할 수
> 없는 목표치다.**

#### 왜 대(大)는 배율이 아니라 고정값인가

배율은 낮은 확률에서 힘을 못 쓴다. +23에서 12%인데 ×2를 해도 24%다.
**고정값은 그 벽을 무시한다.**

| 강화 단계 | 기본 | 소 ×1.5 | 중 ×2.0 | **대 = 50% 고정** |
|---|---|---|---|---|
| +12 | 48% | 72% | 90% (상한) | 50% (오히려 손해) |
| +15 | 38% | 57% | 76% | 50% |
| +18 | 28% | 42% | 56% | 50% |
| **+21** | 18% | 27% | 36% | **50%** ⭐ |
| **+24** | 12% | 18% | 24% | **50%** ⭐ |

**+21 이상에서 대가 압도적, +15 이하에서는 중이 낫다.**
자연스럽게 "언제 쓸까"가 판단이 된다. 아껴뒀다가 고강화에서 쓰는 것이 최선이 되고,
그것이 정확히 의도한 바다.

#### 중복 사용 규칙

| 조합 | 가능 여부 |
|---|---|
| 확률권(소·중) + 상급 강화 | ⭕ 곱연산 |
| 확률권(소·중) + 파괴방지권 | ⭕ |
| **확률권(대)** + 다른 확률 아이템 | ❌ **불가** |
| **확률권(대)** + 상급 강화 | ❌ **불가** |
| **확률권(대)** + 파괴방지권 | ⭕ **가능** (효과 축이 다름) |

> 대는 이미 50%로 강력하다. 다른 걸 얹으면 90% 상한에 닿아 무의미해진다.
> 다만 파괴방지권은 **성공 확률이 아니라 실패의 결과**를 바꾸므로 겹쳐도 계산이 꼬이지 않는다.
> 이게 없으면 +24에서 대를 쓰고 실패했을 때 -2강이나 초기화를 그대로 맞아 너무 가혹하다.

#### UI

- 대를 체크하면 소·중·상급 강화 체크박스가 **자동 비활성화**
- 사용 시 확률 변화 즉시 표시 (`12% → 50%`)
- 보유 개수를 각각 표시

#### 확정 강화권을 보스 전용으로 한 이유

사냥터에서 굴러다니면 가치가 없다. 보스 보상으로 한정하면 **보스에 도전할 이유**가 하나 더 생긴다.
그리고 50%라 매번 나오지 않으므로 나왔을 때 반가움이 있다.

**저점 보장 장치이기도 하다.** PRD 6.2에서 계산했듯 +14 이상은 맨손 기댓값이 음수다.
운이 나쁘면 몇십 번 굴려도 제자리인데, 확정권만이 "반드시 앞으로 간다"를 보장한다.
확률권으로는 이 역할을 대신할 수 없다 — ×2를 해도 절반은 실패하기 때문이다.

#### 강화 확률 공개 (필수)

**성공 확률과 실패 분포를 숨기지 않는다.**

```
일반 강화 (700G) · 성공 28% → 공격력 48 (현재 43)
상급 강화 (3,500G) · 성공 42% → 공격력 48 (현재 43)

실패 시: 유지 10% / -1강 35% / -2강 26% / 초기화 1%
```

> 확률을 숨기면 유저가 판단을 못 한다. "+17에서 28%인데 700골드를 걸까"가 판단인데,
> 28%를 모르면 그냥 누르는 것이다. **공개해야 강화가 도박에서 선택이 된다.**
> -2강이 26%라는 걸 보면 +18에서 손이 떨린다. 그것이 이 게임이 원하는 감정이다.

---

## 7. 장비 `[🔄 설계 대폭 변경됨 — 이 문서에서 가장 크게 바뀐 챕터]`

### 7.0 등급 체계 (7단계) `[✅ 구현 완료 — 색상·배율 전부 data/items.js ITEM_GRADES와 일치]`

| # | 한글 | 영어 | 색상 | 성능 배율 | 강화 | 성격 |
|---|---|---|---|---|---|---|
| 1 | **일반** | Common | 흰색 `#e6e6e6` | ×1.0 | ~~⭕~~ | 초반 소모품 |
| 2 | **희귀** | Rare | 파랑 `#4da6ff` | ×1.4 | ~~⭕~~ | 중반 주력 |
| 3 | **영웅** | Epic | 보라 `#a64dff` | ×2.0 | ~~⭕~~ | 후반 주력 |
| 4 | **전설** | Legendary | 주황 `#ff9933` | ×3.0 | ~~⭕~~ | 목표 장비 |
| 5 | **유물** | Relic | 금색 `#ffd700` | ×4.5 | ~~⭕~~ | 고강화 대상 |
| 6 | **고대** | Ancient | 붉은색 `#e0393e` | **×8.0** | ❌ | **득템 자체가 보상** |
| 7 | **태초** | Primordial | 무지개 (hue 순환) | **×15.0** | ❌ | **판을 뒤집는 한 방** |

> **[🔄 설계 변경됨] "강화" 열은 전부 무의미해졌다.** 7.5가 통째로 폐지되면서
> 장비는 어떤 등급이든 "강화"라는 행위 자체를 하지 않는다(취소선 표시). 대신
> **아이템 레벨**(7.5 신설)이 같은 등급 안에서의 성장 축을 대신한다 — "고강화 유물"이
> 아니라 "고레벨 유물"이 목표 장비가 되는 식으로 바뀌었다.

> **[🔄 설계 변경됨] 위 성능 배율은 갑옷의 방어력(`defenseFlat`)에는 더 이상
> 그대로 쓰이지 않는다.** 장갑·신발(`attackPercent`/`speedPercent`)은 위 표를
> 그대로 쓰지만, 갑옷은 무한 모드 인커밍 데미지 재설계(PRD-forge-game-roblox.md
> 20.11-4) 과정에서 등급 간 증가폭이 단조 감소하도록 새로 만든 별도 배율표
> (`ARMOR_DEFENSE_GRADE_MULTIPLIER`, 1.184/2.397/3.903/5.866/8.347/11.25/15.0)를
> 쓴다 — `getItemStatValue`가 `statType`으로 두 표를 분기해야 한다.

**고대·태초를 다른 등급과 다르게 둔 이유**

~~강화는 "굴려서 올리는 재미"고, 득템은~~ 득템은 "떨어지길 기다리는 재미"다.
**기본 성능이 압도적**이어서, 유물보다 고대가 낫도록 설계한다(등급 배율 자체가 그 역할을 한다).

> 결과적으로 "유물을 더 굴릴까, 고대를 노릴까"라는 판단이 생긴다.

**등급명 설계 노트**
초안은 `유니크 / 전설`을 나란히 뒀으나, 한국 게임에서 두 단어는 사실상 같은 자리를 차지해
위계가 직관적으로 읽히지 않는다. `전설 → 유물 → 고대 → 태초`로 바꾸면
"오래된 것 → 더 오래된 것 → 시초"라는 시간 축으로 자연히 읽힌다.

### 7.0-1 캐릭터 레벨의 용도 = 아이템 레벨 각인 `[🔄 설계 변경됨]`

**~~장비를 강화할 수 있는 최대 단계는 현재 캐릭터 레벨과 같다.~~**
장비 강화가 없어지면서 "상한"이라는 개념 자체가 사라졌다. 캐릭터 레벨의 역할은
**드랍 시점에 아이템에 각인되는 아이템 레벨**로 바뀌었다(7.5 참조).

```
캐릭터 레벨 = 무기 경험치 레벨 (1~25, 4.2 참조)
아이템 레벨 = 드랍(획득) 시점의 캐릭터 레벨
```

**의도**: 캐릭터 레벨은 사냥만 하면 확정으로 오르는 축이다.
~~여기에 "장비 강화 상한 해금"이라는 역할을 주면~~ 여기에 "아이템 레벨 각인"이라는 역할을 주면
**레벨업 자체가 보상**이 된다 — 레벨이 높을 때 주운 장비일수록 같은 등급이라도 더 세다.
디아블로4식 "고레벨에 얻은 영웅이 저레벨 전설을 이길 수 있다" 구조와 같은 의도다.

> 고대·태초도 이 레벨 각인 규칙을 그대로 따른다 — 예전 설계("강화 불가라 레벨 무관")와 달리,
> 지금은 **어느 등급이든 언제 주웠느냐가 성능에 영향**을 준다(7.5).

### 7.1 드랍 방식 — 바닥에 떨어진다 `[✅ 구현 완료]`

**즉시 가방에 들어가지 않는다. 바닥에 떨어지고 주워야 한다.**

| 단계 | 표시 |
|---|---|
| 드랍 순간 | 바닥에 **등급 색상의 빛나는 아이템** 표시 |
| 드랍 시점에 알 수 있는 것 | **등급(색상)만** |
| 부위(갑옷/장갑/신발) | **주운 뒤에 확인** |

**의도**: 멀리서 붉은빛(고대)이 보이는 순간의 감정.
등급을 먼저 알려주면 "달려갈 가치가 있는가"가 즉시 판단되고,
부위를 숨기면 주울 때 한 번 더 두근거린다.

- 바닥 아이템은 60초 후 사라짐
- 코너 파밍 중 드랍이 쌓이면 회수 동선도 판단 요소가 된다

#### 다중 드랍 — 몬스터당 최대 2개 `[✅ 구현 완료 — dropSlot2Multiplier=0.3으로 수치 일치]`

**드랍 판정을 두 번 굴린다. 각 슬롯은 독립 확률.**

| 슬롯 | 드랍 확률 | 성격 |
|---|---|---|
| 1번 | 몬스터별 기본 드랍률 (예: 25%) | 주 보상 |
| 2번 | **1번의 30%** (예: 7.5%) | 보너스 |

- 두 슬롯 모두 7.2의 등급 확률표를 **각각 따로** 굴린다
- 따라서 한 마리에서 고대 두 개도 이론상 가능 (극히 희박)
- 2개가 나오면 바닥에 **약간 떨어뜨려 배치** — 겹쳐서 하나로 보이지 않게

> **2번 슬롯을 30%로 낮게 잡은 이유**: 같은 확률로 두 번 굴리면
> 실질 드랍률이 두 배가 되어 전체 밸런스가 무너진다.
> "가끔 두 개 나온다"는 놀라움만 주면 충분하다.

### 7.1-1 경험치 토큰 — 주워야 오른다 `[✅ 구현 완료]`

**몬스터 처치 시 경험치가 즉시 들어오지 않는다. 바닥에 토큰이 떨어진다.**

| 크기 | 색상 | 반지름 | 경험치 | 드랍 비중 |
|---|---|---|---|---|
| **소** | 하늘색 `#7dd3fc` | 4px | 기본값 ×1 | 70% |
| **중** | 청록 `#2dd4bf` | 6px | 기본값 ×3 | 25% |
| **대** | 밝은 청록 `#5eead4` | **8px** | 기본값 ×8 | 5% |

**색이 아니라 크기로 구분한다.** 초기 설계에서 대 토큰을 금빛으로 두었더니
전설 등급 장비(금색)와 헷갈려 매번 확인하러 가는 피로가 생겼다.

**색 체계 규칙 (반드시 지킬 것)**

| 계열 | 색상 | 용도 |
|---|---|---|
| 흰 · 파랑 · 보라 · **주황** · **금** · **붉은** · 무지개 | `#e6e6e6` `#4da6ff` `#a64dff` `#ff9933` `#ffd700` `#e0393e` | **장비 등급 전용** |
| 하늘 · 청록 | `#7dd3fc` `#2dd4bf` `#5eead4` | **경험치 토큰** |
| **자홍** | `#ff44aa` | **확정 강화권** |
| 초록 | `#33cc66` | **확률 강화권 · 파괴방지권** |

**✅ 확정 강화권 색상 수정 완료**

주황 `#ffa500`(전설 `#ff9933`과 거의 같은 색이라 혼동됨) → **자홍 `#ff44aa`로 이미 변경됨**
(`data/items.js`의 `ENHANCE_TICKET_TYPES.guaranteed.color`). 이 절이 지적했던 문제는 해결됐다.

> 색이 곧 정보다. 멀리서 보고 **"달려갈까 말까"**를 0.5초에 판단해야 하므로,
> 서로 다른 종류가 같은 색 계열을 쓰면 안 된다.

- 기본값은 몬스터 등급에 비례 (`data/monsters.js`의 `weaponExp`)
- 몬스터 한 마리당 토큰 **1~3개** 랜덤 (등급 높을수록 많이)
- **자동 흡수 반경**: 플레이어 반경 80px 안에 들어오면 빨려 들어옴
- 바닥 지속 시간 **30초** (장비보다 짧게 — 쌓이면 화면이 지저분해짐)

**설계 의도**

1. **처치 → 획득 사이에 동선이 생긴다.** 지금은 죽이면 끝이라 아무 감각이 없다
2. **코너 파밍과 맞물린다.** 몰아서 잡으면 토큰이 한곳에 쌓여 회수가 효율적이다
3. **대 토큰이 뜨는 순간의 작은 쾌감.** 5%라 자주는 아니지만 눈에 띈다

> 흡수 반경 80px은 "가까이 가면 알아서 들어오지만, 멀리 있는 건 걸어가야 하는" 거리다.
> 반경이 너무 넓으면 동선이 사라지고, 너무 좁으면 하나하나 밟고 다녀야 해서 성가시다.

**장비 흡수 옵션 (v2 예정)**
신발 특수 옵션으로 흡수 반경 +40px — 파밍 특화 세팅이 성립한다.

### 7.2 등급별 드랍 확률 — 몬스터 등급에 연동 `[✅ 구현 완료 — data/items.js DROP_GRADE_TABLE과 수치 일치]`

| 몬스터 등급 | 일반 | 희귀 | 영웅 | 전설 | 유물 | 고대 | 태초 |
|---|---|---|---|---|---|---|---|
| 1 | 90% | 10% | – | – | – | – | – |
| 2 | 70% | 27% | 3% | – | – | – | – |
| 3 | 45% | 40% | 14% | 1% | – | – | – |
| 4 | 20% | 40% | 30% | 9% | 1% | – | – |
| 5 | 5% | 25% | 40% | 25% | 4.5% | 0.5% | – |
| 6 | – | 10% | 30% | 40% | 18% | 1.9% | **0.1%** |

- **태초는 6등급 몬스터에서만, 0.1%**. 1,000마리에 한 번
- 고대는 5등급부터. 고등급 사냥터에 머무를 이유가 된다
- 드랍 자체가 발생할 확률(드랍률)은 이와 별개로 몬스터마다 설정(현재는 전 몬스터 공통 임시값 25%,
  `data/monsters.js`의 `dropChance` — 아직 몬스터별로 세분화되지 않았다)

> 이 표가 PRD 8.0-2의 딜레마에 답을 준다.
> **"강화 재료를 빨리 모을까, 장비를 노릴까"** — 고등급으로 갈수록 후자가 유리하다.

### 7.2-1 착용 등급 연동 드랍 보정 `[✅ 구현 완료 — 신규, 위 표에 없던 내용]`

**착용 중인 장비가 좋을수록, 다음 등급이 조금 더 잘 나온다.** 위 7.2 표는 순수 몬스터 등급
기준이고, 이 보정은 그 위에 곱해지는 별도 레이어다(`core/loot.js` `getBoostedDropTable`).

```
기준 등급 = 착용 3부위 중 최하위 등급 (미착용 부위는 일반 취급)
대상 등급 = 기준 등급 + 1단계만 (그 위 등급은 손대지 않는다 — 등급을 건너뛰면 성장 곡선이 끊겨 보임)
보정량   = min(weight=0.15, 원래확률 × maxMultiplier=3.2)  // 둘 중 작은 쪽이 실제 상한
차감     = 대상보다 낮은 등급에서만, 확률에 비례해서 (대상과 같거나 높은 등급은 건드리지 않음)
```

- **태초는 항상 보정 대상에서 제외한다** — 최종 목표 아이템이라 과하게 풀리면 안 된다.
  고대는 보정 대상이 될 수 있다(3부위 유물 착용 시 고대 확률이 오르도록 승인된 밸런스 조정).
- 원래 확률이 아주 작은 등급(예: 고대)은 `weight`가 항상 `원래확률×3.2`를 압도해
  사실상 `maxMultiplier`만이 실질 상한이 된다. 원래 확률이 큰 등급(예: 전설)은 반대로
  `weight`가 상한이 된다.
- 3부위 유물 착용 시 5등급 몬스터의 고대 확률이 0.5% → 1.6%까지 오른다(시뮬레이션 확인).

> **의도**: 이미 좋은 장비를 낀 유저가 사냥을 계속할 이유. 장비에 랜덤 옵션이 붙는
> 지금 시점부터는, 같은 등급 안에서도 옵션 조합으로 가치가 갈리므로("원하는 옵션이
> 붙은 유물"은 여전히 희소) 등급 자체의 문턱을 살짝 낮추는 이 조정과 방향이 맞는다(7.5).

### 7.3 장비창 (I 키) `[✅ 구현 완료]`

| 항목 | 내용 |
|---|---|
| 열기 | **I 키** (다시 누르면 닫힘) |
| 착용 슬롯 | 갑옷 / 장갑 / 신발 |
| 가방 | 20칸 (`BALANCE.inventoryBagSize` — 로블록스에서 50·100칸으로 유료 확장 예정, PRD-forge-game-roblox.md 20장 참조) |
| 표시 | 아이템 테두리를 **등급 색상**으로 |
| 조작 | 클릭으로 착용·해제, 우클릭으로 판매, **Shift+좌클릭으로 잠금 토글**(신규, 아래) |

**슬롯 기본 효과**

| 부위 | 기본 효과 |
|---|---|
| 갑옷 | 방어력 +X |
| 장갑 | 공격력 +X% |
| 신발 | 이동·공격속도 +X% |

> **[🔄 설계 변경됨] 장갑·신발 기본 효과가 +5% → +15%로 올랐다.**
> 스탯 계산식이 `1 + 값` 구조(1절 참고)라 밑깔림 1이 레벨 1→25 체감 증가폭을
> 갑옷(flat 가산이라 밑깔림 없음)보다 훨씬 약하게 눌렀다 — 전설 기준 Lv1→25 배율이
> 갑옷 1.69~1.96배인데 장갑은 1.19배에 그쳤다. `base`를 0.05→0.15로 올려
> 1.45배까지 끌어올렸다(완전히 같아지진 않는다 — `ratio(x)=(1+2.44x)/(1+x)`가
> `x→∞`로 가야 2.44에 수렴하는 구조라서, 격차를 줄일 순 있어도 없앨 순 없다).

#### 7.3-1 일반 옵션 — 랜덤 수치형 `[✅ 구현 완료 — 신규, PRD 7.3 개정]`

**PRD와의 차이**: 원래 설계는 "특수 옵션은 영웅 이상에서 낮은 확률, 부위당 1종"이었다.
실제로는 그보다 확장된 **"등급마다 여러 개 붙는 랜덤 옵션"**으로 구현됐다. 아래가 새 설계고,
기존의 "부위당 1종·규칙을 바꾸는 옵션"은 7.3-2(특수 옵션)로 이름을 바꿔 분리했다.

| 등급 | 옵션 개수 |
|---|---|
| 일반 | 0 |
| 희귀 | 1 |
| 영웅 | 2 |
| 전설 | 2 |
| 유물 | 3 |
| 고대 | 3 |
| 태초 | 4 |

- **개수는 등급별 고정값**이지 범위가 아니다 — 개수까지 랜덤이면 "등급+개수+스탯+수치"가
  겹쳐 같은 등급 안 편차가 너무 커져 등급 자체의 의미가 죽는다
- 옵션 풀은 **10종**: 방어력·공격력·이동속도·치명타율·치명타피해·회복량·**라이프스틸**·
  스킬쿨다운감소·대시쿨다운감소·무적시간 중, **그 부위의 기본 스탯과 위협수치·제어력(아직
  소비하는 코드가 없는 v3 전용 자리, 15장 참고)을 제외**한 것 `[🔄 라이프스틸 추가 — 설계
  승인, 15.2-1 참고]`
- **한 아이템 안에서 같은 스탯 중복 불가** (무복원추출)
- 값 = `optionBase × 등급배율 × 아이템레벨계수 × rollFactor[0.7~1.3]`
  — 등급·레벨 배율은 기본 효과와 같은 축을 공유해서 일관성을 유지하고, `rollFactor`가
  "같은 등급이라도 롤에 따라 갈리는" 파밍 동기를 만든다
- **옵션 하나가 전용 부위 기본효과의 대략 10% 크기**다 — 등급이 주 파워 축, 옵션은 그 위의
  보조 축이라는 위계를 지키기 위해서다. 너무 세면 등급이 무의미해지고, 너무 약하면
  옵션 자체가 무의미해진다.
- 툴팁에 옵션의 **롤 품질**(0~100%, 색으로도 구분)을 표시한다 — "원하는 옵션을 먹기까지
  파밍한다"가 이 시스템의 존재 이유라, 롤의 좋고 나쁨이 안 보이면 그 절반이 죽는다.
  단, 가방 슬롯 배지에는 개수만 표시하고 품질은 넣지 않는다 — 아이템 하나를 보는 툴팁과
  20~100칸을 훑는 슬롯은 요구가 다르다.

#### 7.3-2 특수 옵션 — 규칙형 (고대·태초 전용) `[⛔ 미구현 — 목록·방향 확정, 수치는 전부 TBD]`

**부위당 1종.** 수치가 아니라 규칙을 바꾸는 옵션이라, 여러 개 중첩되면 밸런스가
조합적으로 깨지기 쉽다 — 그래서 일반 옵션과 달리 이 제한을 유지한다.

**모든 특수 옵션의 효과는 "내 공격력 기준 배수"로만 정의한다.** "적 최대체력의
X%"·"적 현재체력 비례" 형태는 금지 — 무한 모드에서 몬스터 HP가 지수로 자라므로
그런 식으로 정의하면 %피해도 같이 지수로 자라 다른 모든 딜을 압도한다. **조건은
%로, 결과는 배수로** 쓴다(예: "체력 30% 이하 적에게 피해 ×TBD배"는 되지만
"적 최대체력의 TBD%"는 안 된다).

**부위별 성격 배분** — 옵션 한 줄만 보고 팔지 말지 판단할 수 있어야 한다는 게
목적이다. `[🔄 설계 변경됨]` **웹 v1은 장비 부위가 3개뿐이라(`ITEM_PARTS`,
`data/items.js` — 무기는 장착 슬롯이 아니라 캐릭터 고유 스탯) 이 절이 한때
갑옷·장갑·신발 3부위로만 재배치됐었다.** 로블록스 전환에서 **무기를 정식
장착 슬롯으로 승격**하기로 하면서(PRD-forge-game-roblox.md 20.14) 부위가 다시
4개가 됐고, 아래는 그 4부위 기준으로 되돌린 표다:

| 부위 | 성격 |
|---|---|
| 무기 | 딜(추가 타격·처형·스킬 강화) |
| 갑옷 | 생존(방어막·피해 감소·즉발 안전장치) |
| 장갑 | 공속·치명타·쿨다운 |
| 신발 | 이동·회피·기동 |

부위당 4개(고대 2 + 태초 2), 총 16개로 확정한다. 그 이상 만들지 않는다.

| 등급 | 부위 | 이름 | 효과(한 줄) |
|---|---|---|---|
| 고대 | 무기 | 연속격 | 3타 강타가 2타마다 발동 |
| 고대 | 무기 | 속사의 흔적 | 치명타 적중 시 확률(TBD%)로 추가 타격 발동, 피해 ×TBD배 |
| 태초 | 무기 | 심판의 표식 | 체력 30% 이하 적에게 주는 피해 ×TBD배(처형) |
| 태초 | 무기 | 삼위일체 | 스킬 적중 직후 다음 평타가 강화되어 추가 피해 ×TBD배 |
| 고대 | 갑옷 | 최후의 저항 | 체력 1칸 남으면 3초 무적(쿨다운 TBD) |
| 고대 | 갑옷 | 가시덤불 | 피격 시 확률(TBD%)로 공격자에게 내 공격력×TBD배 반사 |
| 태초 | 갑옷 | 불사조의 가호 | 치명적 피격 1회를 체력 1칸으로 대체(판당 1회) |
| 태초 | 갑옷 | 방벽의 잔향 | 방어막이 깨지면 TBD초 뒤 쿨다운 없이 즉시 1회 재생성(판당 1회) |
| 고대 | 장갑 | 여진 | 치명타 적중 시 확률(TBD%)로 스킬 쿨다운 1초 감소 |
| 고대 | 장갑 | 가속하는 손 | 적중할 때마다 공격속도 증가 스택 누적, 최대치에서 정점 |
| 태초 | 장갑 | 완벽한 순간 | 치명타 확률이 TBD% 이상이면 다음 공격이 확정 치명타로 전환(쿨다운 TBD) |
| 태초 | 장갑 | 질풍의 재장전 | 스킬 사용 시 확률(TBD%)로 다른 스킬 쿨다운 TBD초 감소 |
| 고대 | 신발 | 그림자 각인 | 대시 중 무적 |
| 고대 | 신발 | 연쇄 스텝 | 대시 경로에 스친 적마다 피해 ×TBD배(관통형 보조딜) |
| 태초 | 신발 | 잔영 질주 | 대시 2연속 사용 가능(충전식) |
| 태초 | 신발 | 폭풍의 잔재 | 무피격 스택이 최대치에 도달하면 다음 공격이 주변으로 확산, 피해 ×TBD배 |

(연속격·최후의 저항·가시덤불·불사조의 가호·방벽의 잔향·여진·그림자 각인·연쇄
스텝·잔영 질주·폭풍의 잔재·심판의 표식·삼위일체 12개는 3부위 시절 목록을
그대로 유지한 것이고, 속사의 흔적·가속하는 손·완벽한 순간·질풍의 재장전
4개가 무기/장갑이 새로 나뉘며 채워야 했던 자리에 새로 추가한 것이다.)

**표시 규칙**: 특수 옵션은 일반 랜덤 옵션(7.3-1)과 **다른 색**으로, 툴팁에서
`"특수 — 효과"` 순서로 표시한다 — 7.3-1의 "롤 품질을 색으로 구분"과 같은 원리로,
"규칙을 바꾸는 옵션"과 "수치만 다른 옵션"을 한눈에 구분할 수 있어야 한다.

**밸런스 검증·확률·정확한 배수는 이번 라운드에서 정하지 않는다** — 구현 후
실플레이 데이터로 잡는다(위 표의 TBD가 그 자리다).

**추가 검토 방향** (우선순위 판단은 다음 라운드에서):
- **이펙트 변환** — 밸런스에 손대지 않는 순수 외형 변화(예: 화살이 궤적을 남긴다,
  타격에 고유 파티클이 붙는다). **로블록스 전환 시 코스메틱 수익 모델과 직결**되므로
  (PRD-forge-game-roblox.md 20장 참고) 우선순위를 높게 볼 만하다 — 확률형 판매 규제를 피하면서 과금 여지를
  만드는 몇 안 되는 합법적 축이다.

### 7.3-3 아이템 잠금 — 판매 사고 방지 `[✅ 구현 완료 — 신규]`

옵션이 생기면서 "등급은 낮은데 옵션이 좋은" 아이템이 나온다. 일괄판매는 등급만 보므로
그런 아이템도 그냥 팔아버린다. 좋은 옵션템을 실수로 판 순간 유저가 게임을 그만둘 수 있다.

- **조작**: `Shift+좌클릭`으로 잠금 토글. 툴팁에 버튼을 더 넣으면 판매 버튼과 붙어 오폭
  위험이 있고, 우클릭은 이미 판매 동작이라 이 조합키가 유일하게 비어 있었다
- **차단 범위**: 잠긴 아이템은 **우클릭 판매·툴팁 판매 버튼·일괄판매** 세 경로 모두에서
  제외된다. 일괄판매 확인창에는 `"(잠긴 N개 제외)"`를 표시해 잠금이 실제로 걸렸는지
  눈으로 확인할 수 있게 한다
- **표시**: 가방 슬롯 좌상단에 자물쇠 아이콘(레벨 배지는 우상단이라 반대쪽), 호버 없이
  항상 보임
- 저장 데이터에 포함된다 (`item.locked`, SAVE_VERSION 4, 14장 참고)

### 7.4 자동 판매 → 일괄판매 (수동) `[🔄 설계 변경됨]`

**PRD 설계는 "드랍 즉시 자동으로 팔린다"였다. 실제 구현은 인벤토리 안의 수동 "일괄판매"
버튼이다** — 선택한 등급 이하를 한 번에 판다(잠긴 아이템은 자동 제외, 위 7.3-3).
드랍 시점에 자동으로 골드로 바뀌는 버전은 구현되지 않았다.

**자동 판매(설계)와 가방 확장(PRD-forge-game-roblox.md 20장)의 역할 분담**

| 기능 | 역할 | 상태 |
|---|---|---|
| 일괄판매(자동 판매의 실현형) | **잡템 처리** — 등급 낮은 것을 한 번에 치운다 | ✅ 수동 버전 구현 |
| 가방 확장 (50·100칸) | **상위 등급 보관** — 판단을 미루고 쌓아둘 공간을 넓힌다 | ⛔ 로블록스에서 유료 상품으로 (PRD-forge-game-roblox.md 20장) |

둘 다 궁극적으로 로블록스에서 광고·유료 상품이 될 자리다(PRD-forge-game-roblox.md 20장). **역할이 겹치면 안 된다** —
잡템 처리가 잘 되면 가방 확장을 살 이유가 줄고, 가방이 무한히 넓으면 잡템 처리를 볼 이유가
준다. 일괄판매는 "낮은 등급을 안 쌓이게" 하는 용도로, 가방 확장은 "높은 등급을 더 갖고
있게" 하는 용도로 서로 다른 니즈를 겨냥해야 둘 다 팔린다.

```
(설계 원안) 자동 판매 대상 = 착용 중인 같은 부위 장비보다 2등급 이상 낮은 것
예) 영웅 갑옷 착용 중 → 일반 갑옷 자동 판매, 희귀는 남김
```

- 착용 장비가 좋아질수록 자동으로 처리 범위가 넓어지는 것이 원래 의도 — 지금의 수동
  일괄판매도 등급 드롭다운으로 같은 효과를 낼 수 있다
- **정확히 1등급 아래는 남긴다** — 교체 후보가 될 수 있으므로(일괄판매도 동일)
- 고대·태초는 **어떤 경우에도 판매 목록에 들어가지 않는다** (`BULK_SELLABLE_GRADES`)

> 후반에 가장 귀찮은 작업이 바닥·가방의 잡템 회수다. 자동이든 수동이든, 이 부담을 줄이는
> 기능은 광고·유료 전환의 좋은 자리다 — 강제 노출보다 자발적 사용이 늘 낫다.

### 7.5 아이템 레벨 시스템 `[✅ 구현 완료 — 신규, 장비 강화(파괴)를 대체]`

**옛 7.5("장비 강화 — 실패 시 파괴")는 폐지됐다.** 장비는 더 이상 강화하지 않고, 실패해도
파괴되지 않는다. 대신 **획득(드랍) 시점의 캐릭터 레벨이 아이템에 각인**되고, 그 레벨이
같은 등급 안에서의 성장 축이 된다.

```
아이템 스탯 = base × 등급배율 × (1 + 0.06 × (아이템레벨 − 1))
```

`0.06`은 무기 경험치 공격력 계수(4.2, `BALANCE.weaponExpAttackBonusPerLevel`)와
**같은 값을 그대로 재사용**한다 — 레벨 25에서 무기 공격력과 아이템 스탯이 나란히
2.44배로 정점을 찍도록 의도적으로 맞췄다.

**의도**: 디아블로4식 "고레벨에 얻은 영웅이 저레벨 전설을 이길 수 있다." 등급 하나만으로
전투력이 정해지지 않고, 레벨이 등급을 일부 뒤집을 수 있어야 파밍이 계속 의미 있다.

**등급 뒤집힘에 필요한 레벨차** (인접 등급 기준, 이 계수로 역산):

| 인접 등급 | 등급 배율차 | 뒤집는 데 필요한 레벨차 |
|---|---|---|
| 일반↔희귀, 희귀↔영웅 | ×1.4 | 약 8레벨 |
| 영웅↔전설, 전설↔유물 | ×1.5 | 약 9레벨 |
| 유물↔고대 | ×1.78 | 약 14레벨 |
| 고대↔태초 | ×1.875 | 약 16레벨 |

위 등급일수록 레벨만으로 뒤집기 어렵게 남겨 등급의 희소성을 지킨다.

- 던전에서 획득한 장비는 **가방에 저장** (7.3)
- 가방에서 자유롭게 착용·해제 가능
- 기존 저장(옛 7.5의 `enhanceLevel`)은 SAVE_VERSION 마이그레이션으로 아이템 레벨로
  변환됐다 (14장 참고)

## 8. 던전 `[🔄 부분 구현 — 구역 분리·타이머·코너파밍·보스존은 완료, 상자·다국어 배경은 일부만]`

### 8.0-1 구역 분리 — 사냥터와 대장간 `[✅ 구현 완료]`

**한 화면을 위아래 두 구역으로 나눈다.** 이동 자체가 선택의 비용이 된다.

```
┌─────────────────────────────┐
│        대장간 (위)            │  ← 강화 · 가방 · 상점
│  안전 지대. 몬스터 없음        │
├─────────────────────────────┤
│                             │
│        사냥터 (아래)          │  ← 몬스터 15~20마리 배치
│  등급별로 구역이 나뉘어 있음    │
└─────────────────────────────┘
```

| 구역 | 기능 | 몬스터 | **보스 타이머** |
|---|---|---|---|
| **대장간** | 무기 강화 · ~~장비 강화~~ · 가방 관리 · 상점 | 없음 (완전 안전) | **정지** |
| **사냥터** | 몬스터 사냥 · 아이템 드랍 | 있음 | 흐름 |

### 8.0-1-a 타이머 정지 — 핵심 규칙 `[✅ 구현 완료, 🔄 타이머 길이·배속은 변경됨]`

**대장간에 있는 동안 보스 타이머는 흐르지 않는다.**

> **[🔄 설계 변경됨] 타이머 길이 600초(10분) → 1,800초(30분).** `BALANCE.bossTimerDuration`.
> 대신 **배속(1x/2x/3x, `BALANCE.gameSpeedOptions`) 기능이 새로 생겼다** — 파밍량은
> 배속과 무관하게 그대로 두고, 실제 걸리는 시간만 압축한다. 3배속이면 1,800초가 실질
> 10분이 되어, 이 문서가 원래 말하던 "10분"과 결과적으로 같아진다. 배속을 두는 핵심
> 원칙: **배속은 이득을 사는 게 아니라 실제 시간을 압축하는 것뿐**이다 — 그래야 나중에
> 12장 기록표가 붙어도 "배속을 안 켠 사람이 손해"라는 치팅 축이 생기지 않는다.
> **보스맵에서는 배속을 1로 강제한다** — 전투 난이도까지 압축되면 안 되므로.
>
> **[✅ 구현 완료, 신규] 보스 결과창이 떠 있는 동안 시뮬레이션을 정지한다.** 안 멈추면
> 결과창을 안 닫고 사냥터에서 몬스터를 계속 잡을 수 있어 무한 안전 파밍이 된다.

이것이 구역 분리의 가장 큰 이유다. 결과적으로 게임 구조가 이렇게 바뀐다.

| | 타이머가 흐를 때 | **타이머 정지 (채택)** |
|---|---|---|
| 강화를 제한하는 것 | 시간 | **재료 획득량** |
| 플레이어의 판단 | 언제 강화할까 | **어떤 몬스터를 잡을까** |
| 10분의 의미 | 전체 플레이 시간 | **순수 사냥 시간** |

**따라오는 조정 사항**

1. **재료 드랍률이 게임 속도를 결정하는 유일한 손잡이가 된다.**
   `data/monsters.js`의 드랍률 하나로 전체 진행 속도를 조절할 수 있다.
2. **보스 권장 강화 수치를 상향해야 한다.**
   10분이 순수 사냥 시간이 되면 기존 설계보다 훨씬 많이 성장한다.
   PRD 9.3의 권장 강화(+6 → +10 → +14 → +17 → +20 → +23)는
   11단계 밸런스 조정에서 실측으로 다시 잡는다.
3. 플레이어는 대장간에서 부담 없이 가방·장비를 정리할 수 있다.

> 강화 타이밍이라는 고민은 사라지지만, **몬스터 선택**이라는 고민이 그 자리를 대신한다.
> 그리고 총 플레이 시간이 늘어난다 — 한 판이 배속 없이는 30분, 3배속으로도 순수
> 사냥에 꽉 채운 10분이 걸린다(대장간 체류 시간은 별도).

### 8.0-1-b 강화 조작 — 구간별 분리 `[✅ 구현 완료]`

| 강화 구간 | 방식 | 이유 |
|---|---|---|
| **+1 ~ +10** | **자동** (대장간 진입 시 재료가 있는 만큼 자동 진행) | 성공률 50~95%. 반복 작업일 뿐 긴장이 없다 |
| **+11 이상** | **수동 탭** — 버튼을 직접 클릭 | 계수가 0.20 → **0.40**으로 두 배가 되는 지점. 위험도 함께 뛴다 |

> **+11을 경계로 잡은 이유**: PRD 6.1 계수표에서 단계당 데미지 증가폭이
> 정확히 여기서 두 배가 된다. **탭이 뜨는 순간 = 데미지 체감이 뛰기 시작하는 순간**이
> 일치해야 "여기서부터 진짜다"라는 감각이 생긴다.
>
> 지루한 구간은 자동화하고, **긴장되는 구간만 손에 쥐어준다.**

### 8.0-1-c 몬스터 선택 유지 `[✅ 구현 완료]`

- 사냥터에서 고른 목표 몬스터는 **대장간에 다녀와도 그대로 유지**된다
- 흐름: `몬스터 선택 → 안 잡힘 → 대장간에서 강화 → 사냥터 복귀 → 같은 몬스터 재도전`
- 이 왕복이 이 게임의 기본 리듬이다

**설계 의도 — 안전한 계획 공간**

대장간은 시간이 멈추는 곳이다. 몬스터에게 쫓기지 않고 가방을 정리하고,
강화를 시도하고, 다음에 어떤 몬스터를 노릴지 정한다.
**한 판의 리듬을 "긴장(사냥) → 이완(대장간)"으로 나누는 장치.**

**강화는 완전 자동이 아니다**

- 대장간 진입 시 **+10까지는 자동 진행**, **+11부터 수동 탭이 열린다**
- 수동 구간에서는 **반드시 직접 클릭**한다
- 재료가 있는 한 연타 가능 (답답하지 않게)

> 고강화 구간에서 자동 강화는 넣지 않는다. **"내가 눌러서 +18이 1강이 됐다"**와
> "저절로 돌다가 1강이 됐다"는 완전히 다른 경험이다.
> 이 게임의 핵심 감정은 전자에서만 나온다.

### 8.0-1-d 맵 규격과 경계 `[✅ 구현 완료 — 수치 일치]`

**현재 사냥터가 좁다. 확장이 필요하다.**

| 항목 | 값 | 비고 |
|---|---|---|
| 캔버스 (보이는 화면) | 800 × 600 | 고정 |
| **사냥터 맵 전체** | **2400 × 1600** | 캔버스의 약 3배 × 2.7배 |
| 대장간 | 800 × 600 | 화면 하나에 들어옴 (카메라 불필요) |
| 벽 두께 | 20px | 맵 가장자리 |

**필수 구현 — 카메라와 경계**

맵이 화면보다 커지므로 **카메라가 플레이어를 따라다녀야 한다.**

```js
// data/balance.js
mapWidth: 2400,
mapHeight: 1600,
wallThickness: 20,

// core/render.js — 카메라 오프셋
camera.x = clamp(player.x - canvas.width/2,  0, mapWidth  - canvas.width);
camera.y = clamp(player.y - canvas.height/2, 0, mapHeight - canvas.height);

// main.js — 플레이어 위치 제한 (맵 밖으로 못 나감)
player.x = clamp(player.x, wallThickness + player.radius,
                 mapWidth  - wallThickness - player.radius);
player.y = clamp(player.y, wallThickness + player.radius,
                 mapHeight - wallThickness - player.radius);
```

- 몬스터와 투사체도 같은 경계 규칙을 적용
- 벽은 시각적으로 그려서 **어디가 끝인지 보이게** 한다
- 맵 크기는 `data/balance.js`에 두어 **나중에 숫자만 바꾸면 조절 가능**하게

> 코너 파밍(8.0-3)이 성립하려면 맵에 **명확한 구석**이 있어야 한다.
> 벽 없이 무한히 밀려나면 몰아넣기가 불가능하다.

### 8.0-2 몬스터 배치와 선택 `[✅ 구현 완료]`

**메뉴에서 등급을 고르는 방식이 아니라, 걸어가서 때려보는 방식.**

- 사냥터에 **몬스터 15~20마리**가 등급별 구역으로 나뉘어 배치
- 아래로 갈수록(또는 오른쪽으로 갈수록) 고등급
- 플레이어가 직접 걸어가서 공격 → 안 죽으면 물러난다

> 등급 선택 UI보다 낫다. "이건 아직 안 죽네"를 몸으로 느끼고,
> 강화 후 다시 가서 죽는 걸 보는 순간이 성장 실감이 된다.

**장비 파밍과의 딜레마**

고등급 몬스터일수록 좋은 장비를 준다. 그런데 잡는 데 오래 걸린다.
**"강화 재료를 빨리 모을까, 장비를 노릴까"**를 플레이어가 스스로 저울질하게 둔다.
정답을 알려주지 않는다.

### 8.0-3 코너 파밍 — 위험을 감수한 속도 `[✅ 구현 완료]`

| 상황 | 몬스터 행동 |
|---|---|
| 기본 | 제자리 대기. 플레이어가 다가가야 전투 |
| **플레이어가 구석으로 이동** | 몬스터가 플레이어 쪽으로 이동 |
| 몬스터가 자리를 비움 | **빈 자리에 즉시 새 몬스터 스폰** |

**결과**: 구석에 몰아넣으면 사냥 속도가 크게 오른다.
**대가**: 여러 마리에게 동시에 맞는다. 체력 10칸 · 피격 상한 30%이므로
3~4마리에 둘러싸이면 순식간에 위험해진다.

> **위험을 감수하면 빨라지는 구조.** 실력 보상이 되고, 활(방어력 0.6배)에게는
> 특히 도박이 된다. 대검은 상대적으로 안전하게 코너 파밍이 가능하다.

**성능 제한 (반드시 적용)**

| 항목 | 값 |
|---|---|
| 동시 생존 몬스터 상한 | **20마리** |
| 리스폰 간격 하한 | 0.3초 |

> 상한이 없으면 코너에서 무한히 쌓인다. 20마리 · 투사체 수십 개는
> Canvas 2D에서 성능 문제가 없다 (체감 렉은 보통 500개 이상부터).

### 8.0-4 강화 재료 상자 `[⛔ 미구현]`

몬스터 대신 **낮은 확률로 상자가 스폰**된다.

| 항목 | 값 |
|---|---|
| 등장 확률 | **2~4%** (몬스터 스폰 시 판정) |
| 보상 | 선택한 몬스터 구간에 비례 — 고등급 구역일수록 좋은 재료 |
| 외형 | 반짝이는 상자. 공격하지 않음 |

```
상자 보상 등급 = 해당 구역 몬스터 등급 기준
예) 5등급 구역 상자 → 상급 강화 재료 + 파괴방지권 확률
```

> **의도**: 고등급 구역에 머무를 이유를 하나 더 만든다.
> 잡기 어려운 곳에서 버티는 플레이가 보상받아야 도전 동기가 생긴다.

### 8.0 배경 테마 — 단계별 상승 구조 `[🔄 부분 구현 — 4/6단계]`

**아래에서 위로 올라가는 여정.** 단계가 오를수록 배경이 밝아지고 시야가 트인다.

| 단계 | 배경 | 색조 | 분위기 | 상태 |
|---|---|---|---|---|
| 1 | 지하 깊은 곳 | 어두운 자주 · 검정 | 좁고 답답함. 횃불 빛 | ✅ |
| 2 | 지하 동굴 | 회색 · 청록 | 광물 반사, 조금 넓어짐 | ✅ |
| 3 | 지상 · 폐허 | 갈색 · 황토 | 처음 하늘이 보임 | ✅ |
| 4 | 거대 나무 | 초록 · 갈색 | 나뭇가지 위, 잎 사이 햇빛 | ✅ |
| 5 | 구름 위 | 흰색 · 하늘색 | 발밑이 구름, 개방감 | ⛔ |
| 6 | 천공 | 짙은 파랑 · 금색 | 별과 오로라. 최종 | ⛔ |

> **[🔄 설계 변경됨]** `data/stages.js`의 `STAGES`는 1~4단계만 있다. 또한 원래 설계는
> "1단계=1몬스터등급"의 1:1 대응이었는데, 실제 구현은 각 던전이 몬스터 등급 **범위**를
> 겹쳐서 가진다(예: 1단계는 1~3등급, 2단계는 2~4등급 — 한 던전 안에서도 저~중~고등급을
> 동시에 만난다). 8.0-2의 "걸어가서 때려보는" 설계와는 맞물리지만, 이 표가 암시하는
> "한 단계 = 한 등급"과는 다르다.

**확장 예정 (v2 이후)**

| 단계 | 배경 | 색조 |
|---|---|---|
| 7 | 천국 | 백금 · 순백 |
| 8 | 지옥 | 진홍 · 검정 |

**구현 방식 (초기)**
- 배경은 **단색 그라데이션 + 단순 도형 실루엣** 2~3개로 시작
- 배경 이미지는 나중에 교체 가능하도록 `stage.background` 값 하나만 참조
- 스크롤·패럴랙스는 v2 이후

> **의도**: 배경이 바뀌는 것만으로도 "진도가 나가고 있다"는 감각이 생긴다.
> 6단계를 다 비슷한 화면에서 하면 1시간이 지루해진다.

### 8.0-5 희귀 몬스터 — 발견의 재미 `[✅ 구현 완료 — 확률·보상 수치 일치]`

일반 스폰 시 낮은 확률로 **특별한 몬스터**가 대신 등장한다.

| 종류 | 등장 확률 | 외형 | 보상 |
|---|---|---|---|
| **반짝이 몬스터** | **0.5%** | 무지개빛 반짝임 + 파티클 | **유물 이상 확정 드랍** (유물 90% / 고대 9% / 태초 1%) |
| **재료 몬스터** | **1.5%** | 금빛 테두리 | **확률 강화권** (소 60% / 중 30% / 대 10%) |

**공통 규칙**

- 스폰 시점에 판정. 일반 몬스터를 대체한다
- 체력·방어력은 해당 구역 등급을 따르되 **골드는 적게** (보상이 아이템이므로)
- 도망가지 않는다 — 발견하면 반드시 잡을 수 있어야 한다
- 화면 밖에 있어도 **미니 화살표로 방향 표시** (놓치면 억울하다)
- **보상은 즉시 지급하지 않고 바닥에 떨어뜨린다** (7.1 드랍 방식과 동일)
  - 확률 강화권: **초록빛** `#33cc66`
  - 확정 강화권: **자홍** `#ff44aa` (전설 등급 주황과 구분)
  - 소·중·대는 크기로 구분, 30초 후 소멸
  - 주우면 `"확률 강화권(중) 획득"` 안내 2초 표시

> **의도**: 장비 드랍은 "잡고 나서" 알게 되는 보상이고,
> 희귀 몬스터는 **"잡기 전에" 보이는 보상**이다. 감정이 다르다.
> 화면에 반짝이는 게 뜨는 순간 무조건 달려가게 된다.
>
> 보상을 바닥에 떨어뜨리는 이유 — 즉시 지급하면 **얻었다는 감각 자체가 없다.**
> 주우러 가는 동선이 있어야 획득이 사건이 된다.

### 8.0-6 보스존 — 완전히 분리된 공간 `[✅ 구현 완료]`

**보스존은 사냥터와 같은 맵에 두지 않는다.**

| 항목 | 규칙 |
|---|---|
| 공간 | 사냥터와 **별개의 전용 맵** |
| 잡몹 | **없음** — 보스와 1대1만 |
| 진입 위치 | 보스와 **최소 400px 떨어진 지점**에 스폰 |
| 진입 연출 | 화면 페이드 아웃 → 보스존 → 페이드 인 |
| 시작 유예 | 진입 후 **2초간 보스 정지** (준비 시간) |
| **체력 회복** `[✅ 구현 완료, 신규]` | **진입 시 체력 전체 회복.** 대시 쿨다운·무적 타이머도 함께 초기화 |
| 퇴장 | 클리어·실패 시 사냥터로 복귀 |

> **[✅ 구현 완료, 신규]** 보스전은 파밍과 별개의 도전이므로, 사냥터에서 깎인 체력
> 상태를 그대로 물려받을 이유가 없다 — 진입 즉시 풀피로 시작한다. 대시 쿨다운·
> 무적시간 타이머를 같이 초기화하지 않으면 사냥터에서 막 대시를 썼거나 맞은 직후
> 보스존에 들어갔을 때 그 페널티가 그대로 넘어와 불공평하게 불리해진다.

**해결하는 문제**

1. 잡몹이 따라 들어와 보스전이 난장판이 되는 것
2. 보스와 같은 자리에 스폰되어 시작하자마자 맞는 것
3. 보스가 사냥터 몬스터와 겹쳐 보이는 것

> 별도 맵이므로 카메라 경계·벽도 보스존 크기에 맞춰 따로 설정한다.
> 크기는 사냥터보다 작게 (1200 × 900 정도) — 도망만 다니지 못하게.

### 8.1 몬스터 (외형: 둥글고 귀엽게 — 도트 또는 단순 도형) `[🔄 설계 변경됨 — 등급당 1종 → 등급×유형 15종]`

**PRD 원안은 등급당 몬스터 1종(6종 합계)이었다.** 실제 구현은 각 등급에
**유형(물몸형·탱커형·재료형)** 변주를 더해 총 15종이다 — "처치 시간은 탱커형 1.4배·
물몸형 0.7배로 조정한다"는 원안의 방향을 몬스터 개체 자체로 구현한 형태다.
이름도 새로 지어졌다. 실제 데이터(`data/monsters.js`)는 아래와 같다.

| 유형 | 성향 |
|---|---|
| 물몸형 | 체력·방어 낮음, 골드 적음, 감지범위(aggroRange) 180 — 빠르게 잡히지만 덜 위협적 |
| 탱커형 | 체력·방어 높음, 골드 많음, 감지범위 320 — 오래 걸리지만 보상이 큼 |
| 재료형 | 방어 중간, 골드 적음, 감지범위 250 — 강화권 재료 몬스터(8.0-5)와는 별개 개념 |

| 등급 | 이름 (유형) | 체력 | 방어력 | 공격력 | 골드 |
|---|---|---|---|---|---|
| 1 | 말랑꼬물이 (물몸) | 40 | 0 | 3 | 4 |
| 1 | 딱딱꼬물이 (탱커) | 90 | 4 | 3 | 8 |
| 2 | 물렁슬라임 (물몸) | 150 | 3 | 7 | 12 |
| 2 | 찐득슬라임 (탱커) | 340 | 10 | 9 | 24 |
| 3 | 여린고블린 (물몸) | 550 | 12 | 13 | 28 |
| 3 | 굳센고블린 (탱커) | 1,400 | 35 | 17 | 65 |
| 3 | 채집고블린 (재료) | 800 | 20 | 11 | 20 |
| 4 | 날쌘오크 (물몸) | 2,000 | 40 | 22 | 70 |
| 4 | 육중오크 (탱커) | 5,200 | 105 | 28 | 160 |
| 4 | 채굴오크 (재료) | 3,000 | 60 | 19 | 55 |
| 5 | 민첩트롤 (물몸) | 8,000 | 100 | 36 | 170 |
| 5 | 거대트롤 (탱커) | 21,000 | 270 | 46 | 400 |
| 5 | 약초트롤 (재료) | 12,000 | 150 | 32 | 130 |
| 6 | 폭군드래곤 (탱커) | 80,000 | 550 | 70 | 900 |
| 6 | 비늘드래곤 (재료) | 50,000 | 400 | 55 | 500 |

- 등급 선택은 8.0-2의 공간 배치 방식으로 대체 (메뉴 없음) — 원안의 "상자(1등급)는
  공격하지 않는다"는 아이디어는 채택되지 않았다. 대신 **1등급 공격력을 무피해
  경계선에 맞추는 불변식**으로 같은 목적(초반 안전 파밍)을 달성한다 — 아래 참고
- 드랍률은 몬스터·등급 무관 균일 25% (`dropChance`, 전 몬스터 공통 임시값, 세분화 전)
- 원안의 "권장 강화 단계"·"드랍 유형(재료/장비/희귀재료)" 열은 지금 데이터에 없다 —
  강화 권장치는 9.1로 통합됐고, 드랍은 등급 확률표(7.2)가 유형과 무관하게 공통 적용된다

**1등급 무피해 불변식** `[🔄 설계 변경됨 — 비율 감소 모델로 번역, 5.2 참고]`

```
1등급 공격력 ≤ playerDefense × 최소 직업 방어배율     (원안, 뺄셈 전제)
```

방어력 하한은 활(직업 배율 0.6배) 기준 `BALANCE.playerDefense(5) × 0.6 = 3`이다.
**5.2가 받는 피해를 뺄셈에서 비율 감소로 바꾸면서 이 부등식은 그대로 성립하지
않는다** — 유한한 방어력·공격력에서 감소율이 정확히 100%(=정확히 0 데미지)가
되는 경우는 없기 때문이다(비율식이 완전 무적을 막는 것과 같은 이유로, 정확한
0도 만들지 못한다). 대신 **반올림 기반 등가**로 번역한다: 위 방어력(3)으로
1등급 공격력(3)에 대한 감소율은 93.3%, 원본 데미지는 0.20으로 `round()`하면
0이 된다(PRD-forge-game-roblox.md 20.11-4에 전 직업 계산 있음) — 1등급 몬스터
공격력은 바꿀 필요가 없었다. "1등급 = 초반 안전 파밍 구간"이라는 원래 취지는
**이 반올림 결과로 성립하는 데이터**라는 뜻이다 — 1등급 몬스터를 추가·조정할 때
이 경계를 넘기면 안전 파밍 구간이라는 전제가 깨진다.

**골드 드랍 원칙 — 초당 골드가 완만하게 오를 것** `[✅ 원칙 유지 — 정확한 수치는 재계산 필요]`

체력이 오르는 만큼 골드를 올리면 상위 몬스터를 잡을 이유가 없어진다.
반대로 골드만 크게 올리면 상위 구역만 가게 된다.

```
목표: 등급이 하나 오를 때 초당 골드 획득량이 약 1.3~1.5배
골드 = 체력 × k  (k는 등급이 오를수록 완만히 증가)
```

이 원칙 자체는 유효하지만, 원안의 예시 표(체력·처치시간·골드)는 6종 단일-몬스터
가정으로 계산된 것이라 15종 물몸/탱커/재료 구조에는 그대로 대입할 수 없다.
실측 처치시간 기준 재계산이 필요하다(18장 개선 루프에서 다룰 항목).

### 8.2 특수 드랍 — 강화 지원 아이템 `[⛔ 미구현]`

일반 몬스터 대신 **낮은 확률로 등장**. 해당 던전의 평균 강화 수준에 맞춰 지급.

| 이름 | 효과 | 등장 확률 |
|---|---|---|
| 강화의 정수 | 현재 단계 +1 확정 성공 | 3% |
| 축복의 모루 | 다음 3회 강화 확률 +15%p | 5% |
| 수호의 부적 | 파괴방지권 2개 | 4% |

> **의도**: 강화 실패가 연속될 때 숨통을 틔워준다.
> 확률 게임에서 "아무것도 안 되는 5분"이 이탈의 주범이다.

### 8.3 조작 `[🔄 부분 구현 — 데스크톱 입력은 완료, 모바일은 미구현]`

| 입력 | 동작 | 상태 |
|---|---|---|
| **WASD** / **방향키** | 이동 (둘 다 지원) | ✅ |
| **마우스 좌클릭** | 공격 | ✅ |
| **좌클릭 더블클릭** | **자동 공격 ↔ 단발 공격 전환** | ✅ |
| **마우스 우클릭** 또는 **스페이스바** | 대시 | ✅ (우클릭·스페이스 둘 다) |
| **Q** | 스킬 1 | ✅ |
| **E** | 스킬 2 | ⛔ (힐러 딜링모드만 예외로 구현, 4.3 참고) |
| **R** | (v2 예정 — 3번째 스킬) | ⛔ |
| **I** | 장비창 열기/닫기 | ✅ |
| **Shift+좌클릭** (인벤토리) | 아이템 잠금 토글 (신규, 7.3-3) | ✅ |
| **ESC** | 설정 창 | 🔄 (저장 요약·삭제·닫기만 있는 최소 패널 — 아래 8.4 참고) |

**공격 모드**

| 모드 | 동작 | 표시 |
|---|---|---|
| 단발 | 클릭할 때마다 1회 공격 | 커서 기본형 |
| **자동** | 사거리 내 적을 자동 공격 | 커서에 회전 표시 + 화면 우상단 `AUTO` |

> 더블클릭으로 전환하는 이유: 별도 토글 버튼을 두면 화면이 복잡해지고
> 전투 중 손이 마우스를 떠나야 한다. 같은 버튼에 겹치면 손이 안 움직인다.

**모바일 대응** `[⛔ 미구현 — 터치 입력 코드 없음, 로블록스에서 자동 지원되므로 17장에서 2차 범위 제외]`

| 입력 | 동작 |
|---|---|
| 좌측 가상 조이스틱 | 이동 |
| 우하단 큰 버튼 | 공격 (길게 누르면 자동 모드) |
| 우하단 보조 버튼 3개 | 대시 · 스킬 Q · 스킬 E |

### 8.4 설정 (ESC) `[⛔ 대부분 미구현 — 저장 요약·삭제·닫기만 있는 최소 패널]`

**싱글 모드에서만 게임이 멈춘다. 멀티에서는 창만 열리고 게임은 계속 진행된다.**

| 항목 | 내용 | 상태 |
|---|---|---|
| **단축키 보기** | 전체 조작 목록 표시. v1은 확인만, 변경은 v2 | ⛔ |
| **소리** | 배경음 / 효과음 각각 0~100 슬라이더 + 음소거 | ⛔ (사운드 자체가 아직 없음 — 16.3 참고) |
| **화면 비율** | 창 모드 / 전체 화면 (웹). 앱은 기기 해상도 자동 | ⛔ |
| **데미지 숫자** | 표시 / 숨김 (숫자가 시야를 가릴 때) | ⛔ |
| **게임 종료** | 슬롯 저장 후 메인으로 | 🔄 (저장은 자동저장으로 대체, "삭제 후 처음부터" 버튼만 있음) |

> 소리 설정이 필요한 이유: 강화 성공음과 치명타 효과음이 이 게임 쾌감의 큰 부분이다.
> 반대로 공공장소에서 소리 없이 하고 싶은 사람도 있으니 즉시 끌 수 있어야 한다.

> 소리 설정이 필요한 이유: 강화 성공음과 치명타 효과음이 이 게임 쾌감의 큰 부분이다.
> 반대로 공공장소에서 소리 없이 하고 싶은 사람도 있으니 즉시 끌 수 있어야 한다.

---

## 9. 보스전 `[🔄 부분 구현 — 1·2단계만 존재]`

### 9.1 6단계 구성 `[🔄 부분 구현 — 1·2단계만 구현, 3~6단계는 설계 목표치]`

| 단계 | 보스 | 체력 | 방어력 | 공격력 | 제한시간 | 권장 강화 | 상태 |
|---|---|---|---|---|---|---|---|
| 1 | 고블린왕 | ~~20,000~~ **12,000** | ~~30~~ **8** | 20 | 60초 | +6 | ✅ 구현(수치는 실측 재조정) |
| 2 | 오크로드 | 80,000 | 80 | 30 | 75초 | +10 | ✅ 구현(설계 목표치 그대로 채택) |
| 3 | 리치 | 300,000 | 180 | 45 | 90초 | +14 | ⛔ 미구현 |
| 4 | 드래곤로드 | 1,000,000 | 350 | 60 | 105초 | +17 | ⛔ 미구현 |
| 5 | 심연의군주 | 3,500,000 | 600 | 80 | 120초 | +20 | ⛔ 미구현 |
| 6 | 태초의용 | 12,000,000 | 1,000 | 100 | 150초 | +23 | ⛔ 미구현 |

> **[🔄 설계 변경됨] 1단계 체력·방어력이 실측으로 다시 잡혔다.** 원래 20,000/30은
> 순수 설계 목표치였고 실제로는 4,000/8으로 구현돼 있었다 — 전 직업이 2.5~5초 만에
> 잡히는 "너무 빨리 죽는다" 문제가 있었다. 무기+13·전설 3세트(Lv15) 기준 DPS
> 시뮬레이션으로 체력을 12,000까지 올렸다(클리어 7.42초, S등급 기준 18초의 41%만
> 사용 — 여유가 크다). **이 12,000도 임시값이다** — 랜덤 옵션(7.3-1) 도입으로 DPS가
> 다시 올랐으므로(전설 3세트 기준 기대 DPS 증가 +5~15%), 다음 밸런스 라운드에서
> 재계산할 여지가 있다. 2단계(오크로드)는 원래 설계치 그대로 구현되어 손대지 않았다.
>
> 방어력도 8로, 원안 30보다 훨씬 낮다 — 이 역시 실측 재조정 결과다.

- 보스도 **받는 피해 상한 30%** 규칙 적용 → 즉사 없음
- 클리어한 최고 단계는 저장되어 다음 판에서 이어서 도전

**광폭화 — 체력 10% 구간** `[🔄 설계 변경됨 — 구현은 됐으나 "스킬"이 아니라 "공격력 전체"가 2배]`

| 조건 | 효과 |
|---|---|
| 보스 체력 **10% 이하** (`BALANCE.bossEnrageHpRatio`) | 공격력 2배 (`BALANCE.bossEnrageAttackMultiplier`) |
| 시각 연출 | 화면 가장자리 붉은 비네트 (`drawEnrageVignette`) |
| 음향 | ⛔ 미구현 (사운드 자체가 없음) |

> **[🔄 설계 변경됨]** v1 보스에는 "평타"와 별도의 "스킬"이 없다 — 접촉 데미지 하나가
> 전부다(9.x의 패턴 시스템은 3~6단계와 함께 아직 안 만들어졌다). 그래서 원안의
> "평타는 그대로 두고 스킬만 2배"는 지금 구현과 맞지 않는다 — 실제로는 그 유일한
> 공격력(`boss.attack`) 자체가 광폭화 시 2배가 된다. 회피로 피할 수 있는 공격에만
> 위험을 몰아야 한다는 원래 의도는, 보스 패턴이 생기는 3~6단계 작업에서 다시
> 살려야 한다.

> **의도**: 마지막 10%가 가장 위험해야 클리어의 쾌감이 산다.
> 딜이 부족해 질질 끄는 플레이는 여기서 죽고, 강화가 충분하면 광폭화 전에 끝난다.
> **"조금만 더 강화할걸"과 "아슬아슬하게 잡았다"를 동시에 만드는 구간.**

### 9.1-1 세션 구조와 저장 슬롯 `[⛔ 웹 미구현 — 저장 슬롯 3개는 웹에서 만들지 않는다]`

> 이 절이 그리는 "슬롯 선택 → 직업별 병행 → 슬롯 카드 UI" 구조는 웹 버전에서 구현하지
> 않기로 했다. 지금 저장은 `core/save.js`에 **단일 슬롯**(`localStorage` 키 하나)뿐이다.
> 로블록스 전환 시 DataStore로 다중 슬롯을 만들 계획이라(17장, PRD-forge-game-roblox.md 20장 참조), 이 절의
> 설계 의도(직업별 병행 플레이, "아무렇게나 리셋 못 하는 3개") 자체는 유효하게 남겨두고
> 로블록스 버전에서 구현한다. 아래 원문은 그대로 둔다.

**한 판 = 보스 1단계 = 약 10분.** 6단계 전부 = 약 1시간이지만,
**한 번에 앉아 있으라고 요구하지 않는다.**

#### 진행 방식 (싱글)

```
[슬롯 선택] → 직업 선택 → 1단계 (10분 파밍 + 보스)
    ↓ 클리어
[자동 저장] 무기강화 · 무기레벨 · 장비 · 가방 · 아이템 그대로 보존
    ↓
2단계 선택 (지금 이어서 / 나중에 이어서)
    ↓ 10분 파밍 — 강화는 1단계에서 쌓은 상태에서 계속 올린다
    ↓ 클리어 → 자동 저장 → 3단계 ...
```

- **상태는 전부 이어진다.** 1단계를 +12로 깼으면 2단계는 +12에서 시작
- 각 단계마다 타이머 10분이 새로 주어지므로, 6단계까지 총 60분의 누적 성장
- → 보스 권장 강화(+6 → +10 → +14 → +17 → +20 → +23)는 이 누적을 전제로 한 값
- 클리어 직후 **"이어서 도전" / "나가기"** 둘 중 선택. 나가도 상태는 저장됨

#### 저장 슬롯 (최대 3개)

| 항목 | 규칙 |
|---|---|
| 슬롯 수 | **3개** |
| 저장 시점 | 보스 단계 클리어 시 **자동 저장** (수동 저장 없음) |
| 슬롯이 꽉 찼을 때 | 새 게임 시작 시 **삭제할 슬롯을 선택**해야 진행 가능 |
| 삭제 확인 | 확인 모달 1회 — `이 기록은 복구할 수 없습니다` |

**슬롯 카드에 표시할 정보**

```
┌─────────────────────────────┐
│ 슬롯 1                       │
│ 활 딜러 · 3단계 리치 클리어    │
│ 무기 +16 · 레벨 21           │
│ 최근 플레이: 2일 전           │
│         [이어하기] [삭제]     │
└─────────────────────────────┘
```

- 직업 / 도달 단계 / 무기 강화 / 무기 레벨 / 마지막 플레이 시각
- 빈 슬롯은 `+ 새 게임`으로 표시

> **의도**: 슬롯이 3개인 이유는 **직업별 병행 플레이**를 위해서다.
> 대검으로 5단계까지 갔는데 활은 어떨까 싶을 때, 기존 기록을 지우지 않고 새로 시작할 수 있다.
> 무제한이면 실패한 판을 버리고 새로 파는 게 습관이 되어 강화의 무게가 사라진다.
> **3개는 "여러 직업을 시도할 수는 있지만, 아무렇게나 리셋하지는 못하는" 숫자.**

#### 모드별 저장·일시정지

| 모드 | 저장 | 일시정지 | 근거 |
|---|---|---|---|
| **싱글** | 단계 클리어 시 자동 저장 (슬롯 3개) | 가능 | 3단계 깨고 껐다가 나중에 4단계부터 재개 |
| **멀티 (v2·v3)** | 단계 종료 시 기록 전송만 | **불가** | 한 세션이 10분이라 감당 가능. 중간 이탈은 포기 처리 |

**저장 항목**: 슬롯 번호 · 직업 · 도달 보스 단계 · 무기 강화 · 무기 레벨 ·
착용 장비(강화 수치 포함) · 가방 내용 · 보유 아이템 · 마지막 플레이 시각

**웹 vs 앱**
- 웹: localStorage에 슬롯 3개를 각각 저장. 탭을 닫아도 유지
- 앱: 동일하되 **백그라운드 전환 시 즉시 저장** (전화·알림으로 앱이 내려가도 안전)
- 멀티는 앱·웹 모두 일시정지 없음. 나가면 그 판 포기

> **의도**: 1시간 연속 구속은 모바일에서 이탈의 가장 큰 원인이다.
> 단계별 완결감을 주면 "오늘은 3단계까지"가 성립하고, 그게 재접속 이유가 된다.
> 반대로 멀티는 짧고 끊기지 않아야 긴장이 유지된다 — 10분이라 감당 가능한 길이.

### 9.2 타이머 `[🔄 설계 변경됨 — "도전 가능"이 아니라 타이머 만료 시 자동 진입]`

- ~~게임 시작 후 10분 경과 시 보스 도전 가능~~ → 파밍 타이머(1,800초, 8.0-1-a)가
  0이 되면 **카운트다운 후 자동으로 보스존에 진입한다.** 플레이어가 스스로 "지금
  도전한다"를 누르는 버튼은 없다 — 9.3(조기 도전)이 구현되면 그게 그 버튼 역할을
  하게 될 것이다
- 남은 시간 상단 상시 표시
- 사망해도 타이머는 계속 흐름

### 9.3 조기 도전 (Early Challenge) `[⛔ 미구현]`

강화 대박을 즉시 보상으로 연결하는 장치. **v1의 차별점.**

> 배너·확인 모달·보너스 티어 전부 코드에 없다. 지금은 9.2처럼 타이머 만료 시
> 자동 진입만 있다 — 이 절 전체가 다음 구현 대상이다(17장 "11 재도전·조기도전" 참고,
> 재도전은 이미 구현되어 있다).

1. 강화 성공으로 권장 기준선 초과 시 배너 등장
   → `현재 +19 · 권장 +17 초과. 지금 도전 시 보상 +50%`
2. 확인 모달 1차 — 현재 딜량, 예상 클리어 시간, 성공 가능성 표시
3. 확인 모달 2차 — `실패해도 재도전은 2회까지입니다. 도전할까요?`
4. 즉시 보스전 진입

| 남은 타이머 | 추가 보상 |
|---|---|
| 7분 이상 | +100% |
| 4~7분 | +50% |
| 1~4분 | +25% |
| 1분 미만 | +0% |

### 9.4 실패와 재도전 `[✅ 구현 완료 — 광고 게이팅만 제외]`

| 항목 | 규칙 | 상태 |
|---|---|---|
| 재도전 횟수 | **한 판당 최대 2회** | ✅ (`BALANCE.bossMaxRetries = 2`) |
| 재도전 대기 | **3분 추가 파밍** | ✅ (`BALANCE.bossRetryFarmDuration = 180`) |
| 재도전 비용 | **긴 광고 1회 시청** | ⛔ 미구현 — 지금은 광고 없이 자동 재시작 |
| 2회 소진 후 | 판 종료 → 기록 등록 후 새 판 | ✅ |

> 3분은 "한 단계 더 강화해볼 수 있는" 최소 시간.
> 무한 재도전을 막아야 보스 도전이 사건으로 남는다.

### 9.5 보상 — 클리어 시간 기준 `[✅ 구현 완료 — 수치 일치]`

**딜량이 아니라 클리어 속도로 등급을 매긴다.**

| 클리어 시간 | 등급 | 배율 |
|---|---|---|
| 제한시간의 30% 이내 | S | ×2.0 |
| 50% 이내 | A | ×1.5 |
| 75% 이내 | B | ×1.2 |
| 100% 이내 | C | ×1.0 |

```
최종보상 = 기본보상 × 등급배율 × (1 + 조기도전보너스)
```

> `BALANCE.bossGradeThresholds`/`bossGradeMultipliers`가 위 표와 정확히 일치한다.
> 다만 `조기도전보너스`는 9.3이 미구현이라 지금은 항상 0이다.

### 9.6 보스 클리어 보상 — 확정 강화권 `[✅ 구현 완료 — 수치 일치]`

**확정 강화권의 유일한 출처는 보스다.** 사냥터에서는 나오지 않는다.

| 항목 | 값 |
|---|---|
| 지급 확률 | **50%** |
| 지급 내용 | **확정 강화권 +1** ×1 |
| 나머지 50% | 골드만 (등급 배율 보상은 그대로) |

**확정 강화권 동작**

```
확정 강화권 → 현재 단계에서 +1 확정 상승. 실패 판정 없음
예) +17 상태에서 사용 → +18 (강등·리셋 위험 없음)
```

- 등급(S~C)과 무관하게 **클리어하면 50% 판정**
- 등급 배율은 골드에만 적용
- 가방에 보관. 쓸 타이밍을 유저가 고른다

> **왜 보스 전용인가**
> 사냥터에서 굴러다니면 가치가 없다. 보스 보상으로 한정하면
> **보스에 도전할 이유**가 하나 더 생긴다. 50%라 매번 나오지 않으므로 반가움도 남는다.
>
> **왜 확률권으로 대체할 수 없는가**
> PRD 6.2에서 계산했듯 +14 이상은 맨손 기댓값이 음수다.
> 확률권(중)으로 ×2를 해도 절반은 실패한다 — 운 나쁜 유저에겐 도움이 안 된다.
> **확정권만이 "반드시 앞으로 간다"를 보장한다.** 이것이 이 게임의 저점 보장 장치다.
>
> **왜 쓸 타이밍을 유저가 정하는가**
> `+18`에서 아껴뒀다가 쓸지, `+14`에서 바로 쓸지 고민하는 것 자체가 판단 재미다.
> 획득 즉시 자동 적용하면 그 고민이 사라진다.

---

## 10. 상점 — 잉여 골드 소비처 `[⛔ 미구현]`

후반으로 갈수록 골드가 남는다. **쓸 곳이 없으면 사냥의 의미가 사라진다.**

### 10.1 판매 목록

| 상품 | 비용 | 제한 |
|---|---|---|
| **파괴방지권** | **광고 1회** (골드로 구매 불가) | 하루 5회 |
| **강화 확률 상승권** (+10%p, 1회용) | 골드 (고가) | 무제한 |
| **강화 확률 대폭 상승권** (+20%p, 1회용) | 골드 (매우 고가) | 하루 3회 |
| **자동 판매 기능** | **긴 광고 1회** | 1회 활성화 시 그 판 내내 유지 |
| 회복 물약 | 골드 (저가) | 무제한 |

**가격 설계 원칙**

```
확률 상승권 가격 = 현재 보스 단계 기준 10분 파밍 골드의 약 40%
→ 단계가 오를수록 가격도 오른다. 후반에도 "비싸다"는 감각 유지
```

### 10.2 자동 판매 기능

**긴 광고 1회로 활성화.** 그 판이 끝날 때까지 유지된다.

```
자동 판매 대상 = 현재 착용 장비보다 2단계 이상 낮은 등급의 장비
→ 드랍 즉시 골드로 전환, 가방에 들어오지 않음
```

- 착용 장비가 좋아질수록 자동 판매 범위도 자동으로 올라간다
- 가방(20칸)이 잡템으로 차는 스트레스를 없애는 것이 목적
- **정확히 1단계 아래는 남긴다** — 교체 후보가 될 수 있으므로

> **의도**: 후반에 가장 귀찮은 작업이 가방 정리다.
> 이걸 광고로 풀면 유저가 자발적으로 광고를 본다. 강제 노출보다 훨씬 낫다.

### 10.3 광고와 골드의 역할 분담

| 항목 | 수단 | 이유 |
|---|---|---|
| 파괴방지권 | **광고만** | 골드로 살 수 있으면 후반에 무한 획득 → 강화의 긴장이 사라진다 |
| 확률 상승권 | **골드만** | 잉여 골드의 주된 소비처. 광고로 주면 골드가 또 남는다 |
| 자동 판매 | **광고만** | 편의 기능이라 광고 시청 동기로 적합 |

> **핵심 원칙**: 강화 실패의 위험을 줄이는 것(파괴방지권)은 골드로 살 수 없다.
> 이 선이 무너지면 골드 많은 후반부에 강화가 무위험 작업이 되고, 게임이 끝난다.

---

## 11. 퀘스트 — 확률 게임의 저점 보장 `[⛔ 미구현 — 11.4 "밸런스 1차 조정 이후" 조건 아직 미도래]`

**확정 보상으로 "아무것도 안 되는 5분"을 막는다.**

확률 게임의 이탈 원인 1위는 연속 실패다. 지금 이를 막는 장치는 특수 드랍(3~5%)뿐인데
그것도 확률이라 운이 나쁘면 또 안 나온다.
**퀘스트는 확정이므로 "실패가 진행이 되는" 구조를 만든다.**

### 11.1 설계 원칙 두 가지

**① 퀘스트가 플레이를 지시하면 안 된다.**

"3등급 몬스터 20마리 잡기"가 뜨면 유저는 하고 싶던 걸 멈추고 3등급으로 간다.
그러면 8.0-2의 "어떤 몬스터를 잡을까"라는 판단이 사라진다.

→ **어차피 하게 될 것**을 세는 방식으로 만든다.

**② 목록이 길면 안 된다.**

한 판이 10분인데 퀘스트가 10개 뜨면 게임이 체크리스트가 된다.

→ **동시에 3개만.** 하나 완료되면 새로 하나를 채운다.

### 11.2 퀘스트 유형

| 유형 | 예시 | 보상 | 역할 |
|---|---|---|---|
| **누적형** | 몬스터 50마리 처치 | 골드 | 자연히 달성됨 |
| **행동형** | 강화 15회 시도 (성공·실패 무관) | **파괴방지권 1개** | 실패도 진행이 된다 |
| **도전형** | 5등급 이상 10마리 처치 | 확률 강화권(중) 1개 | 약간의 목표 |
| **소비형** | 확정 강화권 사용 | 골드 + 기본 장비 | 아이템 순환 |

**행동형 보상이 파괴방지권인 것이 핵심이다.**
파괴방지권은 광고로만 얻게 설계했는데, 퀘스트로도 소량 주면
**"광고를 안 봐도 되지만 보면 빨라지는"** 구조가 된다. 광고를 강제하지 않으면서 존재 이유는 남는다.

### 11.3 UI

- 화면 **우상단**에 배치 (던전 선택 버튼은 상단 중앙 — 8.0-1 참조)
- 접힌 상태가 기본, 클릭하면 펼쳐짐
- 각 퀘스트에 진행도 바 (`37 / 50`)
- 완료 시 우상단에서 반짝임 + 보상 획득 안내 2초

### 11.4 구현 시점

**밸런스 1차 조정 이후.** 그때가 되면 "어디서 막히는가"를 알게 되고,
퀘스트 보상을 그 지점에 맞출 수 있다. 지금 만들면 보상 수치를 감으로 잡아야 한다.

---

## 12. 비동기 멀티 — 기록표 `[⛔ 미구현 — 서버·기록표 연동 없음]`

**실시간 협동 없음.** 각자 플레이하고 결과만 공유한다.

### 12.1 표시 항목

| 항목 | 내용 |
|---|---|
| 보스별 최속 클리어 | 단계별 상위 50위 (시간·무기·강화단계) |
| 최고 강화 기록 | 전체 상위 50위 (닉네임·단계·달성 시각) |
| 오늘의 통계 | 평균 도달 단계, 평균 클리어 보스, 조기도전 성공률 |

> 남의 기록을 보는 것 자체가 콘텐츠. "+24 찍은 사람이 있네"가 다음 판의 동기가 된다.

### 12.2 치팅 방지 (핵심 설계)

전투를 서버에서 돌리지 않고도 막는 방법:

| 검증 | 방법 |
|---|---|
| **이론상 최소 시간** | 강화단계·무기·레벨로 계산한 최대 DPS로 물리적 최소 클리어 시간을 산출. 그보다 빠르면 거부 |
| **행동 로그 동봉** | 클리어 시 강화 이력·처치 수·소요 시간을 함께 전송. 앞뒤가 안 맞으면 거부 |
| **강화 이력 검증** | +25인데 강화 시도 횟수가 3회면 불가능 → 거부 |
| **상위 기록 수동 검토** | 1~3위는 등록 보류 후 눈으로 확인 (월 1~2회 유지보수에 포함) |

> 완벽히 막을 수는 없다. **목표는 "쉽게는 못 하게"**다.
> 순위 보상이 현금이 아니라 명예뿐이므로, 이 정도면 충분하다.

### 12.3 서버 구성

| 항목 | 선택 |
|---|---|
| 저장소 | Supabase 무료 티어 |
| 인증 | 없음 (닉네임 + 브라우저 고유 ID) |
| 통신 | 클리어 시 1회 POST, 기록표 조회 시 GET |
| 비용 | 월 0원 (무료 티어 한도 내) |

**실시간 연결이 없으므로 서버 부하가 거의 없다.** 이것이 비동기를 택한 이유.

---

## 13. 수익 모델 — 플랫폼별 분리 `[⛔ 미구현 — 광고 자리·과금 로직 없음. 웹 수익 모델은 PRD-forge-game-roblox.md 20장에서 로블록스 모델로 대체 검토]`

**같은 게임의 두 판본. 웹은 광고, 스팀은 유료.**

| | 웹 | 스팀 |
|---|---|---|
| 가격 | 무료 | 유료 (약 $4.99~9.99) |
| 광고 | 있음 (보상형만) | **없음** |
| 목적 | **재미 검증 + 유입** | 수익 |
| 착수 시점 | v1 | **웹에서 반응이 확인된 뒤** |

### 13.1 웹 — 보상형 광고

**강제 전면광고 없음.** 유저가 자발적으로 보는 구조만 만든다.

| 위치 | 형태 | 보상 |
|---|---|---|
| 강화 실패 후 | 짧은 광고 | 파괴방지권 1개 (하루 5회) |
| 장비 교체 | 짧은 광고 | 장비교체권 1개 |
| **자동 판매 활성화** | **긴 광고** | 그 판 내내 유지 |
| 보스 재도전 | **긴 광고** | 재도전 1회 |
| 보스 클리어 후 | 짧은 광고 | 보상 2배 |
| 던전 입장 | 짧은 광고 | 10분간 획득량 1.5배 (하루 3회) |

- 확률형 아이템 **판매 없음** → 게임산업법 확률 공시·등급분류 이슈 회피
- 광고 네트워크는 유입이 확인된 뒤 연동. v1에서는 자리와 호출 지점만 구현

### 13.2 스팀 — 착수 전 확인할 숫자

**비용은 낮다.**

| 항목 | 값 |
|---|---|
| Steam Direct 등록비 | **앱당 $100** (약 14만 원) |
| 환급 조건 | 조정총수익 $1,000 도달 시 다음 지급에 반환 |
| 수수료 | **30%** ($1,000만 미만 구간은 전부 30%) |
| 대기 기간 | 신원 확인 후 **30일** + 검수 1~5일 |
| 스토어 페이지 | 출시 **2주 전**부터 공개 필요 |

**그런데 이 숫자를 먼저 보라.**

| 지표 | 값 |
|---|---|
| 2025년 스팀 출시작 | 1만 9천 개 이상 |
| 인디 게임 **중간값 수익** | **약 $249** (약 34만 원) |
| $100조차 회수 못 하는 비율 | 약 40% |

> 웹스토어 배당 확장 조사에서 확인한 것과 **같은 구조**다.
> 문제는 만들기가 아니라 눈에 띄기다. 등록비 14만 원은 감당 가능하지만,
> **웹에서 반응이 없었던 게임을 스팀에 올리면 결과도 같다.**

**스팀 착수 조건**

1. 웹에서 실제 반복 플레이 유저가 확인됨
2. 커뮤니티에서 "재밌다" 또는 구체적 개선 요구가 나옴
3. 그래픽·사운드가 도형 단계를 벗어남

> 스팀 유저는 웹 유저보다 기대치가 훨씬 높다.
> 웹에서 "무료니까 이 정도면" 소리를 듣는 완성도는 스팀에서 부정적 리뷰가 된다.

**스팀 진입 순서**

```
데모 먼저 공개 → 플레이테스터 피드백 수집 → 반응 확인 → 본편 유료 출시
```

데모는 스팀 안에서 **발견되는 경로가 하나 더 생긴다**는 이점도 있다.

### 13.3 광고와 골드의 경계 (재확인)

| 항목 | 수단 | 이유 |
|---|---|---|
| 파괴방지권 | **광고만** | 골드로 살 수 있으면 후반에 무한 획득 → 강화의 긴장 소멸 |
| 확률 상승권 | **골드만** | 잉여 골드의 주 소비처 |
| 자동 판매 | **광고만** | 편의 기능이라 광고 동기로 적합 |

**스팀 판본에서는** 광고가 없으므로, 광고로 얻던 항목은 아래로 대체한다.

| 웹 (광고) | 스팀 (대체) |
|---|---|
| 파괴방지권 | 보스 클리어 시 확정 지급 (단계당 1~2개) |
| 자동 판매 | 처음부터 활성화 |
| 보스 재도전 | 그대로 2회 (광고 없이) |

> 유료 게임에서 편의 기능을 잠그면 반발이 크다. **스팀 판본은 전부 열어둔다.**

---

## 14. 기술 스펙 `[🔄 부분 구현]`

| 항목 | 선택 | 이유 | 상태 |
|---|---|---|---|
| 형태 | 웹 · **탑다운 2D** | 링크만으로 플레이. 앱스토어 심사·등록비 불필요 | ✅ |
| 렌더링 | Canvas 2D (엔진 없음) | 회전 스프라이트 방식이라 도형·이미지 모두 간단 | ✅ |
| 스택 | HTML + CSS + JavaScript | 빌드 도구 없이 시작 | ✅ |
| 로컬 저장 | localStorage | 진행 상황·설정 | ✅ (아래 참고) |
| 서버 | Supabase 무료 티어 | 기록표 전용 | ⛔ (12장, 서버 자체가 없음) |
| 배포 | ~~Vercel 또는~~ **GitHub Pages** | 무료 | ✅ (`CLAUDE.md`: 커밋 후 origin master 푸시 시 자동 반영) |

**모바일 우선**: 세로 화면 기준, 터치 영역 최소 44px. `[⛔ 미구현 — 8.3 참고]`

### 14.1 저장 `[✅ 구현 완료]`

**단일 슬롯**(9.1-1과 달리 3슬롯이 아니다) `localStorage`, `SAVE_VERSION` + `migrate()`.
현재 버전 **5**. 강화 성공·보스 결과 등 핵심 행위 직후는 즉시 저장, 나머지는 **10초 주기 +
`visibilitychange`(hidden) + `pagehide`**로 저장한다(9.1-1의 "앱은 백그라운드 전환 시
즉시 저장"과 같은 취지를 웹에서 구현한 형태).

**두 탭 동시 저장 감지 (v5, 설계 승인)** `[✅ 구현 완료]` 같은 브라우저 두 탭에서 같은
저장을 동시에 플레이하면, 저장이 전부 무조건 덮어쓰기라 나중에 저장한 탭이 이긴다 —
두 탭 진행 중 더 적은 쪽이 이겨도 이상하지 않다(실제 재현 확인). 저장 데이터에
`savedAt`(타임스탬프)을 추가하고, 각 탭이 "내가 마지막으로 읽거나 쓴 savedAt"을
메모리에 들고 있다가 저장 직전 `localStorage`를 다시 읽어 비교한다 — 거기 적힌
`savedAt`이 내가 아는 값보다 최신이면 다른 탭이 그 사이 저장했다는 뜻이므로 **덮어쓰지
않고 그 탭의 자동저장을 영구 중단**한 뒤 "다른 탭에서 플레이 중입니다. 이 탭의 진행은
저장되지 않습니다. 새로고침해 주세요" 배너를 띄운다. **판단 기준은 오직 "더 최근 저장"
하나다** — 골드처럼 쓰면 줄어드는 값이 섞여 있어 "어느 진행도가 더 큰가"로는 판단이
성립하지 않는다. `doAutosave()` 하나가 주기·가시성변경·종료·핵심행위 즉시저장을 전부
거치므로, 이 함수 하나만 고치면 모든 저장 경로가 같은 검사를 받는다.

**마이그레이션 이력**

| 버전 | 계기 | 처리 |
|---|---|---|
| v1 → v2 | 장비 강화(구 7.5) 도입 | 기존 장비에 `enhanceLevel: 0` 채움 |
| v2 → v3 | 장비 강화 폐지 → 아이템 레벨 시스템(7.5) | `enhanceLevel` 삭제, `weaponExp`로 역산한 현재 캐릭터 레벨을 `itemLevel`로 채움 (레벨 1로 채우면 기존 장비가 전부 최약체가 되어 "저장 무손실 승계" 취지에 반함) |
| v3 → v4 | 랜덤 옵션·잠금(7.3-1, 7.3-3) 도입 | `options: []`, `locked: false`로 채움 (소급 재굴림 없음 — migrate()에 새 난수를 주입하지 않는다는 원칙) |
| v4 → v5 | 두 탭 동시 저장 감지 도입 | `savedAt`이 없으면 가장 오래된 값(0)으로 채움(소급해서 지금 시각을 넣으면 실제로 더 최신인 다른 탭의 저장을 오래된 것으로 오판할 위험이 있다) |

### 14.2 프레임 상한 `[✅ 구현 완료]`

`dt` 상한 **0.1초**(`BALANCE.maxFrameDt`). 백그라운드 탭에서 복귀하면
`dt = (now - lastTime)/1000`이 수십 초로 튈 수 있어, 그 순간 순간이동·타이머 급감이
일어나는 걸 막는다. **배속은 이 클램프를 적용한 뒤에 곱한다** — 순서를 반대로 하면
3배속의 실제 `dt`(최대 0.3)가 0.1로 잘려 2배속과 구분이 안 된다.

### 14.3 숫자 축약 표기 `[✅ 구현 완료]`

10,000부터 1,000단위로 알파벳(1A/1B/1C…), 소수 1자리 내림, Z 이후는 스프레드시트
열 이름처럼 AA·AB로 확장(`core/format.js`, `data/balance.js`의 `NUMBER_ABBREVIATION`).
내림을 쓰는 이유는 반올림을 쓰면 `1.99A`가 `2A`로 표시돼 아직 도달 안 한 다음 단계에
이미 도달한 것처럼 오해하기 때문이다.

---

## 15. 데이터 구조 설계 — v2·v3를 빠르게 만들기 위한 뼈대 `[✅ 원칙 구현 완료 — 실제 파일 구성은 아래처럼 늘어남]`

> **이 섹션이 이 문서에서 가장 중요하다.**
> 여기를 잘못 잡으면 v2를 만들 때 v1을 다시 쓰게 된다.
> 원칙: **숫자와 정의는 데이터로, 코드는 그 데이터를 읽기만 한다.**

### 15.1 파일 분리 `[🔄 설계 변경됨 — 원안보다 파일이 세분화됨]`

```
/game
  data/
    classes.js     직업 정의 (스탯, 치명타 성향, 스킬 ID)
    skills.js      스킬 정의 (효과 조각의 조합)
    monsters.js    몬스터 · 보스 정의 (stages.js는 배경/타이머만 분리됨, 아래 참고)
    stages.js      던전 배경 · 몬스터 등급 범위 (제한시간·권장강화는 monsters.js의 BOSSES가 가짐)
    enhance.js     강화 확률표 · 계수표
    items.js       장비 등급 · 부위 기본 스탯 · 랜덤 옵션 개수 · 소모품
    stats.js       [신규] 스탯 레지스트리 (아래 15.2-1)
    balance.js     자주 만지는 수치 모음 + 숫자 축약 표기 설정
  core/
    combat.js      데미지 계산
    enhance.js     강화 판정 로직
    equipment.js   [신규] 장비 스탯 계산 · 판매가 · getEquipmentBonuses
    loot.js        [신규] 드랍 판정 · 랜덤 옵션 부여
    weaponExp.js   [신규] 무기 경험치 → 레벨 변환
    skills.js      [신규] 스킬 효과 조각 해석 (heal/buff 계산)
    aggro.js       [신규] 어그로 대상 판정
    save.js        저장 (슬롯이 아니라 단일 슬롯 + 버전 마이그레이션, 14장)
    format.js      [신규] 숫자 축약 표기
    render.js      화면 그리기
  main.js
```

**규칙: 밸런스 수치를 `core/` 안에 절대 직접 쓰지 않는다.**
치명타 계수 2.6을 바꾸려면 `data/classes.js` 한 줄만 고치면 되어야 한다.
(이 원칙은 실제로 계속 지켜지고 있다 — `core/`에서 매직넘버로 직접 박힌 밸런스 수치는 없다.)

### 15.2 직업 정의 — 새 직업은 객체 하나 추가로 끝 `[✅ 구현 완료 — 배열이 아니라 id 키 객체]`

```js
// data/classes.js (실제로는 배열이 아니라 { id: {...} } 형태 객체 + CLASS_ORDER 배열)
const CLASSES = {
  greatsword: {
    id: "greatsword", name: "대검",
    atk: 1.6, atkSpeed: 0.6, def: 1.3,
    critRate: 0.10, critDmg: 1.8,
    attackType: "melee", meleeRange: 100, meleeArc: 70,
    healPower: 0, aggro: 0, control: 0   // v3용 — 지금도 0으로 비워둠
  },
};
```

> **핵심**: `healPower`·`aggro`·`control`을 **v1부터 0으로 넣어둔다.**
> v3에서 탱커를 추가할 때 스탯 구조를 다시 설계하지 않아도 된다.
> **`healPower`는 이제 실제로 작동한다** — 15.2-1의 스탯 레지스트리가 장비로 이 값을
> 올릴 수 있게 했고, 치유 스킬(4.3)·딜링모드(4.1-1)·라이프스틸(15.2-1)이 이 값을
> 소비한다(치유·딜링모드는 무상한, 딜링모드 환산에만 `dealModeHealPowerCap=0.8`
> 적용). `aggro`·`control`은 여전히 소비하는 코드가 없다(v3 파티 시스템 전용).

### 15.2-1 스탯 레지스트리 — 새 스탯은 데이터 한 줄로 `[✅ 구현 완료 — 신규]`

15.2의 원칙(새 직업 = 객체 하나)을 스탯 자체에도 적용한 것이다. 원래 `getEquipmentBonuses`는
부위별로 `if (part === "armor") ... else if (part === "gloves") ...` 식 분기였다 —
치명타율·회복량·쿨다운 감소 같은 스탯을 추가할 때마다 이 분기를 계속 늘려야 했다.

```js
// data/stats.js
const STAT_REGISTRY = {
  defenseFlat:   { label: "방어력", appliesAs: "additive", unit: "flat", optionBase: 0.2 },
  critRate:      { label: "치명타율", appliesAs: "additive", unit: "percent", optionBase: 0.00543 },
  healPower:     { label: "회복량", appliesAs: "additive", unit: "percent", optionBase: 0.01087 },
  lifesteal:     { label: "라이프스틸", appliesAs: "additive", unit: "percent", optionBase: 0.00543, cap: 0.20 },
  // ... 11종 (신규 추가된 라이프스틸을 빼면 기존과 동일)
};
```

`getEquipmentBonuses`는 이제 부위별 분기 대신 `STAT_ID_ORDER`를 순회하며 부위 기본효과 +
장비 옵션을 스탯 id별로 합산한다.

**한계 — 솔직하게 적어둔다.** 이 구조가 커버하는 건 **"이미 있는 공식에 숫자를 하나 더
얹는 것"**까지다. 예를 들어 `cooldownReduction`을 추가할 때는 정말 `data/stats.js`
한 줄 + 그 값을 읽는 기존 소비 지점(`qCooldownTimer = skill.cooldown × (1 - 값)`) 한 줄로
끝난다 — 이미 "쿨다운"이라는 개념과 그걸 줄이는 자리가 코드에 있었기 때문이다. 반대로
**완전히 새로운 규칙**(7.3-2의 규칙형 특수 옵션들)은 그 규칙을 처음 소비할 코드 자체가
없으므로 레지스트리에 항목을 추가하는 것만으로는 끝나지 않는다 — 별도 훅이 필요하다
(7.3-2가 각 후보별로 이걸 평가해뒀다). **라이프스틸이 정확히 이 경우였다** — 이번에
설계를 확정했다(아래).

#### 라이프스틸 — healPower가 범용 회복 계수로 확장되며 함께 설계됨 `[⛔ 미구현, 설계 승인]`

`healPower`가 힐러의 치유 스킬(4.3)에만 쓰이는 건 4직업 중 1개만 혜택을 받는다는
뜻이라, 전설 이상 장비 옵션 롤의 약 1/9(위 7.3-1 옵션 풀 기준)이 나머지 3직업에게는
낭비였다. **`healPower`를 "회복 효과 전반의 배율"로 재정의**해 라이프스틸에도
곱해지게 한다 — 그러면 회복량 옵션이 전 직업에 유효해진다.

```
피격 판정 없이, 자신이 가한 데미지가 확정된 시점에(calcDamage 반환 직후) 발동:
라이프스틸 발동확률 = min(lifestealCap, 라이프스틸계수 × (1 + healPower))
발동 시 체력 1칸 회복 (힐러의 기존 "체력 환급" 확률형 회복 패턴과 동일한 구조 재사용 —
  게임 체력이 정수 10칸이라 소수점 회복 대신 확률-정수 방식이 이미 검증돼 있다)
lifestealCap = 0.20   // data/stats.js STAT_REGISTRY.lifesteal.cap
```

- **소비 지점**: `main.js`의 `applyDamageToMonster`/`applyDamageToBoss` — `calcDamage`가
  데미지를 반환한 직후, 크리 여부와 무관하게 판정한다.
- **공격속도가 빠를수록 유리하다**: 초당 판정 횟수가 늘어나므로 쌍검(4.821회/초)이
  가장 큰 수혜를 받는다 — 4.1에서 근접으로 딜 천장을 옮긴 것과 같은 방향으로 작용해
  "근접이 위험을 감수하는 대신 보상도 크다"는 설계 의도를 강화한다.
- **딜링모드 중에는 발동하지 않는다** — 4.1-1 참고. 딜링모드 자체가 회복량계수를
  딜로 바꿔 쓰는 스킬이라, 그 위에 라이프스틸까지 겹치면 "체력을 자원으로 쓴다"는
  전제가 무너진다.

### 15.3 스킬은 "효과 조각"의 조합 `[✅ 구현 완료 — taunt 조각이 실제로 쌍검 분신에 쓰이고 있음]`

스킬마다 전용 함수를 짜면 스킬 10개에 코드 10벌이 된다.
조각을 만들어 두고 **데이터로 조합**하면 새 스킬은 한 항목 추가로 끝난다.

미리 만들어 둘 조각: `damage` `dash` `buff` `dot` `summon` `heal` `taunt` `stun`

```js
// data/skills.js
export const SKILLS = {
  charge_thrust: {
    name: "관통돌진", key: "Q", cooldown: 10,
    effects: [
      { type: "dash", distance: 4, direction: "forward" },
      { type: "damage", ratio: 1.8, damageType: "physical", canCrit: true }
    ]
  },
  shadow_clone: {
    name: "그림자분신", key: "Q", cooldown: 14,
    effects: [
      { type: "summon", duration: 5, behavior: "taunt" },
      { type: "buff", stat: "guaranteedCrit", value: 1, nextAttacks: 1 }
    ]
  }
};
```

**이 구조의 이점**

| 나중에 할 일 | 필요한 작업 |
|---|---|
| v2에서 R 스킬 4종 추가 | `skills.js`에 항목 4개 추가. 코드 수정 0 |
| v3 탱커 도발 | `taunt` 조각을 쌍검 분신에서 이미 만들어 둠 → 재사용 |
| v3 힐러 광역 힐 | `heal` 조각에 `target: "party"` 옵션만 추가 |

> 쌍검 분신에 `taunt`를 넣은 것은 우연이 아니다.
> v1에서 미리 만들어 두면 v3 탱커 구현이 절반으로 줄어든다.

### 15.4 저장 데이터 버전 관리 `[✅ 원칙 구현 완료 — 실제 이력은 14.1 참고]`

**v2에서 필드를 추가해도 v1 저장이 깨지지 않아야 한다.**
(예시 코드의 `slot_${n}` 다중 슬롯은 원안이고, 실제로는 9.1-1에서 밝혔듯 단일 슬롯이다.)

```js
// core/save.js
const SAVE_VERSION = 1;

function loadSlot(n) {
  const raw = JSON.parse(localStorage.getItem(`slot_${n}`));
  return raw ? migrate(raw) : null;
}

function migrate(data) {
  if (data.version === 1) {
    data.partyRole = null;    // v2에서 추가된 필드
    data.version = 2;
  }
  return data;
}
```

> 이걸 안 해두면 v2 출시 시 기존 유저의 슬롯 3개가 전부 날아간다.
> 그걸 잃은 유저는 다시 안 온다.

### 15.5 밸런스 전용 파일 `[✅ 구현 완료 — 실제 키 이름은 다르지만 원칙 그대로]`

플레이 테스트에서 **가장 자주 만질 값**을 한 파일에 모은다.
(`data/balance.js`가 실제 파일 — 예시의 `healerDealRatio` 등 힐러 딜 환산 관련 값은
4.1-1에서 밝혔듯 아직 코드가 없어 값 자체가 존재하지 않는다.)

```js
// data/balance.js
export const BALANCE = {
  damageFloorRatio: 0.1,      // 최소 데미지 하한 10%
  incomingDamageCap: 0.3,     // 피격 상한 30%
  hpBars: 10,
  regenDelaySec: 12,
  dashCooldown: 8,
  dashDamageReduction: 0.5,
  healerDealRatio: 2.5,       // 힐러 딜 환산 — 활보다 낮게 유지
  healerRefundCap: 0.25,
  iframeSec: 0.8
};
```

### 15.6 CLAUDE.md에 넣을 규칙 `[✅ 구현 완료 — 규칙 1개 추가됨]`

프로젝트 폴더의 `CLAUDE.md`에 아래를 적어 두면 클로드 코드가 구조를 유지한다.

```
- 밸런스 수치는 data/ 폴더 안에만 둔다. core/ 하드코딩 금지
- 새 직업·스킬·몬스터는 데이터 객체 추가로만 구현한다
- 스킬은 effects 배열의 조각 조합으로 정의한다. 스킬 전용 함수를 새로 만들지 않는다
- 저장 구조 변경 시 SAVE_VERSION을 올리고 migrate()에 처리를 추가한다
- 한 번에 한 단계씩만 구현한다. 다음 단계 전에 동작을 확인한다
- 그리기는 render.js에만 둔다. 도형을 이미지로 교체할 때 한 곳만 고치기 위함
- 작업을 마치고 커밋한 뒤에는 origin master로 푸시한다. 푸시해야 GitHub Pages에
  반영되어 실제 플레이어가 볼 수 있다.   ← [🔄 신규 추가]
```

---

## 16. 첫 출시 기간을 줄이는 방법 `[🔄 부분 진행 — 세부 상태는 각 절 참고]`

**목표는 완성도가 아니라 검증이다.**

### 16.1 시점과 그래픽 `[✅ 구현 완료 — 탑다운·회전 스프라이트 방식으로 그려짐(현재는 도형)]`

**시점: 탑다운 (위에서 내려다보는 방식).**

탑다운을 택한 결정적 이점 — **방향별 스프라이트가 필요 없다.**
스프라이트 한 장을 마우스 방향으로 회전시키면 모든 방향이 해결된다.

```js
ctx.rotate(Math.atan2(mouseY - playerY, mouseX - playerX));
```

사이드뷰·8방향은 방향마다 그림이 따로 필요해 에셋 작업이 몇 배로 늘어난다.
WASD 이동 + 마우스 조준이라는 조작 설계와도 정확히 맞물린다.

| 요소 | 1차 (도형) | 2차 (에셋 교체) |
|---|---|---|
| 플레이어 | 원 + 방향 선 | Kenney 탑다운 캐릭터 (회전) |
| 몬스터 | 색상 원·사각형 (등급별 크기·색) | Kenney Roguelike 팩 |
| 배경 | CSS 그라데이션 + 도형 실루엣 | 타일 배경 |
| 평타 모션 | 반원 호(arc) 0.15초 | 스프라이트 + 파티클 |
| 스킬 모션 | 단색 도형 확대·회전 0.3초 | Particle Pack |

#### 노멀맵 조명은 Canvas 2D로 불가능 (기록)

빛에 따라 스프라이트 명암이 바뀌는 효과는 **픽셀마다 빛 방향을 계산하는 셰이더**가 필요하다.
Canvas 2D API에는 그런 기능이 없다. 노멀맵 에셋 상품 설명에도 지원 엔진으로
Unity·Unreal·GameMaker·Godot이 명시되고 Canvas 2D는 없다.

→ 구현하려면 **WebGL 전환**(PixiJS 등)이 필요하며, 이는 16.4에서 Canvas 2D를 택한 전제
("학습 시간 0")를 무너뜨린다. **v1·v2에서는 하지 않는다.**

참고: 노멀맵은 Laigter 같은 도구로 일반 스프라이트에서 생성할 수 있으므로,
전환을 결심한다면 에셋을 따로 살 필요는 없다.

**순서를 지킬 것: 1차는 도형, 2차에 교체.**
`render.js`에 그리기를 몰아두면 교체가 한 파일 수정으로 끝난다.

> 에셋을 먼저 붙이면 게임이 그럴듯해 보여 **재미 판단이 흐려진다.**
> 도형 상태에서 강화 버튼에 손이 떨리면 그건 진짜다.

### 16.2 에셋 출처 — Kenney 하나로 끝낸다 `[🔄 부분 진행 — 다운로드만 완료, 적용은 아직]`

에셋 탐색은 시간을 무한히 빨아들인다. **아래 4개만 받고 더 찾지 않는다.**

| 팩 | 용도 | 상태 |
|---|---|---|
| **Roguelike/RPG Pack** (16x16, CC0) | 던전 배경·몬스터·아이템 | 🔄 `assets/`에 다운로드 완료, 아직 미적용 |
| Top-down Shooter (580개) | 플레이어·적 캐릭터. 회전 기준으로 그려져 있음 | ⛔ |
| Particle Pack (80+) | 치명타·스킬 이펙트 | ⛔ |
| 50 RPG sound effects / 51 UI sound effects | 타격·강화 성공/실패·버튼음 | ⛔ |

- 전부 **CC0 라이선스** — 출처 표기 불필요, 상업적 사용 자유. 나중에 광고 수익화해도 문제없음
- 모든 팩이 **동일한 스타일**이라 여러 출처를 섞을 때의 이질감이 없다
- All-in-1 통합 패키지는 유료($19.95)이나 **개별 팩은 무료** — 지금은 개별로 충분

**1차 개발 중에 미리 다운로드만 해둔다.** 붙이는 것은 2차에서.

> **[🔄 설계 변경됨] `assets/`는 `.gitignore`로 제외되어 있다** — 용량이 큰 에셋을
> 저장소에 커밋하지 않기 위해서다. 아직 `render.js`가 도형만 그리므로 실제 적용은
> 안 된 상태다.
>
> **로블록스 전환을 앞둔 판단**: 2D 픽셀 스프라이트는 로블록스로 이식되지 않는다
> (Roblox는 3D 파트·메시 기반). 그래서 **이 에셋을 웹 버전에 적용하더라도 몬스터·
> 플레이어까지만** 하고, **타일 배경·UI는 건너뛴다** — 어차피 로블록스에서 새로
> 만들 것들에 시간을 쓰는 건 낭비다. 몬스터·플레이어는 "도형이 아닌 것"만으로도
> 재미 검증(16장 서두 목표)에 바로 도움이 되지만, 타일/UI는 그 정도의 체감 개선
> 대비 작업량이 크다.

### 16.3 사운드 `[⛔ 미구현 — 코드에 오디오 재생 자체가 없음]`

- Kenney 사운드 팩으로 대부분 해결. 부족하면 freesound.org (CC0)
- 필요한 건 **5개**: 타격 / 치명타 / 강화 성공 / 강화 실패 / 보스 등장
- 배경음은 v2로 미룬다

### 16.4 기술 선택 `[✅ 구현 완료]`

| 항목 | 선택 | 이유 |
|---|---|---|
| 렌더링 | **Canvas 2D** (엔진 없음) | 학습 시간 0. 도형 그리기에 충분 |
| 빌드 도구 | **없음** | HTML 파일 열면 바로 실행 |
| 배포 | GitHub Pages | 무료, 커밋하면 자동 반영 |

> Phaser·Unity는 배우는 데만 2주가 든다. Canvas 2D는 그리기 명령 몇 개면 시작된다.

### 16.5 1차 개발에서 뺄 것

- 두 번째 스킬(E) — 첫 스킬 Q만
- 튜토리얼 · 인트로 화면
- 설정 창 (소리·화면비율)
- 저장 슬롯 3개 → 일단 1개
- 배경 6종 → 일단 2종
- 모바일 대응
- 장비 시스템

> 이걸 다 빼면 **첫 플레이 가능 버전까지 12~15일.**
> 재미가 없으면 안 만든 게 이득이다.

---

## 17. 개발 순서 `[🔄 1차 전부 완료, 2차 재구성됨 — 로블록스 전환으로 3개 항목 제외]`

### 1차 — 재미 검증까지 (12~15일) `[🔄 부분 완료 — 6.1-1의 +10 스킬 해금만 별도 승인 대기]`

| 단계 | 내용 | 기간 | 상태 |
|---|---|---|---|
| 1 | 데이터 구조 뼈대 (`data/` · `core/` 분리) | 1일 | ✅ |
| 2 | 이동(WASD) + 평타 + 자동공격 토글 + 몬스터 처치 | 3일 | ✅ |
| 3 | **치명타 + 데미지 숫자 표시 (색상·폰트 변화)** | 2일 | ✅ |
| 4 | 무기 강화 + 확률표 + 강화 부가 효과 | 3일 | 🔄 확률표·강화 판정 ✅, 부가 효과(6.1-1: 사거리·투사체 수/2연타·관통) ✅, **+10 스킬 해금만 별도 승인 대기로 보류** |
| 5 | 체력 10칸 + 피격 + 무적시간 + 대시 | 2일 | ✅ |
| 5-1 | **맵 확장 + 벽 경계 + 카메라 추적** | 2일 | ✅ |
| 5-2 | **구역 분리(대장간·사냥터) + 타이머 정지 + 몬스터 15~20 배치 + 코너 파밍** | 3일 | ✅ |
| 6 | 직업 4종 + **첫 스킬(Q)만** + 보스 1~2단계 + 광폭화 | 4일 | ✅ |

> 4번의 "강화 부가 효과"(6.1-1)는 사거리 증가(+15/+20)·단일 대상 명중 배가(+18)·다중 대상
> 확장(+23, 원거리는 관통·근접은 판정 부채꼴 확대)까지 구현했다. "+10 무기 스킬 1개 해금"만
> 기존 세이브의 즉시 스킬 회수(너프)로 이어져 별도 승인 항목으로 남겨뒀다(아래 6.1-1 표기 참고).

**→ 여기서 멈추고 직접 플레이한다.**

판단 기준: **강화 버튼을 누를 때 손이 떨리는가.**
그렇다면 계속. 아니라면 `data/enhance.js`부터 조정한다.

### 2차 — v1 완성 `[🔄 진행 중 — 로블록스 전환으로 범위 재조정, 원래 약 20일 → 약 13일]`

**로블록스 전환이 정해지면서 아래 3개를 2차 범위에서 제외한다** — 로블록스가 대신
해결하기 때문이다.

| 제외 항목 | 이유 |
|---|---|
| 12 저장 슬롯 3개 | Roblox DataStore는 구조가 완전히 다름 — 웹에서 만들어도 이식 안 됨 |
| 13 모바일 대응 | 로블록스 클라이언트가 자동 지원 |
| 15 다국어(ko/en) | 로블록스에 로컬라이제이션 시스템이 내장됨 |

| 단계 | 내용 | 기간 | 상태 |
|---|---|---|---|
| 7 | 두 번째 스킬(E) 4종 | 3일 | ⛔ 미구현 (4.3) |
| 8 | 장비 + 가방 + ~~파괴~~ + 특수 옵션 | 4일 | 🔄 장비·가방 ✅, ~~파괴~~는 개념 자체가 폐지(7.5), 일반 옵션 ✅, 특수 옵션(규칙형) ⛔ (7.3-2) |
| 9 | 보조 아이템(확률권 3종) + 특수 드랍 + **상점** | 3일 | 🔄 확률권 3종 ✅(6.3), 특수 드랍 ⛔(8.2), 상점 ⛔(10장) |
| 10 | 보스 3~6단계 + 배경 6종 + ~~광폭화~~ + **클리어 주머니** | 4일 | 🔄 3~6단계 ⛔, 배경 6종 중 2종 남음(8.0), ~~광폭화~~는 **이미 구현됨**(9.1), 클리어 주머니 ⛔(어느 챕터에도 스펙 없음 — 미정의) |
| 11 | ~~재도전~~ + 조기 도전 | 3일 | 🔄 **재도전은 이미 구현됨**(9.4), 조기 도전만 남음(9.3) |
| ~~12~~ | ~~저장 슬롯 3개 + 설정 창~~ | ~~3일~~ | 저장 슬롯은 위 표대로 제외. 설정 창(8.4)은 남는 작업 |
| ~~13~~ | ~~모바일 대응 + 광고 자리~~ | ~~2일~~ | 모바일은 위 표대로 제외. 광고 자리(13장)는 남는 작업이나 로블록스에서는 다른 수익 모델(PRD-forge-game-roblox.md 20장)로 대체 검토 |
| 14 | **퀘스트 시스템** (밸런스 조정 이후) | 3일 | ⛔ 미구현 (11장) |
| ~~15~~ | ~~다국어(ko/en)~~ | ~~2일~~ | 위 표대로 제외 |

**남은 2차 항목 정리**: 7 E스킬 / 8 장비 특수 옵션(규칙형) / 9 상점 + 특수 드랍 / 10 보스
3~6단계·배경 2종 / 11 조기 도전 / 8.4 설정 창 / 14 퀘스트. 기간은 원래 약 20일에서
**약 13일**로 줄어든다(제외 3항목 + 이미 끝난 부분 반영).

### 3차 — 밸런스 (계속) `[✅ 진행 중 — 이번 세션까지 여러 차례 반영됨]`

`data/balance.js`와 `data/enhance.js`만 수정. 코드는 건드리지 않는다.

### 4차 · 5차 — v2 · v3

15장의 데이터 구조를 지켰다면 **v2는 약 1주, v3는 약 3주.**
지키지 않았다면 각각 그 3배가 걸린다.

> **[🔄 설계 변경됨]** 이 구도 자체가 로블록스 전환으로 바뀐다 — v2·v3는 이제
> "같은 웹 코드베이스 위에 기능을 얹는" 것이 아니라 **로블록스로 이식하며 다시
> 만드는** 작업이 된다. PRD-forge-game-roblox.md 20장 참고.

---

## 18. 출시 후 개선 루프 (월 1~2회) `[⛔ 미구현 — 아직 출시 전. 지표 수집 파이프라인 없음(12장 서버 미구현과 연동)]`

수집할 데이터:

| 지표 | 판단 기준 |
|---|---|
| 시간대별 평균 강화 단계 | 5분/10분 시점에 어디쯤 있는가 |
| 보스 단계별 클리어율 | 특정 단계에서 급락하면 그 벽을 낮춘다 |
| 재도전 사용률 | 2회를 다 쓰는 비율이 높으면 난이도 과다 |
| 조기 도전 시도율 | 낮으면 보너스를 올리거나 배너 노출을 강화 |
| 이탈 시점 | 몇 분째에 창을 닫는가 |

**개선 방향**: 어려워서 막히는 구간을 찾아 푸는 용도로만 쓴다.
신규 콘텐츠 추가는 유저가 남아 있는 것이 확인된 뒤에.

---

## 21. 성공 기준

수익이 아니다.

1. **완주** — 출시까지 도달했는가
2. **재미** — 만든 본인이 20분 이상 하게 되는가
3. **반응** — 커뮤니티 반응이 오는가 ("재밌다" 또는 "이게 아쉽다" 둘 다 성공)

3번이 오면 스킬 확장·보스 추가로 간다. 반응이 없으면 게임이 아니라 **유통 방식**을 먼저 바꾼다.
