-- 반짝이 몬스터(19-4 [6]). 웹 data/balance.js rareSparkleChance/sparkleGradeChances를
-- 그대로 옮긴다 - 웹에서 이미 검증된 확률이라 새로 정하지 않는다.
--
-- 웹 조사 결과 정정(지시 - "내 기억이나 추측으로 만들지 마라"): 실제 웹 코드
-- (main.js rollRareType/spawnMonster, core/render.js drawMonster/오프스크린 화살표,
-- PRD-forge-game.md 8.0-5)를 확인한 결과, "접두사(수식어)로 스탯이 바뀌는 몬스터" 시스템은
-- 존재하지 않는다 - 기억하신 "단단한/여린" 같은 스탯 배율 접두사는 웹에도 없다. 실제로는
-- 스폰 시점에 굴리는 "희귀 타입" 2종(sparkle/material)뿐이고, 스탯(HP·방어력·공격력)은
-- 전혀 안 바뀐다(체력·방어력은 그 구역 등급을 그대로 따른다, PRD 8.0-5 "공통 규칙" 원문) -
-- 골드만 낮아지고(보상이 아이템이라는 뜻) 사망 시 드랍만 특별해진다. 도망 행동도 없다
-- (PRD 원문 "도망가지 않는다 - 발견하면 반드시 잡을 수 있어야 한다") - 기억하신 "도망" 행동은
-- 실제 웹 코드에 없다.
--
-- 이번 이식 범위 - 반짝이만: 웹의 또 다른 희귀 타입 "재료 몬스터"는 보상이 "확률 강화권"
-- (강화 시도를 골드 대신 아이템으로 대신하는 소모품)인데, 로블록스 강화(EnhanceConfig)는
-- 골드만 쓰고 강화권 개념 자체가 없다 - 없는 시스템(강화권)을 이번에 새로 만들면서까지
-- 재료 몬스터를 끼워 넣지 않는다(지시 - "웹 값이 이 원칙과 어긋나면... 보고하고" 원칙과
-- 같은 결로, "이식할 대상 시스템 자체가 없다"는 사실을 보고하고 범위를 좁혔다). 반짝이는
-- 최종 보상이 갑옷 드랍(로블록스에 이미 있는 축)이라 그대로 옮길 수 있었다.
return {
	-- 스폰 시점 판정(웹과 동일 시점 - "스폰 시 낮은 확률로 특별한 몬스터가 대신 등장한다").
	-- 잡몹 리스폰마다 매번 다시 굴린다(MonsterSpawner.spawn이 매 스폰마다 호출).
	-- D1-3(사용자 결정 - D1-2 보고 ④ 결정 3): 0.5% → 0.1%. 새 등급표 그대로 0.5%면 1인 태초 +40% · 10만 h 하루 알림 약 58회라 출현을 낮췄다.
	sparkleChance = 0.001,

	-- 확정 드랍 등급 분포. D1-2(사용자 결정): 태초 · 고대 = 보스 첫 클리어(DropTableData.bossGrades.firstClear)의 절반, 나머지를 영웅 · 전설 · 유물로(영웅 이상 확정).
	-- 옛 값(웹 - 유물 90 · 고대 9 · 태초 1%)은 처치당 태초 0.005%로 D1 잡몹(0.0001%)의 50배라 태초 공급을 반짝이가 지배했다(D1 리뷰 1).
	-- 공급 = docs/phase/D1-2-report.md ④(처치당 태초 = sparkleChance × 0.005%). 합 = 1(검증 D1-2(가)).
	sparkleGradeChances = { epic = 0.55, legendary = 0.36, relic = 0.088, ancient = 0.00195, primordial = 0.00005 },

	-- 골드는 오히려 줄어든다(웹 BALANCE.rareGoldMultiplier=0.5, PRD 8.0-5 "골드는 적게
	-- (보상이 아이템이므로)") - 확정 최고 등급 드랍 하나가 보상의 전부라는 뜻을 골드에도
	-- 반영한다. 반짝이가 "무조건 이득"이 아니라 "아이템에 몰아준" 결과라는 점을 그대로 옮긴다.
	goldMultiplier = 0.5,

	-- 전용 보상(22-2 [2]) - 확정 드랍 위에 "그 구역 잡몹 N마리분 골드"를 얹는다(받는 사람의
	-- 스테이지 기준, 잡몹 골드와 같은 InfiniteStage 배율). 위 goldMultiplier(0.5)는 이 보너스와
	-- 별개로 반짝이 자신의 처치 골드에만 곱한다(웹 규칙 유지). 등급표(sparkleGradeChances)는
	-- 절대 올리지 않는다 - 스폰 0.5% × 태초 1% = 처치당 0.005%가 19-4가 잡은 선이고, 20-4의
	-- 보스 첫 처치(태초 2%/0.1%)와의 균형이 그 위에 서 있다(PRD 20.48 [2] 비교표). 골드는
	-- 등급 체계 밖의 축이라 그 균형을 건드리지 않는다. 경제 영향: 0.5% × 10마리분 = 잡몹 골드
	-- 수입의 약 +5%.
	-- D1-2(사용자 결정 - 금빛 몹 = 골드 크게): 10 → 25마리분(반짝이 1마리 ≈ 잡몹 25.5마리 골드). 사냥 골드 몫 = 0.5% × 25.5 ≈ 11%(목표 10 ~ 15% - 보고서 D1-2 ④).
	-- 처치 때 금화 분수 연출(client/SparkleMonsterVisual - coinFountain). 강화 재료 마릿수분은 옛 10 그대로(materialBonusKillEquivalent - 재료 공급은 안 바꾼다).
	-- D1-3(사용자 결정): 출현 0.1%에 맞춰 25 → 125마리분(사냥 골드 중 반짝이 몫 ≈ 0.1% × 125.5 ≈ 11% 유지).
	goldBonusKillEquivalent = 125,
	materialBonusKillEquivalent = 10,
	-- 금화 분수(처치 지점 · 같은 서버 곁의 사람 화면): 입자 예산 = 한 번 최대 count개 · lifeSeconds 뒤 전부 사라짐(파티클 없이 파트 - 드랍 번개와 같은 방식).
	coinFountain = { count = 18, lifeSeconds = 1.4, upSpeed = 26, spread = 9, gravity = 60, size = 0.55, maxDistance = 160 },

	-- 시각(19-4 [6] → 22-2 [2]에서 이펙트 레이어로 재구성). 몬스터 모델의 어떤 속성도
	-- 건드리지 않는다 - 크기 배율(옛 sizeMultiplier 1.3)·Body 안의 PointLight 둘 다 모델에
	-- 얹힌 것이라 최종 모델로 갈아끼우면 사라지므로 폐기했다. 대신 클라이언트
	-- (SparkleMonsterVisual.client.lua)가 모델 밖에 자기만의 이펙트 파트를 만들어 매 프레임
	-- 모델 피벗을 따라가게 한다 - 서버는 "SparkleMonster" 태그 하나만 붙인다. 아래 값은
	-- 그 이펙트 레이어의 수치다. glowRangeStuds(55)는 tier 구역 반너비(96)의 절반을 넘겨
	-- "구역 안쪽에서도, 입구 쪽 복도에서도" 보이게 잡았고(19-4), pillarHeightStuds(60)는
	-- 옆 구역(중심 간 160stud)에서도 지평선 위로 솟은 빛기둥이 보이게 하는 값이다.
	glowRangeStuds = 55,
	glowBrightness = 3,
	pillarHeightStuds = 60,
	pillarWidthStuds = 3,
	particleRate = 24,
}
