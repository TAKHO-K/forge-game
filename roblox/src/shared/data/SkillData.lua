-- 스킬 8종(직업당 Q/E) 정의의 단일 출처(20-2a). 지금은 대검 2종만 채운다 - 나머지 6종은
-- 다음 세션들(20-2b~)의 자리만 남겨 둔다(빈 테이블).
--
-- coefficient는 반드시 "공격력(atk) 대비 비율"이다 - 절대값을 쓰지 않는다. 20-1이 만든
-- 무기 등급 축과 강화 단계가 전부 atk 하나에 곱해지므로(Enhance.getPlayerAttack), 계수를
-- 비율로 두면 등급이 오르든 강화가 오르든 스킬 데미지가 자동으로 같이 늘어난다(20-1
-- [3](다)에서 이미 실측 확인한 성질 - 등급별로 스킬 밸런스를 다시 잴 필요가 없다는 게
-- 이 설계의 핵심 이유).
--
-- 실제 계수·쿨다운 출처: PRD-forge-game.md 4.3(대검 C_Q=5.5·C_E=5.91, 채널링 위험
-- 프리미엄 `0.15×(1-channelMoveSpeedMultiplier)` 반영, 스테이지 환산까지 끝난 검증값).
-- 20-2a 작업 지시문에 있던 "계수8/쿨다운10, 계수14/쿨다운14/채널0.8초"는 두 PRD 어디에도
-- 기록이 없는 값이라([0] 확인 결과 사용자 승인) 채택하지 않았다.
return {
	greatsword = {
		Q = {
			name = "관통돌진",
			shape = "line", -- 시작점~끝점 경로에 닿는 전원 타격(AttackServer의 단일 대상 픽과 다르다).
			-- 21-1 [1]-B: 5.5 → 6.9(+25%). 20-7 재측정에서 대검 단일 로테이션이 4직업 최저였고,
			-- 채널링 중 평타 차단(C)까지 적용하면 격차가 더 벌어진다. PRD 4.3의 C_Q=5.5는
			-- "평타 5~6대 치명타 상당"이라는 절대 하한이지 상한이 아니다 - 그 위로 올린다.
			-- 격자 간격은 안 바꿨다(PRD-forge-game-roblox.md 20.44 [1]-B 참고 - 어그로 0.4s·
			-- 리쉬 0.6s 관계식상 동시 3~4마리는 간격 조정으로 못 만든다).
			coefficient = 6.9,
			cooldownSeconds = 10,

			-- 돌진 거리·시간(20-2a [2]) - 웹 220px/0.3초를 pxPerStud(10)로 기계 환산하지
			-- 않는다. PRD-forge-game-roblox.md 20.15가 이미 이 방법을 폐기했다 - "사거리·
			-- 이동 속도는 픽셀 비율이 아니라 느낌이 맞는 독자적인 값으로 새로 정의한다,
			-- 3인칭 카메라 거리감과 안 맞기 때문"(사용자 승인, 20-2a [0]). 대신 이 프로젝트
			-- 안의 기준점으로 새로 잡는다: 대검 사거리 13stud(CombatConfig.attackRangeStuds
			-- 10 × ClassData.greatsword.rangeMultiplier 1.3)보다 확실히 길어야 "돌진"이라는
			-- 이름값을 하고, 사냥터 한 변(WorldConfig 192stud)의 약 10%면 담장 안에서 실제로
			-- 여러 몬스터를 꿰뚫을 여지가 있다 - 18stud로 잡는다. 0.25초(72stud/s)는 평타
			-- 쿨다운(0.28초/atkSpeed)보다 짧게 느껴지도록 잡은 값 - 정확한 체감은 실기로
			-- 맞춘다(지시 [4], SkillServer.server.lua/WeaponVisual 쪽 참고).
			rangeStuds = 18,
			durationSeconds = 0.25,
			-- 경로 좌우로 몬스터를 인식하는 폭(선분 판정의 반경) - 칼날 리치를 감안한 값,
			-- 몬스터 판정 파트가 대략 반지름 2stud대라 5stud면 스치는 정도도 맞는다.
			hitRadiusStuds = 5,
		},
		E = {
			name = "회전베기",
			shape = "circle", -- 시전자 중심 반경 안 전원 타격.
			-- 누적(총합) 계수 - PRD 4.3 "3초간... 주변 지속 타격(누적 데미지 계수 5.91)"은
			-- 채널링이 끝나는 순간 한 번이 아니라 진행 중 지속 틱이다. 이번 작업 지시문
			-- [3]은 "채널링이 끝나는 시점에 한 번"이라고 적었지만 실제 명세(PRD 4.3)와
			-- 달라 명세를 따른다(지시 [3]의 "명세가 다르면 명세를 따르고 보고해라" 그대로,
			-- 20-2a [0] 보고 참고). tickCount로 나눠 매 틱 coefficient/tickCount만큼 때린다.
			-- 21-1 [1]-B: 5.91 → 7.4(+25%, Q와 같은 비율 - 채널링 프리미엄 1.075 유지: 6.9×1.075≈7.4).
			coefficient = 7.4,
			cooldownSeconds = 16,
			channelSeconds = 3,
			-- 1초 간격 3틱 - PRD 4.3 쌍검 E(8초 도트/8틱→1틱=1초)와 같은 "1초에 1틱" 관례를
			-- 그대로 재사용한다(새 캐던스를 만들지 않는다).
			tickCount = 3,
			channelMoveSpeedMultiplier = 0.5,
			-- 채널링 중 받는 피해 배율 - PRD 4.3 "받는 피해 50% 감소"를 그대로 반영한다.
			-- 이번 작업 지시문 [3]엔 없던 항목이지만 실제 명세에 있어 반영했다(위와 같은 사유).
			incomingDamageMultiplier = 0.5,
			-- 반경(20-2a [3]) - 대검 사거리 13stud의 약 1.5배인 20stud. 단일 대상 평타
			-- 리치보다 넉넉히 커야 광역이라는 이름값을 하고, 사냥터(192stud)를 뒤덮을
			-- 만큼 크지는 않아야 "제자리 회전"이라는 스킬의 정체성이 유지된다.
			radiusStuds = 20,
		},
	},

	-- 20-2b: 활 2종 - Q(속사)는 순수 자기 버프, E(백스텝샷)는 대시+버프. 자기 버프
	-- 프레임워크([1])와 투사체 프레임워크([2])를 여는 첫 사용자다(단, 평타 자체를 투사체로
	-- 바꾼 게 진짜 첫 사용자 - AttackServer.server.lua 참고. 활 스킬 자체는 둘 다 버프다).
	--
	-- [0] 확인 결과 - 활 E는 "관통사격"이 아니다: 이번 작업 지시문의 "직선 관통 1발,
	-- 계수2, 쿨다운12초"는 PRD 4.3 어디에도 없는 값이다. 실제 PRD 4.3 기록은
	-- "백스텝샷 - 뒤로 대시 + 다음 평타 5발에 마법피해 추가, 그 5발 치명타 확률 +30%p,
	-- 쿨다운15초"이고, 로블록스 PRD 20.8-3에 이미 이 스킬 기준 E-Q 시너지 계산(≈3.81단계,
	-- E를 Q보다 먼저 켜서 Q의 치확 스냅샷을 끌어올리는 구조)까지 끝나 있다 - 사용자
	-- 승인으로 PRD를 따른다(관통사격/투사체 스킬이 아니라 대시+버프).
	bow = {
		Q = {
			name = "속사",
			shape = "selfBuff", -- 대상 없음, 자기 자신에게 배율을 건다([1] 프레임워크의 첫 사용자).
			cooldownSeconds = 18,
			durationSeconds = 6,
			-- 공식(PRD 4.3, 웹·PRD 완전 일치 - 20-2b [0] 확인, 유일하게 불일치 없는 Q스킬):
			-- 공속배율 = min(cap, base + 치명타확률×critCoefficient).
			attackSpeedBase = 1.0,
			attackSpeedCritCoefficient = 1.5,
			attackSpeedCap = 2.5,

			-- 꽂히는 화살(20-5 [2], PRD 20.39 숙제 3번 - "공격속도 버프는 체감이 약한데,
			-- 몸에 화살이 쌓이면 '빨라졌다'가 눈으로 읽힌다"). 속사가 켜져 있는 동안 명중한
			-- 평타(활 전용 - ProjectileConfig.kindByClass가 "arrow"인 클래스만)가 대상에
			-- 화살로 남았다가 개별적으로 터져 추가 피해를 준다(누적 폭발이 아니다 - 몬스터가
			-- 중간에 죽어도 이미 터진 화살은 손실이 없다, StuckArrowState.lua 참고).
			--
			-- 계수 산출(20-5 [2] 지시 - "이 값이 넘을 수 있는 기존 관계식을 먼저 확인"):
			-- 활 기본 킷 60초 딜 천장 408.5, 쌍검 436.5(PRD-forge-game.md 4.4) - 활이 쌍검을
			-- 넘으면 안 된다는 명시 규칙(같은 문서)과 "직업 간 단일 총딜 격차 1.32배 이내"
			-- 규칙(PRD-forge-game-roblox.md 20.36 [4], 대검 330.8×1.32≈436.5로 사실상 같은
			-- 상한)이 가리키는 여유폭은 (436.5-408.5)/408.5 ≈ 6.85%다.
			-- 이 화살은 속사 버프 중 명중한 평타에만 붙는다 - 버프 가동률(지속6초/쿨18초=
			-- 33.3%) 동안은 공속 배율(속사 단독 1.225배, 최댓값 2.5배)만큼 평타가 더 자주
			-- 나가므로, 전체 평타 중 이 효과가 붙는 비율은 시간 비율보다 크다:
			--   비율 = (0.333×배율) / (0.333×배율 + 0.667)
			--   배율 1.225(속사 단독) → 비율 ≈ 38.0%,  배율 2.5(이론상 상한) → 비율 ≈ 55.5%
			-- 화살 1개의 기대 추가딜/기본 평타 기대딜 비는 계수 c와 같다(둘 다 같은 치명타
			-- 기대식을 타므로 치명타 항이 약분된다) - 그러므로 총딜 증가율 ≈ 비율×c다.
			-- 최악(비율 55.5%)에서도 여유폭 6.85%를 넘지 않으려면 c ≤ 0.1234 - 지시대로
			-- "보수적으로" 그 절반 아래인 0.08로 잡는다(최악 증가율 ≈4.4%, 평상시(비율
			-- 38%) ≈3.0% - 둘 다 여유폭 안). 시각적 만족이 목적이지 딜 증가가 목적이
			-- 아니므로 하한 없이 작게 잡았다 - 20.39의 4직업 60초 총딜 재측정(20-7 예정)
			-- 때 실제 시뮬레이션으로 재검증한다(이 값은 그 전까지의 잠정값).
			stuckArrowDamageCoefficient = 0.08,

			-- 개별 폭발 지연 - "누적했다가 한꺼번에 터뜨리지 마라"(지시)를 만족하려면 화살
			-- 하나하나가 짧게 살아있다가 각자 터져야 한다. 속사 중 평타 간격(최소 0.28/2.5
			-- ≈0.11초)보다 확실히 길게 잡아 "화살이 꽂히는 순간"이 먼저 보이고 그 다음에
			-- "터지는 순간"이 따로 보이게 한다 - 두 순간이 겹치면 그냥 즉발 피해와 구분이
			-- 안 된다.
			stuckArrowDelaySeconds = 0.8,

			-- 몬스터 하나에 동시에 남아있을 수 있는 화살 상한(지시 - "지저분하지 않을
			-- 상한"). 위 지연(0.8초)과 결합하면 속사 최고 배율(2.5배, 평타 간격 0.11초)에서
			-- 약 0.4~0.9초 만에 가득 차고, 그 뒤로는 새 화살이 꽂힐 때마다 가장 오래된
			-- 화살이 그 자리에서 즉시 터진다(StuckArrowState.lua의 캡 초과 처리) - 피해
			-- 손실 없이 "계속 터지는" 느낌이 유지된다.
			stuckArrowMaxPerMonster = 4,
		},
		E = {
			name = "백스텝샷",
			shape = "dash", -- 대검 Q와 판정 유형은 같은 "돌진"이지만 방향이 반대(뒤로)고 때리지 않는다.
			cooldownSeconds = 15,
			-- 대검 Q(관통돌진, 18stud/0.25초=72stud/s)보다 짧고 느리게 잡는다 - 이건 회피용
			-- "짧은 뒷걸음"이지 교전 거리를 좁히는 돌진이 아니다. 12stud/0.2초=60stud/s.
			rangeStuds = 12,
			durationSeconds = 0.2,
			-- 다음 N회 평타 버프(PRD 4.3) - 이 스킬 자체는 아무도 때리지 않는다.
			chargesGranted = 5,
			critRateBonus = 0.30,
			-- "마법피해 추가"의 정확한 계수는 PRD 4.3에 숫자가 없다(20-2b [0] 확인 - 로블록스
			-- PRD 20.8-3의 E-Q 시너지 계산도 공속 배율만 다루고 이 추가 피해는 아예 안 쓴다).
			-- 계수 원칙(atk 비율)을 지켜 0.5로 잠정 설정한다 - 실기로 조정이 필요한 값이라는
			-- 걸 분명히 남긴다.
			damageCoefficient = 0.5,

			-- 사거리 2배(20-4 [2], 지시 원문 "백스텝샷 사용 후 부여되는 5충전이 남아 있는
			-- 동안 평타 사거리가 2배가 된다"). 실제 적용값은 이 배율을 그대로 곱하지 않고
			-- PlayerCombat.getBuffedAttackRange가 CombatConfig.rangeBuffAggroMarginStuds로
			-- 어그로 범위 아래에서 자른다 - "2배"는 이 필드가 표현하는 의도이지 최종 적용값의
			-- 보장이 아니다(그 이유는 CombatConfig.rangeBuffAggroMarginStuds 주석 참고).
			rangeMultiplier = 2,
		},
	},

	-- 20-6: 쌍검 2종. Q(그림자분신)는 PRD 4.3을 그대로 따른다(분신은 도발용 - 스스로
	-- 공격하지 않는다, 자체 피해도 없다). E(난무)는 PRD 4.3의 "독날강화"(8초 도트) 대신
	-- 이 세션 작업 지시서의 설계를 채택했다 - 사용자 확인 결과([0] 보고, 20-6 세션) PRD와
	-- 지시서가 정면으로 다른 스킬을 가리켜 확인을 거쳤고, "지시서대로 난무"로 확정됐다
	-- (PRD-forge-game.md 4.3 쌍검 E 절도 이 설계로 같이 갱신했다). 계수(14.46)·쿨다운(20초)
	-- 자체는 PRD의 "전체 딜의 26.7%를 스킬이 차지하도록 역산한 총 예산" 수치를 그대로
	-- 가져왔다 - 독날강화든 난무든 "이 스킬 한 번이 벌어야 할 총딜"이라는 예산의 성격은
	-- 같아서다. 다만 독날강화엔 있던 "지속 중 치확 +50%p" 부가가치가 난무엔 없으므로,
	-- 같은 계수라도 실효 딜은 약간 낮아진다 - 4직업 60초 총딜 재측정(다음 세션 예정)
	-- 때 실기로 다시 맞출 값이라는 점을 남긴다.
	dualblade = {
		Q = {
			name = "그림자분신",
			shape = "summon", -- 소환→지속→소멸 생명주기(SummonState.lua, 20-6 [1] 프레임워크 확장).
			cooldownSeconds = 14,
			summonId = "dualbladeDecoy",
			-- 지속시간 하나를 분신 생존시간과 확정 치명타 창이 공유한다(PRD 4.3 "두 값을
			-- 분리된 상수로 관리하면 하나만 바꾸고 잊어버릴 위험이 있다" 권고 그대로 - 화면에
			-- 분신이 떠 있는 5초 = 확정 치명타가 도는 5초, 플레이어가 외울 숫자가 하나뿐이다).
			durationSeconds = 5,
		},
		E = {
			name = "난무",
			shape = "singleChannel", -- 단일 대상 다단히트(대검 회전베기의 "채널링+분할 타격" 구조 재사용, 광역이 아니라 단일이라는 점만 다르다).
			coefficient = 14.46,
			cooldownSeconds = 20,
			-- 대검 회전베기(3초/3틱)보다 훨씬 빠른 캐던스로 "연타" 느낌을 낸다 - 새 상수를
			-- 만드는 대신 채널링+틱분할 구조 자체만 재사용하고 숫자는 이 스킬 전용으로 새로
			-- 정했다(지시 [3] "이 게임 최고 단일 화력" - 대검과 같은 리듬이면 차별화가 안 된다).
			channelSeconds = 1.0,
			tickCount = 6,
			-- 시전 시점의 대상을 끝까지 때린다(매 틱 재탐색하지 않는다) - [3] 지시의 "대상
			-- 고정 방식을 정해라"에 대한 답. 대상이 죽거나(다른 공격으로) 사거리를 벗어나면
			-- 남은 타격은 손실된다(재탐색 안 함 - "한 대상에게 전부 건다"는 결단이 이 스킬의
			-- 판단 축이다). 사거리는 쌍검 평타 사거리(ClassData.rangeMultiplier=1.0 ×
			-- CombatConfig.attackRangeStuds=10)와 같은 10stud - 새 사거리 축을 만들지 않는다.
			rangeStuds = 10,
			-- Q 확정 치명타는 첫 타에만 적용한다(지시 [3] "전부 적용되면 과하다. PRD에 기록이
			-- 있으면 따르고, 없으면 첫 타만 적용하고 보고해라" - PRD엔 난무 자체가 없어 후자를
			-- 따른다, 20-6 [0] 보고 참고).
			guaranteedCritHits = 1,
		},
	},
	-- 20-6: 힐러 2종. Q(치유)는 결과 포맷 확장([4] - hits 배열 대신 self 필드)의 첫
	-- 사용자다. E(딜링모드)는 만료 없는 토글이다(BuffState가 durationSeconds·chargesRemaining
	-- 둘 다 안 주면 자동으로 영구 버프가 된다 - 새 버프 종류를 따로 안 만들어도 됐다, 20-2b [3]
	-- 자평이 맞았다).
	healer = {
		Q = {
			name = "치유",
			shape = "heal", -- 대상 없음, 자기 체력만 바꾼다([4] 프레임워크의 첫 사용자).
			cooldownSeconds = 25,
			-- PRD 4.3 "체력 3칸 회복" - 웹 v1의 옛 10칸 체력 체계(BALANCE.playerMaxHp=10)에서
			-- "3칸"은 30%였다. 로블록스는 세그먼트 개념 없이 연속 HP 비율만 쓰므로 그 비율
			-- (0.3)을 그대로 이식한다 - 절대값(칸 수)이 아니라 "몇 %였는가"가 이식 대상이라는
			-- 판단([0] 조사 결과 로블록스 PRD·코드 어디에도 이 값이 없어 새로 내렸다, 가정
			-- 명시 - 실기 조정 대상).
			healPercentOfMaxHp = 0.3,
			-- PRD 4.3 "치명타 시 회복량 2배" - 클래스 critDmg(힐러 1.8배)와는 다른 고정값이다.
			-- calcDamage를 그대로 못 쓰는 이유(크리 롤만 재사용하고 배율은 따로 곱하는 이유).
			critHealMultiplier = 2.0,
			-- 24-3(PRD 20.64) 파티 최종피해 버프의 지속시간 배율 - 새 지속시간 상수를 만들지
			-- 않고 cooldownSeconds에서 파생시킨다(지시). 1.2배인 이유: 힐러가 쿨타임마다 제때
			-- 힐을 돌리면 버프가 안 끊기지만, 한 번 놓치면 끊긴다 - 힐러에게 할 일을 주는 값.
			partyBuffDurationMultiplier = 1.2,
		},
		E = {
			name = "딜링모드",
			shape = "toggle", -- 만료 없는 토글 - 켜고 끌 때마다 BuffState.apply/clear만 오간다.
			cooldownSeconds = 0, -- PRD 4.3 "즉시 재사용 가능"
			-- 평타 배율 - 이번 작업 지시서 [6]이 "19-3a에서 제안한 C_힐 ≈ 3.13"을 그대로
			-- 재사용하라고 준 값이다(새로 만들지 않는다).
			attackMultiplier = 3.13,
			-- 초당 최대체력 소모율 - 새로 만드는 상수라 기존 관계식(PlayerRegen.server.lua의
			-- regenPercentPerSecond=0.04)에서 역산한다. 목표 가동률 60%(지시 [6])라면
			-- 가동/비가동 시간비 = 0.6/0.4 = 1.5이고, "켜져 있던 동안 소모한 양 = 꺼져있는
			-- 동안 회복한 양"이 균형점이므로 D×Ton = 0.04×Toff, Ton/Toff=1.5 →
			-- D = 0.04/1.5 ≈ 0.0267. 다만 이 역산은 회복 시작 전 5초 대기(regenDelaySeconds)를
			-- 무시한 근사치다 - 대기 시간만큼 실제 가동률은 60%보다 낮게 나올 가능성이 높다는
			-- 뜻이므로, 실측 후 이 값을 낮추는 방향으로(가동률을 올리는 방향으로) 조정할
			-- 가능성이 크다는 것을 남겨 둔다(지시 [6] "실측 가동률이 목표와 다르면 계수를
			-- 조정해라").
			--
			-- 21-1 [1]-D: 0.0267 → 0.012. 위 예상대로 실측 가동률이 낮았다 - 기준 시나리오를
			-- "전투 중 피격 0.25/초(피격 1회 = 최대체력의 10.3%, 앵커 7타 기준)"로 확정하고
			-- BalanceSim.solveHealerDrain(0.6, ...)로 역산한 값 0.0118을 반올림했다(가동률 0.599).
			-- 5초 대기·이탈 2초·복귀 2초·Q 치유까지 넣은 simulateHealerCycle 기준이라 위의
			-- 단순 역산(0.0267)과 다르다. 피격 0이면 가동률 100%(소모 < Q 치유), 피격 1.0/초면
			-- 25% - 실전 체감은 그 사이다. 20.44 [1]-D 참고.
			drainPercentPerSecond = 0.012,
		},
	},
}
