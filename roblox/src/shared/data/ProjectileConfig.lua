-- 투사체 단일 출처 상수(20-2b). 비행 속도는 client/Projectiles.lua가 원래 갖고 있던 값
-- (화살 90stud/s·구슬 55stud/s, 14-2에서 실기로 맞춘 값 - 새로 만들지 않는다)을 그대로
-- 옮겼다 - 서버(AttackServer.server.lua, 피해 판정 시점 계산)와 클라(Projectiles.lua,
-- 시각 재생)가 반드시 같은 숫자를 써야 "도달하는 순간"이 서로 어긋나지 않는다.
--
-- kindByClass: 직업 -> 투사체 종류. 근접(대검·쌍검)은 없다 - 평타가 즉시 판정이라
-- 투사체 개념 자체가 없다(20-2b [2] 지시 - "평타도 투사체로 전환"은 원거리 한정).
return {
	kindByClass = {
		bow = "arrow",
		healer = "orb",
	},

	speedStudsPerSec = {
		arrow = 90,
		orb = 55,
	},

	-- 비행 중 몬스터가 움직여서 빗나갈 수 있는 폭(20-2b [2] "몬스터가 움직이므로 빗나갈
	-- 수 있다") - 발사 시점 조준 지점에서 이 거리 밖으로 벗어나면 도달 시점에 허공을
	-- 가른다. 몬스터 판정 파트 반지름(대략 1~2stud)보다 넉넉히 커서 "약간만 움직여도
	-- 매번 빗나가는" 답답함은 피하되, 실제로 빗나가는 사례가 나올 만큼은 좁다.
	hitToleranceStuds = 6,

	-- 발사체별 관통 플래그(20-2b [2] 지시) - 지금 평타(활·힐러)는 AimPicker.pick이 발사
	-- 시점에 단일 대상 하나로 확정하는 구조라 true로 바꿔도 지나가면서 여러 명을 때리지
	-- 않는다(AttackServer.server.lua가 그 하나의 target만 도달 시점에 재검증한다) - 실제
	-- 다중 관통 판정(경로상 전원 타격)은 아직 아무 스킬도 쓰지 않아 만들지 않았다(대검
	-- 관통돌진은 SkillCombat.hitsOnSegment로 이미 즉시 판정 방식의 "관통"을 갖고 있어
	-- 다른 메커니즘이다). 나중에 발사체형 관통 스킬이 생기면 이 값을 읽어 AttackServer가
	-- 경로상 전원을 때리도록 확장하면 된다 - 지금은 선언만 해 두고 false로 고정한다.
	piercing = {
		arrow = false,
		orb = false,
	},
}
