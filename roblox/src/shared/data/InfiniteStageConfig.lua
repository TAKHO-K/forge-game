-- 무한 모드 스테이지 성장률(11-1). PRD-forge-game-roblox.md 20.9/20.11-4가 확정한
-- k=1.155를 그대로 쓴다 - HP(N)=base×k^(N-1), monsterAttack(N)=base×k^(N-1), 20.9의
-- reward(N)=R0×k^(N-1)까지 전부 같은 k 하나를 재사용하는 게 PRD의 설계 의도다(몬스터가
-- 세지는 속도라는 하나의 축으로 남겨야 한다는 20.11-4 근거를 골드에도 그대로 적용).
--
-- base 값(PRD의 스테이지1 보스체력 38,896·공격력 8)은 여기 그대로 옮기지 않는다 - 그 값은
-- 웹판 레벨·아이템 시스템(무기 Lv25+, 아이템 등급)까지 다 갖춘 시점 기준이고, 로블록스는
-- 아직 그런 시스템이 없다(ClassData.lua critRate 주석과 같은 "출처가 다른 두 값을 뭉뚱그리지
-- 않는다" 원칙). 대신 base는 MonsterData.tier1의 현재 검증된 값(hp=80, attack=8,
-- goldDrop=6)을 스테이지1로 그대로 쓰고, growthRate(k)만 PRD에서 가져온다 - 공격력은
-- 우연히 PRD의 base=8과 이미 일치한다.

return {
	-- P2.5a R1(사용자 확정): 1.155 → 1.02(스테이지당 2%). 숫자를 작게 유지하는 것이 목표 - 1e250에 닿는 스테이지가 약 1,500(1.155)에서 약 28,000(1.02)으로 멀어진다.
	-- 스테이지 1의 기준값(MonsterData tier1 HP 80 · 공격 8 · 골드 6)은 그대로다. k에서 유도되던 상수(보스 파티 HP 지수 · 파티 입장 밴드 · 등급 스테이지 환산)는
	-- 코드가 매번 이 값에서 다시 계산한다. k 기준으로 역산돼 있던 앵커(CombatConfig.damageReductionAlpha · maxHpBonusBase, CharacterLevelConfig)는 새 k로 다시 풀었다.
	growthRate = 1.02,
	-- P2.5a C8(골드 - "획득량을 줄여 소모 속도에 맞춤 · 숫자 작게"): 골드는 k 대신 이 성장률로 큰다(몬스터 · 보스 골드 · 판매가 · GoldCost가 전부 이 한 값 -
	-- InfiniteStage.getGoldMultiplier). 수입과 비용이 같은 율로 크므로 구매력(골드/분 ÷ 강화 1회 비용)은 이 값과 무관하게 스테이지 내내 일정하고, 숫자 크기만
	-- 정한다: 스테이지 20,000에서 골드 배수 1.001^19999 ≈ 4.8e8(k로 키우면 ≈ 1e172).
	goldGrowthRate = 1.001,
	-- P2.5c 결정 10(COMMON §1 "k 의존 값 = 힘 비율"): 옛 k. 옛 스테이지 번호로 정한 임계값(재료 해금 · 방지권 · 보스 밀도 · 보스 드랍 편차)을 같은 힘의 지금
	-- 스테이지로 옮길 때만 쓴다(InfiniteStage.fromLegacyStage · fromLegacySpan - 1.155 → 1.02에서 옛 1칸 ≈ 7.28칸). 게임 성장률이 아니다.
	legacyGrowthRate = 1.155,

	-- S21-0 A4 - 임시 안전 상한(docs/stage-scaling-audit.md §0-3 · §1-1). "게임에서 쓰는
	-- 모든 수치가 1e300 미만인 마지막 스테이지" - 감사 보고서의 실측 공식(1.155^(stage-1)
	-- 성장 체인) 중 가장 먼저 1e300을 넘는 항목은 보스 HP(4인 파티, 20×4^p 배율)이고 그
	-- 다음 스테이지(4739)에서 1e300을 넘는다 - 그래서 마지막으로 전부 유한한 스테이지는
	-- 4738이다. 이중정밀도(double) 자체가 완전히 깨지는 지점(4,871 ~ 4,944, §1-2)보다
	-- 훨씬 여유 있게 앞에서 멈춘다. **진짜 성장 곡선 재설계가 아니다** - 그건 Fable
	-- 결정(감사 보고서 "Fable로 넘길 결정 목록" 8번) - 이건 그때까지 double 붕괴·NaN
	-- 오염(§1-2 ④)을 안 만나게 막는 임시 장치일 뿐이다. StageServer.server.lua의 일반
	-- 스테이지 이동만 막는다 - `/gg stage`(디버그 명령)는 이 상한을 넘길 수 있되 넘기면
	-- 경고를 띄운다(측정 도구는 상한 밖도 봐야 한다).
	-- P2.5a: 같은 정의(모든 수치 1e300 미만인 마지막 스테이지)를 k = 1.02로 다시 쟀다: 4738 → 34320. 새 k에서는 레벨 경험치 누적(레벨 = 스테이지로 보수적으로 - 기준 스테이지가
	-- 레벨 + 167이다)이 보스 4인 HP보다 먼저 넘는다(로컬 하네스 이분법, P25a(가) C1.2가 같은 식으로 대조).
	-- P2.5c: 34320 → 34230 - 5회 환생 뒤 목표 마릿수가 25 → 150(CharacterLevelConfig.killTargetAfterAnchors)이 되어 레벨 경험치 누적이 ×6(= 약 90스테이지) 먼저 넘는다(같은 정의 · 같은 이분법).
	safeStageCap = 34230,

	-- P2.5a R2(사용자 확정): 설계 최대 스테이지 = 상위 1% 프로필(하루 12시간)이 약 2,190시간(6개월)에 닿는 스테이지(EconSim - 구간별 무기 성장 g의 천장
	-- CharacterLevelConfig.weaponGrowthSegments가 만든다 - P2.5c). 여기까지 모든 수치 < 1e250(P25a(가) - 1e250을 처음 넘는 스테이지 28,508). 진행 판정 · 이동 제한에는 안 쓴다 -
	-- 시뮬 이정표 · 보고서 구간 끝 · 검증 기준값이다.
	-- 값: 21,230 = 상위 1%가 누적 2,190시간에 선 최고 스테이지(EconSim 기준선 - 천장 fromLevel 20,000 · late 1.01). 그 지점 최대 수치 ≈ 2.6e187(레벨 경험치 누적).
	-- P2.5c 결정 1: 21,230 → 25,300 - 점진 감속 천장(CharacterLevelConfig.weaponGrowthSegments) + 환생 · 마일스톤 재설계를 모두 넣은 상위 1%가 2,190시간에 선 최고 스테이지(하네스).
	designMaxStage = 25300,
}
