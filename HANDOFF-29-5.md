# 29-5 인수인계 — 이어서 작업할 세션이 가장 먼저 읽는 문서

> 이어받는 법: 사용자가 "29-5 이어서"라고 하면 **이 문서 → 원래 지시문(아래 [0]) → 남은 일([3])** 순서로 본다.
> 전체 작업이 끝나면 이 파일은 지운다(내용은 PRD 20.80과 최종 보고서로 옮겨 간다).

작성: 2026-09-19 · 세션 사용량 한도로 3번까지만 하고 멈췄다. **보고서는 아직 사용자에게 주지 않았다 - 모든 작업이 끝난 뒤에 준다(사용자 지시).**

## [0] 원래 지시(요약)

29-5 = ① 보스 배치 고정 ② 구출 입력 F 홀드 ③ 수정 여왕(대상 선택) ④ 6종 튜닝 ⑤ 고스테이지 보스 2마리(설계만) ⑥ 탱커 대비(설계 + 훅 4종 구현)
⑦ 무게 시스템(설계만) ⑧ 사람 눈 확인 절차서. 토큰이 부족하면 번호가 높은 것부터 버린다. 구현 항목은 끝난 번호까지 검증·커밋.
공통 규칙: Studio MCP로 인스턴스 편집 금지(Rojo 파일만) · 새 파티클/에셋 금지 · 25-4 색 언어 · 검증은 서버 자동 검증 블록 · Play 전 커밋 ·
Play 중 MCP 호출 금지 · 스탠드인은 실제 Player 경로를 따라가게 · 연출은 판정을 건드리지 않는 쪽부터 · 판단 기준은 PRD 0장 톤.
**세션 중 추가 지시**: "보스와 서로 튕긴 공격을 맞고 탱커가 죽는 것도 게임의 재미로 남겨 두는 게 좋다. 나중에 힐러 부활 스킬도 있을 것" →
⑥의 "탱커가 받는 양의 천장"을 설계할 때 **즉사를 막는 천장을 두지 않는 쪽**으로(55% 상한의 예외로 명시 - 선택한 위험: 탱커가 반사를 켠 것).
보고서 목차: 배치 함수·스테이지 1~60 배치표 / F 홀드 시간 도출 / 보스별 구출 조건 표 / 수정 여왕 판단 / 6종 튜닝 표 / 체감 미확인 수치 목록 /
보스 2마리 설계·수정 필요 목록 / 탱커·반사 설계 / 무게 설계·회피 부등식 처리안 / Play 절차서 / 변경 파일 / 확인 14항목 / 어디서 멈췄는지 / 미결 / 임의 결정.
그 뒤 커밋·푸시하고 PRD를 개정한다(새 절 **20.80** - 코드 주석이 이미 "PRD 20.80 [A]/[B]/[C]"를 가리킨다).

## [1] 끝난 것 (커밋 3개 - 전부 로컬 검증까지, **Play는 아직 한 번도 안 돌렸다**)

| # | 커밋 | 내용 |
|---|---|---|
| 1 | `91535d9` | 보스 배치 고정 |
| 2 | `9246712` | F 홀드 구출 |
| 3 | `f6db013` | 수정 여왕 프리즘 분열 |

로컬 검증(Luau CLI 하네스): 바뀐 파일 전부 `luau-compile` 통과 · 29-2 (가) 41/41 · 29-3 (가) 13/13 · 29-4 (가) 18/18 · **29-5 (가) 6/6** ·
F 홀드 하네스 10/10 · 분열 하네스 8/8 · 모형(`runsim.sh`)에서 수정 여왕만 값이 바뀜(지금 76.65 → 74.00 = 설계, 나머지 5종 동일).
29-3·29-4 (가)의 항목 수가 준 것은 수정 여왕 회귀 줄을 29-5 (가)로 옮겼기 때문이다(29-4가 심해·폭풍을 옮긴 것과 같은 방식).

### [A] 보스 배치 — 결정 사항 (PRD 20.80 [A]로 옮길 것)

- `BossRules.bossIdForStage(stage)` - 인자는 stage 하나. 플레이어·세이브·난수·시각을 읽지 않는다. 표는 `BossData.placement.laps`.
- 표 = **6 × 6 행 완전 라틴 방진**(36마리 = 180스테이지 주기). 1줄 = 선언 순서(5 구간 수호자 · 10 서리 · 15 심해 · 20 수정 · 25 전갈 · 30 폭풍).
  2줄부터 바퀴마다 순서가 다르다: 각 보스가 각 자리에 한 번씩, 이웃 쌍(A 다음 B) 30가지가 주기 안에서 정확히 한 번씩. 같은 보스 재등장 최소 간격 4(바퀴·주기 경계 포함).
  "바퀴마다 다르게"를 고른 근거: 고정 순열 표라 여전히 스테이지만의 함수이고(오류 위험 없음), 같은 흐름의 반복을 피한다. 난수 시드 방식은 버렸다(엔진 버전 의존 · 표가 눈에 안 보임).
- 스테이지 1~60 배치: 5 guardian · 10 frost · 15 abyssal · 20 crystal · 25 scorpion · 30 storm · 35 frost · 40 crystal · 45 guardian · 50 storm · 55 abyssal · 60 scorpion
  (`/gg boss table [끝]`로 출력. 전체 표는 `BossData.placement.laps`.)
- 23-5 순환 코드 삭제: `BossRules.nextRotationBossId/recordRotationHistory`, `PlayerProfile.getBossForStage/clearBossRotationPending/getBossRotationInfo/forceBossRotationNext`,
  `BossEncounter.getRotationOwner`(→ `encounter.owner`), `CombatResolution`의 pending 지우기. **세이브의 `bossRotation` 필드는 그대로 두고 읽지 않는다**(SAVE_VERSION 불변, 이관 없음).
- `/gg boss force`는 세션 메모리(`BossEncounter.setDebugForcedBoss` - 그 플레이어가 주인인 다음 스폰 한 번)로 옮겼다. `/gg boss next|history`는 없애고 `/gg boss table`을 넣었다.
- 소비자 점검: 파티(`spawnForParty`)·솔로(`spawnFor`) 둘 다 `bossIdFor` 하나. 크로스서버는 보스 id를 다루지 않는다(한 서버의 encounter 하나). 견습 = `tutorialBossId` 고정(배치표 첫 칸과 같은지 검사).
  첫 클리어 보상은 스테이지만 본다. 도감·보상 미리보기 UI·건너뛰기·리더보드는 **아직 코드에 없다** - 만들 때 이 함수를 보게 하라고 주석에 적어 둠.
- 기존 세이브 전환: 29-5 (나) "유저 상태와 무관한 보스"가 실제 Player 프로필에 (새 세이브 / 순환 4/6 + pending storm_lord + 강제 frost_giant / 필드 없음) 세 상태를 넣고 실제 스폰 경로로 같은 보스가 나오는지 본다.

### [B] F 홀드 구출 — 결정 사항 (PRD 20.80 [B])

- 입력 = `ProximityPrompt`(서버 `BossTrap.createPrompt` - 잡힌 **실제 Player**의 루트에). PC F · 모바일 화면 버튼 · 게임패드 ButtonX가 기본 UI로 붙는다. 직접 입력 코드 0줄.
- **홀드 1.5초 = 기존 `trap.rescueSeconds`**(새 상수 아님). 도출: 위 = 인시 절약 1/3(20.73 A-2)이 T ≤ 1.5 · 가장 빠른 보스(폭풍: 후딜 0 + 전역 쿨 5초)에서 인지 0.5 + 30stud 걷기 1.9 + T ≤ 5 → T ≤ 2.6.
  아래 = 기존 구출 동작(1.0 ~ 1.5초)보다 싸지 않게. 자동 해제 9초와 6 : 1 유지.
- **둘이 같이 누르면 0.75초**(구출자마다 dt ÷ T를 서버가 틱마다 더한다 - 29-3·29-4의 "둘이면 절반" 그대로).
- **피격 리셋은 "예고가 있는 피격"만**(스킬 attack 판정 = `BossPatterns.applySkillDamage`, %피해 = `BossMechanics.applyGimmickDamage` → `BossTrap.noteSkillHit`). **보스 평타는 끊지 않는다** -
  홀드 중에는 서 있어야 해서 평타는 피할 방법이 없다(막을 수 없는 벌은 벌이 아니다). 평타 0.75초 주기인 전갈·폭풍에서 평타까지 끊으면 1.5초 홀드는 영영 안 끝난다. ← 임의 결정, 보고서에 명시.
- 진행은 서버 권위: `beginHold`(HoldBegan) → 틱마다 거리(종류별 `reachStuds` + 오차 2)·생존·잡힘·멤버 여부를 다시 본다 → 조건이 깨지면 0. `endHold`(HoldEnded/Triggered)는 서버가 센 시간이
  지연 몫 0.35초 안으로 모자랄 때만 완료로 친다(일찍 온 "완료"는 거절). 끊기면 `BossRescueHoldBroken` RemoteEvent로 그 구출자의 클라가 프롬프트를 0.2초 꺼서 홀드를 끊는다.
- 보스별 조건(개성):

  | 보스 | 잡힘 | 어디서·어떻게 |
  |---|---|---|
  | 서리 거인 | 빙결 | 얼음 곁 6stud에서 F 홀드 **+ 얼음을 때려도 같은 진행이 찬다**(3타, 원거리 구출 유지 - 때릴 수 있어 보이는 얼음이 무반응이면 안 된다) |
  | 심해 군주 | 침수 | 곁 6stud 어디서나(가장 너그럽다 - 방전 뒤 7.5초 무판정) |
  | 수정 여왕 | 결정화 | 곁 6stud F 홀드 **또는** 누구든 진짜 여왕을 때리면 결정화 전원 해제 |
  | 전갈 여왕 | 속박 | 무덤(흰 원 r4) 바로 밖 6stud에서 F 홀드 → 홀드가 차는 만큼 **구출자 쪽으로 끌려 나온다**(방향·거리는 처음 당긴 순간 고정, 구출자 앞 2stud까지). 값 = 꼬리 박힘 3초(딜타임)를 어디에 쓸지. "붙어서 걷기"는 없앴다(`towardDot` 삭제) |
  | 폭풍 군주 | 감전 | 몸이 닿는 거리 3stud에서 F 홀드(즉시 → 1.5초). 구출자 비용 0 그대로(경로는 `onComplete`에 남김) |

- 보이는 것: 누르는 사람 = 프롬프트의 원형 진행 + 친구 머리 위 파랑 막대 · 픽토그램 ✋(빙결 ✋ ⚔ · 결정화 ?). 잡힌 사람 = 패널 제목이 "… - 친구가 구하는 중"으로 바뀌고 파랑 막대가 찬다.
  잡힌 본인·자기도 잡힌 사람의 화면에서는 프롬프트를 끈다(보이는 것 = 되는 것 - `BossTrapView`가 로컬로 Enabled를 쓴다).
- 29-3 (나) 속박·29-4 (나) 침수·감전 구출 구역을 F 홀드 경로로 고쳤다(기대값: 곁에 서 있기만 하면 0 → 홀드 90틱).

### [C] 수정 여왕 — 결정 사항 (PRD 20.80 [C])

- 분신 = `MonsterSpawner.spawnRescueTarget`의 `lookLike`(보스 인스턴스 데이터) - 체격·색·부착물·이름·HP바(진짜의 비율을 그대로)·게이트 표식(◈ - `GateArmed` Attribute를 분신에도)까지 같다.
  **말풍선은 뺐다(`bubble = "none"`)**: 말풍선은 클라가 "보스"(RescueTarget 태그가 없는 가장 가까운 Monster) 머리 위에만 띄운다 - 띄우면 그것이 진짜를 가리킨다.
- 구분 = **움직임**: 넷 다 빨강 원 r6, 진짜의 원에서만 흰 원이 8초 동안 자란다(낙석 카운트다운과 같은 문법 - 색약에서도 같다. 밝기로도 갈린다). 새 클라 모듈 `BossSplitView`(파트 ≤ 6).
- **분신·원·카운트다운이 같은 틱**에 나온다(20.79 [J]의 시작 확인 - "분열 시점에 식별 수단이 있는가"). 28-2의 "분열 1.0초 뒤 표시"는 없앴다(가릴 단서 없이 때릴 수 있는 1초가 생긴다).
- 자리 = 분열 중심(보스의 지금 자리, 네 자리 + 원이 아레나 안에 들어오게 자름)의 네 방위 20stud. 28-2의 "아레나 중심으로 순간이동"은 버렸다(귀퉁이의 플레이어는 8초 안에 못 닿는다 -
  29-3·29-4와 같은 종류의 구멍). **발동 조건 `memberWithin 40`**(신규 조건 - 한 명이라도 40stud 안) → 회피 거리 60 → 5.19초 ≤ 8초. 모형은 이 조건을 참으로 본다(전술 조건이 아니라 도달 가능성 안전선).
- 벌: 분신을 때리면 그 분신이 깨지고 **때린 사람만** 18.3% + 결정화. 8초 초과 = 파편 폭풍 55%(**잡힘 없음** - `failTraps = false`). 둘 다 `applyGimmickDamage`의 발동당 1인 상한을 같이 써서 **한 번의 분열 합계 ≤ 55%**(셋 다 틀린 사람에게 폭풍은 0).
- 막을 수 없는 벌 면제(갑각 반사와 같은 `hitInfo`): ① 분열 **전에** 시작한 공격(날아가던 화살·돌던 채널)은 분신을 깨지도 벌하지도 않고 진짜에 닿아도 풀어 주지 않는다 ② 꽂힌 화살의 지연 폭발(indirect) 무시
  (속사 화살은 애초에 구출 대상에 안 꽂힌다 - `AttackServer`) ③ 같은 틱에 진짜도 같이 때린 광역기는 벌 없음(벌은 `task.defer`로 그 틱이 끝난 뒤에 매긴다) ④ 벌은 0.75초(`reflect.windowSeconds`)에 한 번. 지속 피해는 게임에 없다.
- 성공 = 진짜를 때린 그 틱에 판정 → 분신 소멸 · 결정화 전원 해제 · 기절 4초(헤롱 자세 + 파랑 말풍선, 받는 피해 ×1.3). 성공은 파티 단위.
- 서버 부하: 분신 3체 × (파트 6 + GUI) 생성/제거가 20초에 한 번. 풀링은 안 했다(12 보스전 × 3체 = 상시 아님) - 29-5 (나)가 세우는 시간을 ms로 찍는다(기준 < 50ms/회). AI 루프는 `isRescueTarget`이라 건너뛴다.
  분신 목록은 st가 아니라 `decoysOf[model]` - 보스가 처치돼 st가 비워져도 `BossPatterns.clearProps`(처치·이탈·전멸 리셋·중단 전부 지나는 곳)가 치운다.
- 힌트: 1단계 = 진짜의 자리 위 흰 ▼ · 2단계 = 제한 8 → 12초.
- **아직 안 한 것**: 20.79 [J]의 "반사 광선의 꺾임"(벽 수정 kit + 꺾인 예고 띠). 기믹이 아니라 일반 스킬 `beam`의 연출·판정 확장이라 이번 범위에서 뺐다 → 미결로 보고.

## [2] 검증 상태 — **Play 미실행**

DevTools가 29-1 → 29-2 → 29-3 → 29-4 → **29-5 (가)(나)** 순으로 돈다(`BossGimmick5Verify.lua`). 첫 Play에서 볼 것:
- 29-4 (나) **19/19**(지난 세션에서 읽는 시점만 고쳤던 것 - 미확인) · 29-3 (나) · 29-2 (나) 회귀.
- 29-5 (나): 유저 상태와 무관한 보스 / 파티·첫 보스·강제 지정 / F 홀드 프롬프트(5종 - 실제 인스턴스의 HoldDuration·KeyCode·거리·RequiresLineOfSight·ClickablePrompt·Style) /
  F 홀드 진행·피격 리셋(평타는 안 끊김 · 낙빙에 맞으면 0) / 둘이 절반 · 실제 Player가 누르는 쪽 / 프리즘 분열 5줄 / 고아 보스 0.
- (나)의 코드는 로컬에서 실행해 보지 못했다(실제 Instance가 필요). 에러가 나면 대개 스탠드인·읽는 시점 문제다(29-4의 교훈) - 제품 코드보다 검증 코드를 먼저 의심.
- 위험이 큰 곳: `prompt.Style == Enum.ProximityPromptStyle.Default` 비교 · `decoy.Body.Size == model.Body.Size`(분신은 같은 data로 짓지만 범위 배율 사본과 무관한지) ·
  분열 구역의 `task.wait()` 뒤 defer 순서 · `BossTrap.release(player, "debug")` 뒤 유예 면역(디버그 해제는 유예를 안 준다 - 코드상 "auto"/"rescued"만).

## [3] 남은 일 (순서대로)

1. **④ 6종 튜닝**: `runsim.sh`(스크래치패드) 또는 `/gg boss sim`. 6종 몬테카를로 - 파훼 후 솔로 ±10%, 초회 1.8 ~ 2.7배. 지금 값(200회): 서리 +(-0.1)% ×2.71 · 심해 +5.2% ×2.69 · 수정 +1.0% ×2.62 · 전갈 +1.6% ×2.70 · 폭풍 −5.6% ×2.51, 4인 폭풍 −8.0%.
   **서리 솔로 ×2.71이 상한 2.7을 넘는다**(200회 기준 - 29-4 때 100회로는 2.70). 위쪽 끝 셋(서리·전갈·심해)을 어떻게 내릴지 정해야 한다 - 후보: 파훼 전 시간을 줄이는 쪽(게이트 0.487은 유도값이라 못 건드린다 →
   기회 창이 아니라 **실패 시 잡힘 9초가 파훼 전 시간의 큰 몫**) 또는 허용 구간의 정의를 몬테카를로 평균으로 고정. 6종 회피 부등식·2연타 105쌍·첫 기믹 10초 재확인(전부 `runsim.sh`가 찍는다).
   20.75 ~ 20.79 + 29-5의 "첫 값"(체감 미확인 수치) 표: 20.78 [6] 3번(끌림 6 · 바닥 9.17% · 넉백 5/8) · 20.79 [K] 5번(회오리 6/1.5초/0.75바퀴/3 · 이른 창 1초 · 흔들림 1초 · 수면 1.5) · 20.76 미결(유사 웅덩이) ·
   환산 계수 1.259 · 29-5(홀드 1.5초 · 지연 몫 0.35 · 거리 6/6/6/3 · standoff 2 · 분열 반경 20 · 원 r6 · 제한 8초 · memberWithin 40 · 기절 ×1.3) 등.
2. **⑥ 탱커 훅 4종(구현 - 비어 있어도 자리는 있어야)**: 보스 스킬마다 `reflectable` 플래그(BossData 스킬 필드 - 기본값 규칙을 정해 데이터에) · 도발 인터럽트 진입점(`BossPatterns.onTaunt(model, data, player)` - 스케줄러가 호출받을 자리,
   지금은 no-op + 로그) · 넉백·띄우기의 무게 계수 입력(`runHitEffects`의 launch payload에 `weightFactor = 1.0` - 클라 `BossStormView.launch`가 높이·체공에 곱할 자리) · 반사 누적 버퍼 자료구조(`BossMechanics`에
   `reflectBuffer = { [model] = { total, byPlayer, bounces } }` + 접근 함수, 아무도 안 채움). 29-5 (가)에 "훅 4종 존재(비어 있음)" 검사 추가.
3. **설계 문서(PRD 20.80 [E] ~ [G])**: ⑤ 보스 2마리(인카운터가 한 마리를 전제한 곳 목록 - `encounter.model` 단수 · `encounterByModel` · `BossPatterns` st 1개 · `nearestLivingMember` · 게이트/힌트/`decoysOf`/kit 슬롯 1개 ·
   `CombatResolution.handleBossDeath`가 처치 = 종료로 봄 · 전역 쿨이 보스별 / 쌍을 배치 함수에 넣는 법 / 시너지 3개 이상 / 공유 전역 쿨 / 처치 시간 앵커) · ⑥ 탱커·반사(튕김 상한 · 천장 없음(사용자 추가 지시) ·
   좋은/훌륭한/엄청난 기준값 3개 · 띄운 보스의 높이차 판정 · 도발 뒤 반격 규칙) · ⑦ 무게(회피 부등식 처리안: 16 바닥 vs 기준 재설정 · 골드/가방 무게가 스트레스가 안 되게 · UI 위치).
4. **⑧ Play 절차서**: 20.76 [H] · 20.78 [5] · 20.79 [H]의 미확인 항목 + 29-5(F 홀드 체감 · 진짜/가짜 구분)를 `/gg boss force <id>` 순서 하나로 묶는다. 첫 Play에서 29-4 (나) 19/19 확인 포함.
5. **Play**(사용자 또는 MCP `start_stop_play` - Play 중에는 MCP를 부르지 않는다) → 결과로 X 수정 → 재커밋.
6. **PRD 20.80 작성** + 기존 절 개정 표시(20.50 [5] · 20.60 [2] 순환 → 폐기 / 20.73 [1-3] · [2-4] · [2-8] A-2·A-4 / 20.75 [C] split 줄 / 20.79 [J]) + 헤더 상태줄. 문서 전용 규칙(CLAUDE.md)대로 수정한 절만 확인.
7. **최종 보고서**(위 [0]의 목차) → 커밋·푸시 → 이 파일 삭제.

## [4] 로컬 하네스 (Play 없이 도는 것)

스크래치패드: `%LOCALAPPDATA%\Temp\claude\C--Users-xkrgh-vibe-game\39cbf3cd-543b-4720-a8f8-cd4a1b0ba539\scratchpad`
(없어졌으면 메모리 `reference_local_luau_harness` 절차대로 다시 만든다). 이번 세션에 더한 것:
- `prelude.luau`에 **ProximityPrompt 스텁**(서버가 연결한 콜백을 `prompts[n].handlers`에 잡아 둔다 → 테스트가 HoldBegan/HoldEnded/Triggered를 쏜다).
- `run_hold.sh`(F 홀드 10시나리오) · `run_split.sh`(분열 8시나리오 - `task.defer`를 실제로 미루는 스텁 포함) · `run_verify295.sh`(29-5 (가) - 실제 `BossRules`를 올린다, Loot 스텁 필요) ·
  `runsim.sh`(6종 모형·회피 부등식·인접 쌍·몬테카를로) · `baseline_sim.txt`(세션 시작 시점) · `src_base/`(세션 시작 시점의 src 스냅샷 - 회귀 diff용) · `placement.py`(배치표 탐색).
- 옛 시나리오 하네스(`run_frost/scorpion/flood/storm.sh`)는 에러 없이 돌지만 **구출 시나리오의 기대값은 옛 방식**(걷기·머물기·닿기)이라 그 줄은 무시한다.

## [5] 임의 결정(지금까지) — 보고서에 그대로 옮길 것

1. 바퀴마다 순서가 다른 고정 표(행 완전 라틴 방진), 1줄 = 선언 순서. 2. `bossRotation` 필드는 남기고 SAVE_VERSION 불변. 3. `/gg boss force`는 세션 메모리 1회용.
4. 홀드 시간 = `trap.rescueSeconds` 1.5초. 5. 평타는 홀드를 끊지 않는다. 6. 빙결은 얼음 때리기를 병행 유지. 7. 속박 = 걷기 폐지 → 끌어내기(구출자 쪽, standoff 2, reach 4 → 6).
8. 감전 = 즉시 → 1.5초 홀드(입력 통일의 값). 9. 결정화 = F 홀드와 "진짜 때리기" 병행. 10. 분열은 아레나 중심이 아니라 보스의 지금 자리 + `memberWithin 40`. 11. 분열 1초 지연 표시 삭제(같은 틱).
12. 분열에 말풍선 없음. 13. 파편 폭풍은 잡지 않는다. 14. 분열 전 공격은 진짜에 닿아도 풀어 주지 않는다(벌 면제와 대칭). 15. 분신 풀링 안 함. 16. 반사 광선의 꺾임은 이번에 안 함.
