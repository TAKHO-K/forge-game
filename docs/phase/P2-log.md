# P2 로그 - 경제 · 성장 · 보석 · 태초 · 치유사 구현 + 시뮬 재검증

자율 진행 단계(COMMON.md §7). 사용자 확정 결정(P1): 1A · 2B · 3A · 4B · 5A · 6B · 7A · 8A · 9A · 10A · 13B. 그 밖의 밸런스 · 규칙은 건드리지 않는다.

## 가정·결정 로그

(사소한 판단 = 직접 정하고 한 줄씩. 번호는 기록 순서.)

1. [A1] 프로필 하루 플레이 시간만 바꿨다: 캐주얼 0.5 → **1**시간 · 일반 1.5 → **3**시간 · 상위 1% 6 → **12**시간. 나머지 가정(직업 · 구역 · 목표 처치 초 · 생존 타수 · 효율 · 보스 · 강화 목표 · 점검 간격 · 보석 변환)은 P0 값 그대로. `hoursPerDay`는 EconSimReport의 달력 일수 계산에만 쓰인다(누적 플레이 시간 결과는 안 바뀐다 - grep으로 확인).
2. [A2] "변경 전 코드 기준" 실행은 HEAD(`05cdc83`) 사본(git worktree, 스크래치패드)에 A1 설정 파일만 넣고 **로컬 Luau 하네스**(P0 파트2와 같은 스텁 · 같은 실제 모듈)로 돌렸다 - 게임 코드를 고치기 전 상태를 Studio Play 없이 정확히 재현하려고. 하네스 결과는 P0 Studio 실행(`E1-baseline.md`)과 이정표 · 최고 스테이지 · 표본 9/9가 전부 같고, 난수를 쓰는 강화 시도 횟수만 다르다(캐주얼 19 → 21회 - 하네스의 `Random` 스텁이 로블록스와 다른 수열). 파일 = `docs/econ/P2-before.md` + CSV 9개(`_extract.py --stem P2-before`). 추출 스크립트에 `--stem` 옵션을 더했다(로그 파일 대신 하네스 출력도 읽는다).
3. [B0 · 코드 사실] 환생 조건 = `PlayerProfile.rebirth`: 활성 직업의 레벨 ≥ 25 × (회차 + 1)(P2 전 - 서버 · 환생 UI(`RebirthView.requiredLevel`) 두 곳에 같은 식이 박혀 있었다), 최대 5회(`GemData.maxRebirthCount`), 스테이지 조건 없음. 추가로 `RebirthAccess`가 제단 반경 · 보스전 · 강화 중을 막는다. **초기화되는 것**: 경험치(→ 레벨 1) · 현재 무한 스테이지(→ 1). **유지**: 최고 스테이지(`infiniteBest`) · 깬 보스 기록(→ 보스 게이트 재통과 불필요, best + 1까지 바로 이동 가능) · 골드 · 장비(갑옷 · 장갑 · 신발) · 가방 · 무기 강화 단계 · 재료 · 방지권 · 보석. **보상**: 무기 등급 = 회차(등급 배율 ×1.0 · 1.4 · 2.0 · 3.0 · 4.5 · 8.0 · 15.0 - 일반 ~ 태초) · 보석 칸 k 개방 + 그 칸 상한 등급 보석 1개 확정 지급(itemLevel = 환생 순간 레벨) · 5회 + 5칸이면 무기 등급 6(태초).
4. [B0 · 코드 사실] 레벨 경험치 = round(K(L) × E(L)) - K(L) = 목표 처치 수(앵커 1:5 · 25:10 · 50:15 · 75:20 · 125:25 선형, 이후 25) · E(L) = tier1이 스테이지 L에서 주는 경험치 = floor(80 × 0.025 × 1.155^(L−1)). 곡선은 회차와 무관하게 하나. 스테이지 s 몬스터(tier t): HP = hp_t × 1.155^(s−1) · 공격 = atk_t × 1.155^(s−1) · 골드 = floor(gold_t × 1.155^(s−1)) · 경험치 = floor(hp_t × 0.025 × 1.155^(s−1)) · hp_t = 80 r_t² · atk_t = 8 r_t · gold_t = 6 r_t²(r_t = tier 기대 장비 가치 비, MonsterData). 스테이지 1 ~ 100에 따로 꺾이는 구간 없음(k 하나). 보스 = 5스테이지마다(`BossData.stageInterval`).
5. [B · 진단] 변경 전 일반 프로필 환생 시각(누적) = 0.11 · 0.70 · 4.49 · 16.45 · **31.73**시간. 느린 구간은 3 ~ 5회차의 "사냥 스테이지보다 레벨이 수십 칸 위" 구간이다 - 사냥 스테이지는 방어구 itemLevel 사다리(결정 3A 유지)와 보스에 묶여 34 ~ 65 근처인데 필요 레벨이 75 · 100 · 125라, 레벨당 필요 경험치(스테이지 L 기준)가 1.155^(격차)배로 커진다.
6. [B · 레버 실험] 하네스로 두 레버를 비교했다(일반 프로필): ① 환생 회차별 경험치 배수(예: ×1 · 3 · 10 · 30 · 100 → 5회차 13.2시간) ② 환생 필요 레벨표. **② 채택** - ①은 마지막 환생(5회) 뒤 배수가 1로 돌아가야 후반 곡선(1A)이 안 바뀌는데, 그러면 "환생했더니 경험치가 줄었다"는 역보상이 생긴다. 두 레버 모두 스테이지 도달 시간(10 · 50 · 100)은 거의 안 바꾼다(스테이지는 방어구 사다리 · 보스가 막는다).
7. [B · 값] 필요 레벨표 = **25 · 50 · 65 · 80 · 92**(1 · 2회차 그대로, 3 ~ 5회차만 75 → 65 · 100 → 80 · 125 → 92). 격자(5단위) 결과: 25-50-65-80-**90** = 11.31시간 · **92** = 12.11시간 · 93 = 12.82시간 · 95 = 13.81시간 → 12시간에 가장 가까운 92. 표는 `CharacterLevelConfig.rebirth.requiredLevels` 한 곳(서버 `PlayerProfile.rebirth` · 클라 `RebirthView.requiredLevel` · EconSim이 `CharacterLevel.getRebirthRequiredLevel`로 읽는다). 환생 지급 보석 itemLevel = 그 회차 필요 레벨(전: 25 × 회차 = 그때의 필요 레벨과 같은 값이었다).
8. [B · 결과] 일반: 환생 1 ~ 5회 = 0.11 · 0.70 · 2.01 · 6.47 · **12.11**시간(목표 약 12시간 O) · 스테이지 10 = **0.01시간**(목표 30분 O - 변경 전에도 0.01시간: 스테이지 1에서 tier5를 3초 안에 잡는다) · 스테이지 100 = **34.47시간**(변경 전과 같음 - 결과 보고). 캐주얼 5회차 40.44 → 21.57시간 · 상위 1% 7.95 → 4.01시간. 스테이지 곡선(10 · 50 · 100 · 250)은 세 프로필 모두 전과 같거나 ±0.7시간 - 후반(1A)에 붙는 접합부에 급변 없음(레벨 곡선 · 몬스터 곡선을 안 건드렸다). B3(두 안 비교)는 목표를 동시에 맞춰 해당 없음.
9. [B · 보고] 환생 UI 두 곳(`RebirthView` · `RebirthAltar`)의 "경험치 배수 ×(회차+1) → ×(회차+2)" 문구는 25-1에서 없앤 옛 회차 배수 설명이 남은 것이다(범위 밖 - 고치지 않았다).
10. [C1 · 구조] 새 모듈 `shared/GoldCost.lua`(공식) + `shared/data/GoldCostConfig.lua`(비용 종류별 기준 스테이지). `GoldCost.cost(기본 비용, 스테이지, 종류)` = floor(기본 × k^max(0, 스테이지 − 기준)) - k = 몬스터 골드와 같은 `InfiniteStage.getMultiplier`. 종류: `enhance`(기준 100) · `rerollTicket`(기준 1) · `protection`(기준 1). 호출부: `Enhance.getCost(level, 계정 최고)`(서버 `EnhanceService` · 클라 `Enhance/Controller`는 Attribute `AccountBestStage`) · `Enhance.getProtectionPrice` · 변환권 `GemServer.rerollTicketPrice` · 클라 `GemWorkshop.ticketPrice` · EconSim. 새 소비처(펫)는 Config에 종류 한 줄 + 같은 함수 호출.
11. [C1 · 스테이지 기준] 강화 비용의 스테이지 = **계정 최고 스테이지**(변환권 · 방지권과 같은 규칙 - 스테이지 1로 내려가 싸게 강화하는 구멍 방지). 기준 100 = "스테이지 100 시점 구매력 유지"라는 지시의 앵커 - 스테이지 100까지는 P2 전 고정표 그대로(초반 경제 불변). 변환권 · 방지권은 기준 1이라 값이 P2 전과 한 자리도 안 바뀐다(floor(6 × k^(s−1)) = `InfiniteStage.getGoldReward`).
12. [C1 · 결과] 구매력(골드/분 ÷ +20→+21 1회 비용, 그 이정표에 처음 선 레벨의 청크) - 일반: 스테이지 100 = 626 · 250 ~ 1750 = 632 ~ 633(**+1%**) · 2000 = 435(−31%) · 2250 = 415 · 2500 = 176(−72%) · 3000 ~ 3450 = 251(−60%). 캐주얼 100 = 266 · 250 ~ 1250 = 276 ~ 277 · 1500 = 293 · 2000 = 72. 상위 1% 100 = 313 · 250 ~ 1500 = 353(+13%) · 2000 = 607 · 3000 = 455. **±20%는 스테이지 100 ~ 1750(일반)에서만 성립** - 2000 이후 떨어지는 까닭은 처치 시간이 0.67초(생존 타수가 사냥 스테이지를 묶는 구간) → 2.9초(g 1.15 < k 1.155로 딜이 모자라는 구간, 결정 1A가 유지하는 천장)로 길어져 골드/분이 k보다 느리게 크기 때문이다. 스테이지만 보는 비용식으로는 못 맞춘다 → "결정 필요" 1.
13. [C1 · 옛 검증] S04(나) `[13]`의 "차감 = 235,000" 고정 비교를 `Enhance.getCost(19, 계정 최고)`(표 235,000 × GoldCost)로 바꾸고, 강화 (나) 블록 셋(`EnhanceVerify` 두 곳 · `EnhanceEffectVerify` 한 곳)이 넣어 주는 준비 골드에 같은 배수를 곱했다(개발 계정 최고 스테이지가 100을 넘으면 옛 준비 골드로는 모자란다). 판정식 자체는 그대로.
14. [C2] 상대 정밀도: 세 프로필 모든 청크에서 "처치 1회 골드 ÷ 보유 골드"의 최솟값 = 1.1e-6 ~ 2.1e-6(상위 1% 스테이지 3900 · 보유 9.6e250 · 처치 1.1e245). double의 상대 정밀도 2^-53 ≈ 1.1e-16보다 10자리 위라 **작은 획득이 사라지는 일 없음 - 보정 안 함**. 획득이 사라지려면 보유액 대비 1e-16 미만, 즉 자기 최고보다 약 250스테이지 아래에서 사냥해야 한다(그 골드는 표시 자릿수로도 안 보이는 값). 처치 팝업은 획득액 자체를 NumberFormat으로 보여 준다.
15. [C3] NumberFormat은 이미 K · M · B · T 뒤에 aa ~ dz 두 글자 단위(+ 세 글자 이상)가 있어 1.797e308까지 표시한다(새 단위 추가 불필요). 표본 28개로 확인하다 **알파벳 구간의 부동소수 결함**을 찾았다: 1000으로 수십 번 나누며 오차가 쌓여 1e45 → "999.9aj"(정답 "1ak") · 1e60 → "999.9ao" · 1e300 → "999.9dq" · 1e308 → "99.9dt"(정답 "100dt"). 고침: 1e15 이상만 단위 칸을 먼저 정하고 한 번만 나눈 뒤 상대 1e-9 여유로 버림(+ 1000.0이 되면 다음 단위). **1e15 미만(K · M · B · T)은 옛 코드 경로 그대로** - 무작위 20만 개 표본에서 옛 식과 차이 0 · 알파벳 구간 경계 1,176개(10^15 ~ 10^308의 1 · 2 · 5 · 9.99배) 전부 정답.
16. [C · 보고] 서버 로그 몇 곳(`GemServer` 변환권 구매 · `ProtectionTickets` · `EnhanceService` 강화 결과 · `SaveCoordinator`)이 골드를 `%d`로 찍는다 - 9.2e18을 넘으면 Luau가 오류 없이 -9223372036854775808을 찍는다(표시만 틀림, 로그 전용 - 범위 밖).
17. [D1 · 식] `Option.levelFactor(L)` = 옛 식(선형, 기준 100에서 1.0) × (L > 125이면 1 + p · log2(L / 125)). p = `OptionData.levelLogSlope` = **0.035**. 125에서 값이 이어진다. f(125) = 1.2161 · f(250) = 1.2587 · f(4738) = 1.4394(+18.4%). 장비 옵션도 같은 함수를 쓴다(20.67 "장비 옵션 1개 ≙ 보석 1개").
18. [D1 · 비중의 뜻 - 해석] "보석이 전체 전투력에서 차지하는 비중"을 **보석 위력 합(보석 공격력% 합 - P0 E4의 "위력 합" 열, 옛 일반 45.8%)**으로 읽었다 - 요청 범위 40 ~ 55%가 P0 값 45.8%를 가운데에 둔 구간이라서. 문자 그대로의 뜻(공격력 중 보석이 만든 몫 = 1 − 보석 없는 공격력 ÷ 공격력)으로는 레벨 125에서 9.5%(장갑 공격력%가 크다)라 p와 무관하게 40 ~ 55%가 불가능하다 → 두 값을 모두 보고하고 해석은 "결정 필요" 2.
19. [D1 · p 선택] 일반 프로필 격자(보석 위력 합 · 공격력 중 보석 몫, 최고 스테이지 이정표에서): p 0.03 = 41.4 ~ 52.4% · **0.035** · 0.04 = 41.4 ~ 54.6% · 0.05 = 41.4 ~ 56.8%(3000에서 56.3% - 초과). 4738 외삽(5칸 모두 Lv4738 · 일반 보석 구성): p 0.035 = 54.2% · 0.04 = 55.4%(초과) → **0.035**. 스테이지 125 시점 41.4%(옛 45.8%보다 낮은 이유: 환생 5회차 지급 보석 itemLevel이 125 → 92, 파트 B). 문자 그대로의 몫은 같은 구간 9.5 ~ 12.1%.
20. [D2] 레벨 1 ~ 4738 전 구간 × 옵션 16종 × 등급 7종 × 최대 롤(1.125): inf · NaN 0 · levelFactor 단조 증가 · 최대 옵션 값 0.486(태초 위력 Lv4738). 상한 없는 축(위력 · 신속 · 치명 · 흡혈 · 직업 스킬)은 곡선을 따라 커지고 상한 있는 축(건강 · 방어 · 성장 · 재생 · 쿨다운류)은 합산 상한에서 멈춘다. `/gg option table`의 "동결 확인" 출력 줄을 새 곡선 표시로 바꿨다(검증 판정 아님).
21. [E1 · 구조] 드랍표 단일 소스 = `shared/data/DropTableData.lua`(tier별 장비 등급표 · 태초 확률 · 레벨 감쇠) + `shared/DropTable.lua`(순수 함수: `effectiveRate(플레이어, 몬스터, 사냥 스테이지)` · `levelDecay` · `primordialBaseRate` · `gradeChance` · `gradeRow`). 옛 `MonsterData.dropGradeTableByTier`는 그 표를 가리키는 별칭(값 그대로 - tier 공정성 r(t) · 몬스터 HP · 골드 · 경험치 한 자리도 안 바뀜). 호출부: 서버 굴림 `CombatResolution` → `Loot.rollArmorDrop(…, primordialRate)` · 조회 API `server/DropTableQuery.lua`(`describe` 순수 · `forPlayer`) + `DropTableServer.server.lua`(RemoteFunction `DropTableQuery(tier)` - 인자 검사 · 0.2초 간격 제한, UI 없음) · 판매가 `Loot.getSellPrice`(tier1 ~ 5 태초에도 가격이 생긴다) · EconSim(가방 후보 · 태초 공급 · E5).
22. [E1 · 굴림 방식] tier1 ~ 5 태초는 기본 등급표에 넣지 않고 **아이템마다 먼저 따로 굴린다**(확률 = effectiveRate) - 안 걸리면 기본 표에서 태초를 뺀 나머지 비율로 굴린다. tier6 · 감쇠 없음이면 분포가 P2 전과 정확히 같다(0.1% + 나머지 × 0.999/0.999). 기본 표에 넣었다면 r(t)가 바뀌어 모든 tier의 몬스터 수치가 흔들렸다. 보스(첫 처치 · 재도전) · 반짝이 · 견습 드랍은 이 굴림을 안 탄다(결정 9A · 범위 밖).
23. [E3 · 해석] 레벨 감쇠 = 격차(본인 **활성 직업** 최고 스테이지 − 사냥 스테이지) ≥ 5이면 (격차 − 4) × 10%를 깎는다(격차 5 → −10% · 14 이상 → 0). "5 이상 낮으면, 초과 1레벨당"을 "5칸째부터 한 칸마다"로 읽었다(격차 5에서 바로 효과). 계정 최고가 아니라 활성 직업 최고를 쓴 이유: 다른 직업의 기록 때문에 새 직업의 태초가 깎이지 않게. 값은 `levelDecay = { startGap = 5, perLevel = 0.10 }`.
24. [E4] 태초 장비의 itemLevel = 그 몬스터를 잡은 사냥 스테이지(= 받는 사람의 스테이지 = `TutorialState.getMonsterStage` - "몬스터 레벨"). 옛 방식은 모든 등급이 `max(1, 스테이지 + δ)`(δ = −2 ~ +2 가중치 표 `ArmorData.itemLevelDelta`) - 태초만 δ를 빼 "보석 레벨 = 몬스터 레벨"을 정확히 맞췄다. 분해하면 itemLevel이 그대로 보석으로 간다(`dismantleItem`, 26-1). 다른 등급은 δ 그대로.
25. [E2 · 근거] 손익분기 M*(드래곤 ÷ tier t 확률 배수가 이 값 이하면 "낮은 스테이지 드래곤"이 우세하지 않다), Δ ≤ 5 · 플레이어 레벨 10 · 150 · 1000 최솟값(감쇠 · 새 보석 곡선 포함): tier1 **1.94** · tier2 **1.71** · tier3 **1.39** · tier4 **0.94** · tier5 **0.71**. 드래곤과 HP 차이가 작은 tier4 · 5는 1 미만 - 드래곤보다 확률이 낮은 한 Δ 4 ~ 5의 드래곤이 항상 이긴다(스테이지 5칸 = HP ×2.06, 감쇠는 Δ5에서 −10%뿐). 요청 범위 "드래곤의 약 1/2 ~ 1/3"(배수 2 ~ 3)은 tier1조차 M*를 넘는다.
26. [E2 · 값] E5 조건을 우선했다: 배수(드래곤 ÷ tier) = tier1 **1.9** · tier2 **1.7** · tier3 **1.35** · tier4 **1.1** · tier5 **1.05** · tier6 1(확률 0.0526% · 0.0588% · 0.0741% · 0.0909% · 0.0952% · 0.1%). tier1 ~ 3은 M* 아래(Δ ≤ 5 지배 전략 없음 O), tier4 · 5는 "tier가 낮을수록 낮게"를 지키는 가장 작은 값(1에 가깝게)으로 두어 우세 폭을 줄였다(레벨 1000 A÷B 최대 tier4 1.17 · tier5 1.49) → "결정 필요" 3. 데이터 표기 = `dragonOverTier`(드래곤 확률 ÷ 이 tier 확률).
27. [E5 · 공급] 잡몹 태초 장비 기대 개수/시간(진행 시뮬 전체 · 스테이지 100까지): 캐주얼 0 → 0.091 · 0.396 / 일반 0 → 0.163 · 0.503 / 상위 1% 1.306 → **0.074**(×0.057) · 1.813 → 0.275. 캐주얼 · 일반은 tier5 이하만 돌아 전에는 0이었다. 상위 1%가 크게 **줄어드는** 이유 = 레벨 감쇠: 시뮬의 모든 프로필은 최고 스테이지(= 아직 못 깬 첫 보스)보다 평균 9.8 · 12.1 · 14.6칸 아래에서 사냥한다(방어구 생존 사다리 + 보스 게이트) → 평소 사냥에도 감쇠가 −60% ~ −100% 걸린다. 감쇠 없이 재면 일반 0.77 · 상위 1% 1.32/시간(전과 비슷). → "결정 필요" 4. 태초 덕에 20,000시간 최고 스테이지가 캐주얼 3000 → 3450 · 일반 3460 → 3665로 올랐다(상위 1%는 3910 → 3895).
28. [E · 도구] P0 what-if `unfreeze` · `cap500`(levelFactor 동결 연장) · `primFlat`(E5 확률표)과 `EconSim.levelFactorScale` · `optionFreezeLevel`은 D1 · E로 대상이 없어져 지웠다. E5는 게임 드랍표(effectiveRate)를 그대로 쓰는 tier 1 ~ 5 비교로 바꿨다. 새 what-if 칸: `rebirthRequiredLevels` · `optionLevelLogSlope` · `enhanceGoldAnchor` · `primordialDragonOverTier` · `primordialDecayPerLevel` · `healerAtk` · `dealingAttackMultiplier` + 이 칸들로 P2 전 값을 되살리는 `p2before`(비교표의 "전" 칸을 새 도구로 다시 잰다).
29. [F1 · 코드 사실] 게임은 **이미** 치유모드 공격에 딜 옵션을 100% 넣는다: `AttackServer.server.lua` 179행 `PlayerCombat.getAttack(…, PlayerProfile.getAttackPercentBonus(player))`(위력%) · 206행 `PlayerProfile.getCritBonus`(치명 확률 · 치명 피해) · 119행 `getAttackCooldown(classId, PlayerProfile.getSpeedPercentBonus(player), …)`(신속%) - 직업 · 딜링모드와 무관하고, 딜링모드는 187행에서 배율만 곱한다. 치유사의 피해는 평타뿐이다(Q = 치유, E = 토글). 지시가 가리킨 `HealCast.lua` 39-40 · 47-48 · 56행은 **치유의** 치명 굴림(`rollCrit` - 옵션 치명 확률을 안 읽는다)과 쉴드 판정이고, `SkillServer` 490행은 스킬 atk(위력% 포함)다. P0 E6의 "치유모드 옵션 미적용"(P0-log 20)은 시뮬 모형의 전제였을 뿐이다 → **게임 코드 변경 없음, EconSim E6 모형을 게임과 같게 고쳤다**(치유모드 딜 = 딜링모드를 끈 평타 · 옵션 100%). 치유 치명 굴림에 옵션 치명을 넣을지는 치유량 규칙이라 "결정 필요" 5.
30. [F · 모형 [가정]] 보스전 치유모드 치유사의 전투 시간 비율 = 1.0(`EconSimConfig.healer.healModeFightRatio`) - 치유모드는 소모가 없어 딜러(모형에서 가동률 1)처럼 계속 싸운다고 봤다. PartyShieldSim의 이탈 기계(0.2658)는 딜링모드 소모를 전제로 한 값이라 치유모드에 안 맞는다. 민감도: 이 비율을 0.5로 보면 F2 목표 atk가 두 배(≈1.32)가 되어 F3(딜링모드 < 치유모드)와 부딪힌다 - 이 가정이 F2 · F3를 동시에 성립시키는 전제다.
31. [F2 · 값] `ClassData.classes.healer.atk` **0.6 → 0.66**("치유모드 기본 딜 계수" - 치유모드 딜이 이 값에 비례). 풀이: "도적 3 + 치유사 1" 세 장비 단계 모두 비중 ≥ 12%인 최소 atk = 0.6580 → 0.66. 결과 비중(없음 · 평균 · 상위): 도적 3 = 12.03 · 12.14 · 12.34% · 검사 3 = 15.29 · 15.51 · 16.07% · 궁수 3 = 12.34 · 12.25 · 12.07% · 혼합(각 1) = 13.07 · 13.13 · 13.27% - 전부 ≥ 12%. (P0 모형으로는 9.6% · 7.6%로 떨어지던 것은 옵션 미적용 전제 때문이었다.)
32. [F3 · 값] `SkillData.healer.E.attackMultiplier` **3.13 → 1.615**. 풀이: atk 0.66에서 딜링모드 60초 로테이션 = 검사 × 0.875(장비 없음)가 되는 배율 1.6154 → 1.615. 결과 딜링모드 ÷ 검사 = 0.8748(없음) · 0.8896(평균) · 0.9276(상위 - 치유사는 모든 피해가 평타라 위력 · 신속 보석을 검사보다 더 받는다, 목표 범위 0.9를 넘는다). 딜링모드 실효 딜은 전의 57%(3.13 × 0.6 → 1.615 × 0.66).
33. [F4 · 결과] 보스 처치 속도 ÷ 딜러 4명(4인 보스 HP 같음, 파티 딜 = (딜러 합 + 치유사 합) × (1 + b), b = `PartyConfig.healerBuffFraction` 1.29%): 장비 없음 · 치유모드 - 도적 딜러 1 · **0.864** · 0.714 · 0.565 · 0.416 / 검사 딜러 1 · 0.897 · 0.781 · 0.665 · 0.549. 순서(3+1 > 2+2 > 1+3 > 4)는 전 구성 O, "딜러4 ≈ 딜러3+치유사1"(판정 = 90% 이상, 임의 기준)은 도적 딜러 86% · 검사 90%(상위 장비만 O) → "결정 필요" 6. 원인: b가 옛 치유사 딜 비(`healerDpsRatio` 0.9491 = 옛 딜링모드 1.5845 × 가동률 0.599)로 뽑힌 값이다 - 같은 식에 지금 치유모드 r(도적 기준 0.41)을 넣으면 b = 17.3%이고 그때 3+1 = 1.000 · 2+2 = 0.83 · 1+3 = 0.65 · 4 = 0.48(보고서의 "b 재산정" 줄 - 게임 값은 안 바꿨다). 딜링모드(쉴드 · 전투 비율 0.26)로 싸우면 3+1 = 0.80 ~ 0.82.
34. [F · 검증] P0(가) 7번("장비가 딜러만 키워 비중이 준다")은 옛 모형의 모양 검사라 "도적 3 + 1 비중 세 단계 ≥ 12%"로 바꿨다. 표본 D2(r 딜링모드 1.5412 등)는 P2 전 값에서 잰 것이라 `p2before` 덮어쓰기 안에서 대조한다(도구가 옛 값을 재현하는가) - 로컬 하네스 P0(가) 18/18.
35. [G1 · 구조] `PartyConfig.expBonusCondition = { radiusStuds = 150, activeWithinSeconds = 60 }`. 판정 = 새 모듈 `server/PartyExpBonus.lua`(순수 `isEligible(받는 사람 정보, 파티원 정보, 지금)` + 구역 판정 `zoneOf`) - `PartyState.setExpEligibility`로 등록(PartyState는 BossEncounter · 위치를 모른다 - 순환 require 방지, 불러오는 곳 = CombatResolution). `PartyState.getExpBonusFor(받는 사람)` = 조건 충족 실제 멤버 수(받는 사람 포함)의 보너스 → `PlayerProfile.getExpGainMultiplier`(경험치 · 재료 지급)가 쓴다. 판정 함수가 아직 없으면 0(옛 무조건 보너스로 새지 않게).
36. [G1 · 구역 해석] "같은 구역" = 둘 다 같은 보스 인스턴스(`BossEncounter.getEncounter` 표가 같다) 또는 둘 다 보스전 밖에서 같은 tier 사냥 구역(`ZoneBounds.isInside` - WorldConfig.tierZoneOrder). 받는 사람이 사냥 구역 밖(리스폰 · 커뮤니티 · 강화소)이면 아무도 안 센다. 반경 = 두 HumanoidRootPart 사이 직선 거리. 활동 = 공격 요청(`AttackServer` 쿨다운 통과 순간) · 스킬 요청(`SkillServer` 쿨다운 통과 순간 - 치유 Q 포함)을 받아들인 시각(`PartyState.noteActivity`). 받는 사람 자신은 조건 없이 1명으로 센다(처치 = 활동).
37. [G2] 판정은 경험치 지급 순간(`getExpGainMultiplier` 호출)에만 - 파티 ≤ 4명 × 거리 · 표 조회뿐(매 프레임 없음). 클라 칩(Attribute `PartyExpBonus`)은 그대로 "인원 보너스"(조건 충족 시 받을 값)를 보여 준다 - 조건 표시는 UI 범위 밖 → "결정 필요" 7.
38. [G · 옛 검증] S09(나)(인원 → +10/15/20% · 옵션 상한 ×1.5)와 S12(나) [7](파티 보너스 × 경험치)의 스탠드인(캐릭터 없는 표)은 새 조건을 못 채운다 → Studio 전용 `PartyExpBonus.debugSetPresence(멤버, { zone, position })`로 실제 Player와 같은 가상 구역 · 자리에 두고 `noteActivity`로 방금 활동한 것으로 기록했다(준비만 추가 - 판정식 · 합격 기준 그대로). 조건 자체는 P2(가) G1(10가지) · P2(나) G(5단계)가 잰다.
39. [G · 시뮬] 사냥 경험치 보너스는 `EconSimConfig.partyExpRequiresPresence`(게임 규칙 = true)에서 프로필의 `partyHuntsTogether`가 참일 때만 붙는다. 상위 1%는 [가정] "사냥은 솔로 · 파티는 보스전만"(P0와 같은 가정)이라 이제 사냥 +20%가 없다(전: 파티 소속만으로 +20%, S21-0 D3). p2before는 옛 규칙.
40. [검증 블록] 새 `server/P2Verify.lua`: P2(가) 26항목(환생표 · GoldCost 5 · 정밀도 · NumberFormat 2 · 보석 곡선 3 · 드랍표 9 · 치유사 3 · 파티 조건 2) · P2(나)(4인 스탠드인 파티의 조건부 배수 5단계 · 실제 처치 경로 태초 드랍 itemLevel = 사냥 스테이지 · 조회 API = 서버 굴림 확률 · 되돌림). 로컬 하네스(스크래치패드 `mk_p2.py` - PartyState · PartyExpBonus · DropTableQuery 실제 모듈 + 스텁) P2(가) 26/26.
41. [H1 · 도구] `/gg econ <프로필> compare` = 지금 값 기준선 + `p2before`(P2 전 게임 값)를 같은 도구로 한 번 더 돌려 "P2 전후 비교" 절(① 도달 시간 · 환생 5회차 ② 구매력 ③ 보석 비중 ④ 태초 공급 · tier별 손익분기 ⑤ 치유사 ⑥ 파티 구성 속도)을 붙인다. 로컬 117초.
42. [B · 재조정] 파트 C ~ G가 다 들어간 뒤 일반 프로필 환생 5회차가 12.11 → 12.82시간(필요 레벨 92)으로 밀렸다 → 최종 시스템에서 격자를 다시 돌려 5회차 필요 레벨을 **90**으로(88 = 11.38 · 89 = 11.70 · **90 = 12.02** · 91 = 12.39 · 92 = 12.82시간). 원인 추적(덮어쓰기로 파트를 하나씩 끔): 태초(E)만이 영향 - 태초를 따로 굴리며 다른 등급 확률이 ×(1 − 태초 확률)(≈ −0.095%)로 줄자, 시뮬의 결정적 문턱("가방 기대 개수 ≥ 1")에서 유물 방어구가 한 점검 늦게 잡혀 생존 사다리가 늦어진다(스테이지 50: 5.40 → 6.40시간 · 환생 5회차 +0.7시간). 게임에서는 0.1% 확률 차이라 체감이 없을 경계 효과 - 태초를 끄면 90 = 11.31 · 92 = 12.11시간(민감도).
43. [H · Studio Play 1회차(검증 모드 켬 → 정지 → 끔)] 자동 검증: P2(가) **26/26** · P2(나) **4/4** · 동반 P0(가) 18/18 · S01(가)(나) 9/9 · 5/5 · S03(가)(나) 10/10 · 5/5 · S04(가) 5/5 · S05(가)(나) 11/11 · 7/7 · S05b(가)(나) 11/11 · 5/5 · S08(가)(나) 20/20 · 5/5 · S09(가)(나) 14/14 · 13/13 · S10(가)(나) 4/4 · 7/7 · S11(가)(나) 6/6 · 8/8 · S12(나) 16/16 · S12b(가)(나) 17/17 · 13/13 · S13(가)(나) 9/9 · 8/8 · S13b(가)(나) 13/13 · 11/11 · S19b(가)(나) 15/15 · 9/9 · S20e(가)(나) 13/13 · 14/14 · S21-0(가)(나) 15/15 · 5/5 · 26-2 41/41 · 26-3 6/6 → **X 0** · 스크립트 에러 0(에셋 다운로드 경고 1건 - 무관). 같은 Play에서 채팅 `/gg econ all compare`(Studio 56초) → `docs/econ/P2-after.md` + CSV 12개(`_extract.py --stem P2-after`). P2(나) E4 실측: 개발 계정 사냥 = 최고 = 스테이지 4 · 드랍 1개 = 태초 itemLevel 4.
44. [H1 · 도구 대조] 새 도구 `p2before` 값(P2-after.md 비교 절 "전")이 옛 도구로 뽑은 `P2-before.md`와 이정표 · 최고 스테이지가 같다(캐주얼 11.1 · 40.2 · 130.5 … 3000 / 일반 5.40 · 34.47 · … 3455 / 상위 1% 1.62 · 7.33 · … 3910) → 비교표의 "전" 칸이 P2 전 게임을 정확히 재현한다. `_extract.py --stem`은 로그의 가장 마지막 실행만 쓰도록 고쳤다(처음 실행에서 옛 실행들이 같은 이름을 덮어써 compare에 없는 옛 CSV 3개를 지웠다).
