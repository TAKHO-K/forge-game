# G1-1 보고 - 표시 · 문구 묶음 + 보스 선택 창 보상 + 3타 표시

지시 = 사용자 프롬프트(G1 D · E · F · 기존 버그, COMMON.md §7). 기준선 = `c499cba`. PRD = 20.124.

**한 줄 요약**
- 등급 색 단일 출처 · 태초 전용 색 · 문구 7곳 · ? 도움말 2단 · 번역 가능 문자열 규칙(COMMON) + `shared/Text` · 3타 표시(발밑 고리 + 조준 외곽선) · 보스 보상 목록 `DropTable` 단일 소스 + 표시 어긋남 3곳 · `shared/BossLook` 추출 · 파티원 아레나 잔류 버그.
- Play 3회 + 스크린샷 Play. 3회차: G1-1(가) 4/4 · (나) 3/3 · S11(UI) 8/8 · S15(UI) 18/18 · S20e(UI) 17/17 · P25b(UI) 11/11 · S12(나) 16/16. 남은 X 2 = 검증 쪽(고친 뒤 미실행) · 타이밍.

## ① 합격 기준

| 항목 | 결과 | 근거 |
|---|---|---|
| 등급 색상표 통일 · 태초 전용 색 | O | `[G1-1][가] 태초 색 #ff3cc8: 가장 가까운 등급까지 ≥ 80 · 흰색까지 ≥ 150` · 흰색 분기 6곳 제거(가방 아이콘 · 보석 헤더 · 보석 칸 · 상세 시트 2 · 재련 · 계승) · 채팅 줄 등급 색 |
| "재련에 먹일 보석" → 원리 한 줄 | O | 재련 탭 머리 "재료 보석 - 레벨을 대상에 넘겨주고 사라집니다(…)" · 버튼 "재료로" |
| "되는 단계" → "성공 시" | O | 행 이름 "성공 시 · 실패 시 · 유지 · 실패 시 · 1강 하락 …"(머리만 바꾸면 실패 행 뜻이 틀림 - D0 (g)) · 열 머리 "단계" · `[G1-1][UI] 강화 확률표: [성공 시] · [실패 시 · 유지] O` |
| ? 도움말 2단 | O | 스크린샷: 순위 창 접힘(짧은 줄 + "눌러서 자세히 ▼") → 패널 클릭 → 상세 + "접기 ▲" · 자체 점검 접힘 높이 ≥ 44 |
| 번역 가능 문자열 규칙 | O | COMMON §2 새 규칙 + `shared/Text.get(key, { 이름 })` · `shared/data/TextData.lua` - 이번에 새로 쓰거나 고친 문장 전부 키로 |
| 3타 표시: 발밑 고리 + 조준 하이라이트 색 | O(스크린샷) · 점검 △ | 스크린샷: 캐릭터 오른쪽 옆 호 3칸(1칸 채움). 자체 점검 칸 채움 · 강타 신호 O, "2초 뒤 꺼짐"만 X - 꺼진 칸 투명도를 스크린샷 뒤 바꿨는데 점검 기준을 안 고쳤다(`3fa1a22`에서 고침, 미실행) |
| 보스 선택 창 어긋남 3곳 | O | ① 강화석 ≈ = 서버 값(지급과 같은 `Loot.expectedMaterialCount` × 경험치 배수) `[G1-1][나] 성장 옵션을 붙이면 띠의 강화석도 오른다 … O` ② "매번 장비 1" → "재도전" 줄로 분리 ③ 재도전 등급 표시 "장비 1개(일반 90% · 희귀 10%)" - 스크린샷 · `[S11][UI] 8/8` |
| 보상 목록 DropTable 단일 소스 | O | `DropTableData.bossGrades`(값 = 옛 tier6 · tier1 그대로, 드래곤 표와 분리) · `DropTable.bossFirstClearGradeTable / bossRetryGradeTable`을 서버 굴림(Loot)과 띠가 같이 부른다 · 재도전 20,000회 일반 0.90 · 희귀 0.10 |
| 보스 조립 공용 모듈(판정 무변경) | O | `shared/BossLook`(루트 · 몸통 · 머리 · 부착물 - 값 · 순서 · 이름 그대로) · 29-5(나) 11/11(분신 ↔ 보스 하위 인스턴스 대조) |
| 파티원 일반 → 일반 이동 시 아레나 잔류 | O(코드) | 지금 보스전에 속하면 빠진다 · 이전 스테이지가 보스가 아니면 자기만(리뷰 2). 다중 클라 재현은 못 함 - S12(나) 16/16 · 27-3(나) 3/3(파티 경로 회귀 없음) |

## ② 검증 · 리뷰

| Play | 목록 | 결과 |
|---|---|---|
| 1 | 필터: G1-1 · S11 · S01 · 29-5 · S10 · P2 | 전부 통과 |
| 스크린샷 | - | 보상 띠 3줄 · 2단 도움말 접힘/펼침 · 3타 고리(체력바 · 몸에 가려 자리를 두 번 옮김 → 오른쪽 옆 호 · 꺼진 칸 패널색) |
| 2 | 전 블록 | 새 X 후보 2 = S20e(UI)(가방 대상 5 → 1) · G1-1(UI) 리셋 - 둘 다 서버 검증과 겹침(3회차 좁게 돌려 S20e 17/17 · P25b 11/11). 옛 X(S10 · S12b · S16 · S19b(UI) · 29-1 힌트 부하 · G1-0 D 한 프레임 2.69ms) |
| 3 | 필터: G1-1 · S11 · S12(UI) · S15 · S20e · P25b · S12(나) · 27-3(나) | 위 ① · X 2 = G1-1(UI) 점검 기준(검증 쪽 - 고침, 미실행) · S12(UI) "stageSelect station 열기 실패"(바로 다음 줄에서 같은 창을 재서 화면 안 O - station 겹침 타이밍으로 봄, 미확정) |

- 동반(COMMON §3): Loot · DropTable · MonsterData · BossRewardPreview · MonsterSpawner · StageServer · 클라 도움말 · 가방 · 보석 · 강화대 · 보상 띠를 쓰는 블록(DevToolsConfig 주석) - 2회차 전 블록.
- 리뷰(서브에이전트) 4건 반영 + 참고 1: ① 파티 도움말(hover)은 상세를 누르러 가면 닫혔다 → 2단은 누름 전용 ② 리더 승계받은 파티원이 이동하면 보스전 전체가 끝남 → 자기만 ③ 펼친 상세가 패널 밖으로 넘침 → 글 높이로 ④ 강화석을 매번 줄에 붙이면 잘림 → 자기 줄(최대 5줄) · 참고: 띠의 강화석은 요청 순간 파티 경험치 조건(같은 구역 · 반경)에 따라 달라진다(지급 식과 같음).

| 커밋 | 내용 |
|---|---|
| `5d00b3f` | A 등급 색 · 문구 · 2단 도움말 · Text · COMMON 규칙 |
| `ee4a988` | B 3타 표시 |
| `718d8b0` | C 보상 단일 소스 · BossLook |
| `8d78abe` | D 파티원 잔류 · 검증 블록 |
| `0cd5f26` | 스크린샷 수정(고리 자리) |
| `725a0d2` | 리뷰 반영 |
| `3fa1a22` | 검증 기준 수정(미실행) |

## ③ 가정 · 결정 로그

| # | 결정 | 이유 |
|---|---|---|
| 1 | 태초 색 = 진한 자홍 (255, 60, 200) | style-bible §6-1 제안 두 개 중 다른 6등급과 가장 멀다(청록은 희귀 파랑과 가깝다) |
| 2 | "되는 단계" = 행 이름을 "성공 시 · 실패 시 …"로, 열 머리는 "단계" | 열 머리만 "성공 시"로 바꾸면 실패 행에서 뜻이 틀린다(D0 (g)) |
| 3 | 2단 도움말 = 표 `{ short, detail }`, 패널 전체가 누를 자리 · 다시 열면 짧은 쪽부터 | 터치 44 이상을 패널 크기로 채운다 · hover 금지 규칙 |
| 4 | 번역 모듈은 최소(`Text.get` · 이름 인자) - 로블록스 `Translator`는 L 단계에서 이 함수 안에 끼운다 | 호출하는 쪽을 L 단계에서 다시 안 고치게 |
| 5 | 3타 고리 = 캐릭터 오른쪽 옆 호(40° 간격 · 반경 2.4) · 꺼진 칸 패널색 | 스크린샷: 카메라 쪽은 체력바 · 스킬 칸에, 뒤쪽은 몸에 가렸다 |
| 6 | 보스 등급표 값은 그대로 두고 드래곤 표와 선언만 분리 | G1-2 드래곤 공정성 보정이 보스 보상을 같이 바꾸지 않게 |
| 7 | 띠의 강화석 = 서버가 계산해 응답에 싣는다(등급표는 클라가 shared `DropTable`로) | 경험치 배수(옵션 · 파티)는 서버만 안다 |
| 8 | 옛 체력바 위 점 자리(`ComboPipsAnchor` · ScreenMap `comboPips`)는 남겨 둠 | 다른 HUD 배치 점검이 그 슬롯을 본다 - 지우는 것은 HUD 정리(P4)에서 |

## ④ 미결 · 보고

- 미결: G1-1(UI) 고리 꺼짐 판정(고친 뒤 미실행) · S12(UI) stageSelect station 열기 실패(타이밍 추정) → G1-2 첫 Play에 G1-1(UI) · S12(UI) 동반.
- 보고: 파티원 잔류 버그는 다중 클라가 필요해 실제 재현은 못 했다(코드 경로 + 파티 회귀 블록만).
- 사람이 확인할 것: 태초 자홍이 "태초답게" 보이는가 · 3타 고리 자리(오른쪽 옆)가 전투 중 읽히는가(폰 포함).
