# P3d-F 로그 - P3d 후속: 재검증 + 확인 6건 반영

지시 = 사용자 프롬프트(자율 진행 단계, COMMON.md §7). 읽은 문서: README · COMMON · PRD 20.121 · P3d-report · P3d-log · docs/perf/baseline.md.
기준선 = `345ee45`(P3d 문서). 하네스 = 스크래치패드(luau CLI는 P3d 세션에서 복사).

## 가정 · 결정 로그

| # | 파트 | 결정 | 이유 |
|---|---|---|---|
| 1 | A1 | 누수 반복 = 같은 슬롯(bossArena1)에서 보스전 A(구간 수호자 진동파) → 재생성 등록 직후 종료(홀수 회) · 전조 중 종료(짝수 회) → 곧바로 보스전 B(서리 거인) 5회. 계측 훅 = `BossPatterns.debugEventHook`의 `regrowQueue`(등록) · `regrowPlan` · `regrowSkip`(취소 - 이유 · 솟기 직전 여부) · `regrowSpawn`에 슬롯 · 토큰을 실었다 | 지연 작업은 두 겹(task.defer 자리 찾기 · task.delay 솟음)이라 두 시점에서 끊어 봐야 둘 다 잰다 |
| 2 | A1 | 스킬이 끝난 순간 그 슬롯에 보스전이 없으면(토큰 nil) 재생성을 등록하지 않는다 | 코드 읽기에서 찾은 틈: nil 토큰은 `planRegrow`의 대조를 건너뛰어 다음 보스전에 솟을 수 있었다(재현은 안 됐다 - 막음) |
| 3 | A2 | 29-1 단독 = Play 1의 목록을 `29-1 · P3d(나) · P3dF(나)`로 줄였다(무거운 (가) 0) | 결과 +10.02초(허용 ±0.25) O → P3d의 +0.56초는 부하 탓. 전 블록 Play 2 · 3에서는 +10.19초(O) · 힌트 예고 0.71 ~ 0.73초(X - 기대 0.60, 부하) |
| 4 | A2 | S19b · P25b 화면은 스크린샷 Play에서 S19b만 찍었다(보스전 없을 때 보스 체력바 숨김 - O). P25b(UI)는 가방 내용(먹이 후보 · 등급별 개수)에 기대는 항목이라 스크린샷으로 가를 수 없어 생략 | "가능하면 스크린샷만" |
| 5 | B1 | 사용자 확인(개발 계정 맞음) → 이미 들어가 있는 `LeaderboardConfig.excludedUserIds = { 11595243049 }`(라이브에서만) 그대로 확정. 코드 변경 없음 | - |
| 6 | B2 | 복귀 보호 0.75초 유지(코드 변경 없음). 단 보호를 "받는 피해 0배"에서 **무적 플래그**(출처 `returnProtect`)로 옮겼다(B6) | 회전베기 감소를 덮지 않게 |
| 7 | B3 | 금 간 단상 = 윗면 한가운데서 뻗는 굵은 금 6줄(폭 0.7 · 가지 하나씩 - 옛 0.25 × 3줄) + 보이는 파트 색을 `UIColors.danger` 쪽으로 35%. 값 = `BossArenaMapData.obstacle.daisCrackLook` | 새 색 금지 - 기존 위험색 재사용. 스크린샷(PC · 폰 800폭 축소)에서 금 · 색 모두 구분됨 |
| 8 | B3 | 붕괴 예고 = 무너질 파동(금 간 단상 · 첫 겹)이 한가운데까지 남은 시간 ≤ 0.5초가 되는 틱에 `stage = "warn"`(서버 `BossArenaMap.warnDais`) → 클라가 그 단상을 남은 시간 동안 크게 흔들고(진폭 0.45) 둘레 먼지 10개. 파동이 0.5초 안에서 태어나면 남은 시간만큼(클라 최소 0.35초) | 판정 시각(무너짐 = 파동이 한가운데를 지나는 순간)은 그대로 - "보이는 장판 = 실제 판정" 유지. Studio: 예고 → 무너짐 0.50초(계산 0.49) |
| 9 | B4 | 상한 교체 = 상한(14)이면 **재생성된 것 중** 가장 오래된 것(전역 일련번호가 가장 작은 것)을 cause `cap`(부드러운 붕괴 - 제자리 주저앉음 + 먼지)으로 무너뜨리고 새로 세운다. 배치(맵 원래 구조물)는 교체하지 않는다 · 누가 끼여 있는 것은 건너뛴다 · 새 자리가 정해진 뒤에 무너뜨린다(못 찾으면 옛것 유지) | "가장 오래된 것" = 재생성분으로 읽었다(배치는 맵 자체). 리뷰 1 · 3 반영 |
| 10 | B4 | 이동 가능 면적 하한 = `regrow.minWalkableFraction = 0.7`(연결 검사 격자 칸 중 빈 칸 비율) - 못 지키면 그 자리를 버린다(`ArenaLayout.regrowOpen` = 닫힌 공간 0 + 하한) | 사용자 예시 70%. 실측 최소 97.4%라 지금 배치 규모에서는 걸리지 않는 안전망 |
| 11 | B4 | 12인 = 아레나 12개가 동시에 전부 상한(솔로 12명 - 최악). 서버 모듈 사본으로 스크린샷 Play에서 쟀다(배치만 → 재생성 상한까지 - 각 3초 정착 뒤 4초 표본) | 자동 검증 Play 3의 같은 측정은 생성 직후 표본이라 Heartbeat가 튀었다(평균 8.03ms) - 정착 뒤 값을 보고에 쓴다 |
| 12 | B5 | 코드 확인 결과 **현행은 끼인 동안 무적**(`setIncomingDamageMultiplierUntil(member, 0, 6.1초)`)이었다 → 무적 제거(받는 피해 평소대로). 3타 탈출 · 6초 자동 파괴 유지 | 지시(악용 방지) |
| 13 | B5 | "끼이는 순간 피해 0 **유지**" - 현행은 끼임도 솟는 순간 평타 ×2를 받았다(P3d 로그 23). 지시 문장대로 끼임은 0, 밀림은 그대로 평타 ×2로 바꿨다 | 결정 필요 2로 확인 요청(현행과 지시가 달랐다) |
| 14 | B6 | 받는 피해 배율 = 출처별 키 `{ multiplier, untilAt }` · 최종 = 살아 있는 배율의 곱 · 무적 = 출처별 별도 플래그(`setInvulnerableUntil` - 있으면 최종 0) · 푸는 쪽은 자기 키만. 키: `dash` · `skill:<직업>:<슬롯>` · `trapReleaseGrace`(0배면 무적) · `returnProtect`(무적) · `launchHold`(회오리 체공 - 무적) · `verify`(P3b 검증) | 지시 그대로. 같은 키를 다시 걸 때는 옛 규칙(더 강하고 더 긴 것이 살아 있으면 둔다)을 키 안에서만 쓴다 |
| 15 | B6 | 결과: 대시(0.5) × 회전베기(0.5) = **0.25배**(옛: 0.5배 하나) | 곱 규칙의 직접 결과 - 결정 필요 1 |
| 16 | 전수 점검 | 서브에이전트 감사(읽기 전용) → 실제 겹침 5칸을 같은 방식(출처별 · 자기 키만 해제)으로 고쳤다: 루트 고정 · 이동속도 · 채널링 칸 · 체력바 눈금 · 치유 버프 지속. 목록 = 보고서 ⑤ | 지시 "발견하면 같은 방식으로 수정" |
| 17 | 전수 점검 | 이동속도 출처에 만료 시각(채널 + 0.5초)과 만료 때 재계산을 둔다 | Play 2: P3b(나)의 실제 회전베기 끝에서 경고 한 줄("value of type nil cannot be converted to a number") 뒤 감속 키가 남았다(Play 3에서는 재발 없음 - 원인 줄 미확정). 끝 처리가 끊겨도 저절로 풀리게 |
| 18 | C | 새 저장 필드 없음 → snapshotForDevTools 변경 없음 · SAVE_VERSION 그대로 | - |
| 19 | 검증 | P3dF(가E)(교체 100시드 × 6맵 × 16회) = 235초로 무겁다 - Play 3 목록에서 뺐다(그 뒤 ArenaLayout 무변경) | Play 2에서 7/7 |

## Play · 리뷰 기록

| Play | 목록 | 결과 | 원인 · 처리 |
|---|---|---|---|
| 1 | 29-1 · P3d(나) · P3dF(나) | 29-1(가) 7/7 · (나) 24/25 · **P3d(나) 11/11**(미결 1 해소 - D 밀림 · 끼임 · 3타) · **P3dF(나) 6/6**(A1 5회 전부 깨끗함) | 29-1 X 1 = 20.82부터의 기존 X(`파훼 실패: 체력 100%`). 첫 기믹 +10.02초 O |
| 2 | P3dF(나)(가E) + P3d와 같은 전 블록(101줄) | P3dF(나) 10/11 · (가E) 7/7 · P3d(나) 11/11 · P3c(나) 17/17 · 29-2(나) 32/32. 그 밖 X = 27-4(나) 4 · 29-1(나) 1(힌트 예고 0.73 - 부하) · S13(나) 4 · S13b(나) 5 · S12b(UI) · S16(UI) · S19b(UI) · P25b(UI) 2 | **P3dF B 이동속도 X = 제품**: 앞 블록의 회전베기 감속 키가 남아 있었다 → 만료 시각(로그 17). **27-4 · S13 · S13b = 개발 계정 상태**: 최고 스테이지 44(기대 7) · 무기 +13(WeaponLevel 13)의 최종 데미지 버킷이 치유량에 곱해져 ×1.467(검증 기대식은 재생 배수만 1.00) - 이번 변경(치유 버프 지속만)과 무관. UI 4건 = 이전부터의 타이밍 계열 |
| 3 | 위 - P3dF(가E) | 95블록 · **P3dF(나) 12/12** · P3d(나) 11/11 · P3c(나) 17/17 · 29-2(나) 32/32(step 60.4마이크로초 O) · 29-3 · 29-4 · 29-5 전부 O. X = Play 2와 같은 계정 · UI 계열 + S10(UI) 1(드랍 피드 흐림 타이밍) | 진단 줄: 채널 끝에 남은 출처 없음 · 경고 재발 없음 |
| 스크린샷 | 검증 끔 | 금 간 단상 전 · 후 · 폰 800폭 축소 · 붕괴 예고 · 보스전 없을 때 HUD · 12인 성능 · 상한 교체가 끼인 구조물 건너뜀(모듈 사본) | 사본 모듈은 RemoteEvent를 따로 만들어 클라에 안 닿는다 - 예고 연출은 실제 이벤트를 FireClient로 쏴서 찍었다 |

**리뷰(서브에이전트, 전체 diff `345ee45..HEAD`) 15건** - 없앤 API 잔존 0 · 옛 검증 기대값(0배 = 무적) 호환 확인

| # | 지적 | 심각도 | 처리 |
|---|---|---|---|
| 1 | 상한 교체가 새 자리를 찾기 전에 옛것을 부숨 - 자리 찾기 실패면 하나가 그냥 사라짐 | 중간 | **고침** - 옛것을 뺀 배치로 자리를 찾고, 정해진 뒤 무너뜨린다 |
| 2 | 대시 × 회전베기 = 0.25배(옛 0.5) | 중간 | **결정 필요 1** |
| 3 | 교체 대상이 끼인 구조물일 수 있음 | 낮음 | **고침** - 끼인 것은 건너뛴다(모듈 사본 확인: 끼인 #1218 대신 #1219 교체) |
| 4 | 리스폰 경합 - reset이 표를 지운 뒤 만료 재계산이 안 불림 | 낮음 | **고침** - 표가 비었어도 재계산 |
| 5 | 몹 사망 때 눈금 출처를 안 지움(옛 칸도 같음) | 낮음 | 보고 - 기존 동작. 다음 set 때 Parent 없는 출처는 빠진다 |
| 6 | 잡힘은 틱 경계에서만 봄(최대 한 틱) | 낮음 | 보고 - 루프 구조상 |
| 7 | releaseMember가 스탠드인 표 루트 고정을 안 풂 | 낮음 | **고침** |
| 8 | 검증이 root.Anchored를 직접 켜고 끄는 경로와 출처별 고정이 섞임 | 낮음 | 보고 - 검증 쪽 위험(게임 회귀 아님) |
| 9 | P3d(나) D4가 실패 시 planRegrow를 한 번 더 부름(이제 상태를 바꾸는 함수) | 낮음 | 보고 - 검증 전용 · 리뷰 1 반영으로 실패 시 상태 무변경 |
| 10 | 예고 뒤 보스가 끊기면 헛예고 · 구덩이 달그락 중이면 흔들림 빠짐 | 낮음 | 보고 |
| 11 | 클라 warn_ 전방 선언 | - | 문제 없음 |
| 12 | 연출 숫자(금 가지 각도 · 먼지 크기 · 만료 여유)가 data 밖 | 낮음 | 보고 - 밸런스 값 아님(기존 연출 코드와 같은 관례) |
| 13 | 치유 버프: 더 늦은 만료는 맞음 · 배율은 새로 건 값 | 낮음 | 보고 - 지금은 같은 상수 |
| 14 | planRegrow가 context를 바꿈 | - | 리뷰 1 반영으로 없앰 |
| 15 | 범위 밖 게임 코드 변경 | - | 없음 |

## 결정 필요

| # | 무엇을 | 왜 · 선택지 |
|---|---|---|
| 1 | 대시 × 회전베기 = 0.25배(곱) | 지시 "최종 = 곱"의 결과. 옛 규칙은 0.5배 하나. 유지하거나, "받는 피해 감소"끼리는 가장 강한 것 하나(무적 · 끼임과는 별개)로 둘 수 있다 - PlayerState.getIncomingDamageMultiplier 한 곳 |
| 2 | 끼이는 순간 피해 0 | 지시는 "유지"였으나 현행은 끼임도 평타 ×2였다 → 지시대로 0으로 바꿨다(밀림은 ×2 그대로). 옛 값으로 돌리려면 BossPatterns 한 줄 |
| 3 | 재생성 자리 찾기 1회 8.1ms(최대 16.4ms) | step 밖(task.defer)이지만 서버 메인 스레드라 그 프레임은 무겁다. 12 아레나에서 스킬 끝이 초당 약 2번 → 가끔 한 프레임 16ms. 연결 검사(격자 BFS) 캐시 · 시도 수 축소로 줄일 수 있다(범위 밖) |
| 4 | 개발 계정 상태에 기대는 옛 검증(27-4 · S13 · S13b) | 계정이 진행(최고 44 · 무기 +13)하면서 X. 기대식을 계정 무관하게(무기 최종 데미지 포함 · 스테이지 강제) 고칠지 - 범위 밖이라 안 건드렸다 |

미결: 리뷰 반영 1 · 4 · 7(`1c8697f`)은 Play 3 뒤라 자동 검증 Play로 다시 못 돌렸다(세션당 3회). 3은 모듈 사본으로 확인했다. 다음 세션 첫 Play에서 P3dF(나)를 동반으로 돌린다.
