# P3d 로그 - 보스 연출 1차(카툰) · 이탈 복귀 변경 · 단상 균열 · 지형지물 재생성 · 겹침 방지 · 치유사 파티 버프 상향 · P3c 결정 반영

지시 = 사용자 프롬프트(자율 진행 단계, COMMON.md §7). 읽은 문서: README · COMMON · P3c-report · P3a-bossmap-plan · PRD 보스 절(20.75 · 20.118 · 20.120) · docs/perf/baseline.md.
기준선 = `6e0a295`(P3c 보고서). 하네스 = 스크래치패드(P3c 세션에서 복사 - `mk_econ.py` · `mk_p3c.py` · `luau/`).

## 가정 · 결정 로그

| # | 파트 | 결정 | 이유 |
|---|---|---|---|
| 1 | F1 | "처치 속도" = 경제 시뮬 F4(EconSimTables.healer - 파티 딜 ÷ 딜러 4명 딜, 보스 HP는 4인 배율로 같다)의 **치유모드** 값으로 잰다. 딜링모드 값은 참고로만 보고 | 치유사의 파티 역할이 치유모드다. 딜링모드는 보스전 전투 비율 0.26(S13b 모형)이라 어떤 b로도 목표와 치유모드를 같이 맞출 수 없다 |
| 2 | F1 | b = **0.1736**(옛 0.01289). 6조합(장비 없음 · 평균 · 상위 × 도적 · 검사)의 버프 뺀 3+1 속도 0.8526 ~ 0.8941 → 모두 [1.00, 1.05]에 드는 창 [0.17292, 0.17439]의 가운데 | 결과 3+1 = 1.0006 ~ 1.0493 · 2+2 = 0.828 ~ 0.925 · 치유사4 = 0.482 ~ 0.676(가장 느림). 창이 0.0015로 좁다 - 딜러 직업 딜이 바뀌면 다시 풀어야 한다(P3d(가)가 매번 잰다) |
| 3 | F1 | 24-4의 식 값은 `PartyConfig.healerBuffFormulaFraction`으로 남기고, S09(가)의 "b 불변" 검사는 그 값을 본다 | S09 검사의 뜻은 "파티 경험치 코드가 b의 입력을 안 건드렸다" - 사용자 결정으로 올린 b 자체를 옛 값과 대조하면 X가 된다 |
| 4 | F2 | 중첩 없음은 이미 구조로 보장된다(같은 buffId `healerBuff`를 refresh로 덮어쓴다 · 딜 계산이 multiplier를 한 번 곱한다). 코드 변경 없이 P3d(나)에서 치유사 2명이 번갈아 걸어도 배율 = 1 + b인지 잰다 | 값이 하나라 "최댓값 하나"와 같다 |
| 5 | F3 | 파티 칩 = 경험치 칩 아래 "✚ 피해 +17.4%" 칩(내가 버프를 받는 동안 · 모바일 축약형은 "✚ +17.4%"). 값 = PartyConfig, 모양 = `SkillTooltipText.pct`(툴팁과 같은 함수) | 옛 칩은 초록 테두리 · 이름 꼬리 ✚뿐이라 값이 안 보였다 |
| 6 | F | 리더보드 부정 방지 상한(`Leaderboard` memberDpsCap)도 (1 + b)를 곱해 17% 넓어진다 | 같은 소스를 읽는다 - 버프 받은 실제 딜이 상한에 걸리지 않게 |
| 7 | G-a · b · c | 코드 변경 없음: 줄넘기 없는 3종에 추가 안 함 · 폭풍 군주 방전 고리 파동 2 × 1.5 유지 · 심해 군주 발판 11곳(반경 140)은 P3c 그대로(BossData abyssalKitParts · BossArenaMapData RADIUS 140) | 사용자 결정 = 지금 값 확정 |
| 8 | G-d | 견습 7단계를 EconSim 앞에 돈다(`EconSim.tutorialPhase` - 프로필 `tutorial = true`, 지금은 캐주얼만). 게임과 같은 입력: 단계별 tier · killTarget · 견습 보스(`BossRules.buildTutorialInstanceData`) · 대여(무기 등급 + 3부위 itemLevel = 지금 레벨) · 지급(1 ~ 3단계 확정 드랍, 더 좋으면 낀다) · 완료 골드. 처치 · 보스 시간 = 무한 모드와 같은 프로필 파라미터 | 결과(하네스): 견습 11.3분(레벨 7로 끝) → **캐주얼 스테이지 20 = 13.3분(견습 포함)** · 견습 제외 4.4분. P3c 추정 26 ~ 35분은 PRD의 단계당 예산(3 ~ 5분 - 안내 읽기 · 강화대 · 스킬 연습 포함)이었다 - 시뮬은 처치 · 보스의 순수 시간만 잰다(차이는 보고) |
| 9 | G-d | 일반 · 상위 1% 프로필에는 견습을 넣지 않았다 | 지시가 "캐주얼 측정"이다. 다른 프로필의 P3c-after 표가 이유 없이 바뀌지 않게 - 필요하면 프로필 한 줄(tutorial = true) |
| 10 | G-e | 첫 시즌 시작 = `LeaderboardConfig.firstSeasonDateKst = { 년, 월, 일 }`(nil = 아직 - P6에서 확정) → 그날 0시(KST) = `LeaderboardRules.kstMidnightUnix`(달력 공식 - os.time의 시간대 해석에 기대지 않는다). 없으면 옛 seasonStartUnix | 검산: 2026-10-01 · 2027-01-01 · 2028-02-29 · 2026-03-01이 python datetime(KST)과 같다 |
| 11 | G-f | "함께 맞은 수" = 같은 원 안에서 실제로 함께 맞은 사람(BossPatterns circleTarget 판정 - 원 번호별로 센다). 원이 겹친 자리의 사람은 판정과 같이 먼저 찾은 원 하나에만 센다 | P3c(나) A4 기대값을 새 규칙으로 고쳤다(배치상 S1 · S2만 같은 원 → 2명 · 높이 6, 개발 캐릭터 1명) |
| 13 | B1 | 복귀 자리 = 본인의 보스방 스폰 자리 = 입장 자리(`BossArenaMap.entryPosition(zone, 멤버 순번, 인원)` - 입장 · 전멸 재도전과 같은 순번). 날려 보낸 기술의 피해는 그 판정에서 이미 들어갔고 복귀는 순간이동만 한다 - 체력 · 기믹 누적(%피해 발동 누적 · 반사 횟수 · 단 기록 · 잡힘)은 손대지 않는다 | 옛 "벽 8stud 안쪽 안전 지점"은 자리만 쓰였다. 스폰 자리가 구조물에 덮이면(재생성은 입장 자리를 비운다 - 안전망) 옛 안전 지점으로 간다 |
| 14 | B2 | 보호 **0.75초**(지시 범위 0.5 ~ 1의 가운데) = 받는 피해 0배(`PlayerState.setIncomingDamageMultiplierUntil`) + 넉백 무시(BossPatterns runHitEffects · 구조물 파편 BossArenaMap.fireBreak). 결정 필요 2 | 스폰 자리에 떨어지던 판정이 복귀 순간 곧바로 맞히는 일을 막는다 |
| 15 | B3 | 시뮬 `ArenaContainment.simulateReturn`: ①②가 뚫렸다고 치고 넉백을 상한 없이 12 × 1 ~ 3으로 줘서 이탈을 억지로 만든다 → ③ 복귀 규칙만 잰다. 1,000조합(파티 1 ~ 4명 순번 · 사건 1 ~ 6) = **이탈 266 → 복귀 266 · 연쇄(복귀 뒤 1.25초 안 재이탈) 0 · 체력 변화 0 · 누적 변화 0 · 스폰 원 밖 0 · 보호로 무시한 넉백 58** | 보호 0초로 같은 시뮬 = 연쇄 0(스폰 자리가 벽에서 50stud - 상한 없는 넉백 36으로도 못 나간다). 보호의 몫은 연쇄 이탈이 아니라 "복귀 직후 스폰 자리에서 맞는 판정"이다 - 보고서에 그대로 적는다 |
| 16 | C | "단상" = 올라갈 수 있는 큰 블록(돌무더기 · 고인돌 - ArenaLayout group "big", climbable, 윗면 3.5). 코드 · 문서에 "단상"이라는 말이 없어 가장 가까운 지형으로 읽었다 | 심해 군주 돌단(킷)은 범람 기믹의 안전지대라 부서지면 기믹 보장(발판 11곳)이 깨진다 - 제외. 작은 구조물(3.8)은 "올라가는 곳"으로 만든 것이 아니다 |
| 17 | C1 | **현행 판정 확인 결과: 단상 위에서도 지진파를 맞았다**(P3c ⑤ "큰 블록 윗면 +3.5 진동파 판정 O" - 파동 판정은 높이차 상한 8 안이면 맞는다). 지시 문장("단상 위에서는 쉽게 피할 수 있다")을 규칙으로 읽어 바꿨다: 윗면에 선 사람(발 ≥ 윗면 − 0.5 · 충돌 원 + 0.5 안)은 파동이 발밑으로 지나간다. 점프 틈 표시(흰 고리)도 단상 위에서는 끈다 | 보이는 파동 띠(높이 1.6)가 윗면(3.5) 밑으로 지나간다 - "보이는 것 = 판정". 단상은 맵당 1 ~ 2개이고 파동 두 번에 무너지므로 늘 쓸 수 있는 답이 아니다 |
| 18 | C2 | 파동은 한 번 찍은 것(첫 겹)이 단상 **한가운데**를 지날 때 센다: 1번째 = 금(서버 금 파트 + 먼지) · 2번째 = 무너짐(cause "wave"). 겹(심해 해일 2겹)은 한 번 | 한 스킬 안에서: 수호자 3파동 = 둘째에 무너짐 · 심해 3파동 = 둘째 · 폭풍 2파동(천둥 · 메아리) = 메아리에 무너짐. 여러 스킬에 걸쳐 누적(전멸 리셋이면 처음대로) |
| 19 | C2 | 무너질 때 위에 있던 사람 = 바닥으로 떨어지기만(피해 · 튕김 없음) + 떨어지는 동안 0.6초 그 파동에서 빠진다(`daisWave.dropGraceSeconds`). **기존 큰 블록 규칙(때리기 5타 · 돌진 1방 = 튕김 + 최대 체력 10%)과 다르다** | 무너지는 순간 띠가 발밑을 지나고 있어, 유예가 없으면 "떨어지면서 맞는" 피해가 생긴다(낙하 0.19초 + 띠 통과 최대 0.22초) |
| 20 | D1 | 재생성 시점 = 보스마다 "땅을 흔드는" 스킬 하나가 **정상으로 끝난 순간**(스킬 조각 `onComplete = { regrowObstacles, count 1 }` - 중단 · 리셋 · 전멸이면 안 돈다): 구간 수호자 진동파 · 서리 거인 빙결 강타 · 심해 군주 해일 · 수정 여왕 파편 폭발 · 전갈 여왕 잠행 찌르기 · 폭풍 군주 방전 고리 | 끝난 뒤 전역 쿨(5 ~ 7초 · 격노 2.5초) 안에 전조 1.5초 + 솟음이 끝나 다른 스킬의 전조 · 판정과 겹치지 않는다(피할 수 없는 조합이 안 생긴다) |
| 21 | D1 | 무엇이 솟나 = 그 맵 배치의 작은 구조물 · 작은 지형지물(group small · feature). 큰 블록(단상)은 안 솟는다 · 견습 보스전에는 안 솟는다 | 단상은 C의 소모 자원이라 다시 생기면 C2의 뜻이 약해진다. 견습은 배우는 자리(29-3 자동회복 예외와 같은 이유) |
| 22 | D2 | 전조 1.5초 = 그림자(어두운 원판이 판정 원 크기로 자란다) + 위험색 테두리 + 금 가는 흰 빛(길어진다). 회피 부등식 1.5 ≥ 0.5 + (가장 큰 발자국 5 + 1) ÷ 16 × 1.25 = 0.97 | 발밑 생성(멤버 한 명의 4stud 안을 50% 확률로 노린다)도 걸어서 비킨다 |
| 23 | D2 · D3 | 솟는 순간 몸이 충돌 원에 닿은 사람(수평 < 원 반경 + 몸 반폭 1): 피해 = 평타 ×2(방어 적용 - "솟는 바위"). 원 가장자리(반경 − 0.8 밖)면 원 밖 1.3으로 밀려나고, 안쪽이거나 밀려날 자리가 막혔으면 **끼인다**: 고정(루트 Anchored) · 받는 피해 0배 · 그 구조물 3타에 부서짐(누가 때려도) · 6초 뒤 저절로 부서짐 · 머리 위 "탈출! n타" + 남은 시간 막대(파티 전원 화면) | "경우에 따라 끼임"을 깊이로 나눴다(가장자리 = 밀림 · 한가운데 = 끼임). 끼인 동안 면역 = 움직일 수 없는 사람에게 오는 판정은 막을 수 없는 벌(29-1 잡힘과 같은 원칙) |
| 24 | D4 | 동시 상한 14(`regrow.maxObstacles` - 배치 최대 12 + 2)면 안 솟는다. 부서진 구조물은 서버 인스턴스(모델 · 충돌 기둥)를 전부 파괴 · 클라 파편은 풀(BossFx)로 돌아간다 | 100시드 × 6맵 × 8회 = 4,800회 중 상한으로 건너뜀 1,904(실전은 구조물이 부서지므로 더 적다) |
| 25 | E1 | 자리 검사 = `ArenaLayout.regrowSpot`(서버와 하네스가 같은 함수): 벽 틈 20 · 입장 방위 ±25°(스폰 자리 = B 복귀 자리 보호) · 다른 구조물(단상 포함)과 10 · 킷 10 · 보스 6 · 모래 구덩이 · 얼음 기둥(동적 지형)과 1 · 둔덕과 4 · 연결(갇힘 없음). 실패 = 다른 자리 80번 → 그래도 없으면 이번엔 안 솟는다(로그) | 배치 생성과 같은 잣대를 쓰되 "중심 26"은 뺐다(보스는 싸우는 동안 움직인다 - 대신 보스 지금 자리와 6) |
| 26 | E2 | 모래 구덩이는 이제 구조물 위에도 생긴다(`props.pit.breaksObstacles` - 옛 "구조물과 겹치지 않게"를 이 지형만 푼다). 걸친 구조물은 틱(0.75초)마다 달그락(클라에서 보이는 파트만 흔든다) → 3틱째 무너진다(cause "pit" - 위 사람은 떨어지기만, 피해 없음) | 사용자 요청. 구덩이는 스킬(잠행 찌르기)이 끝나면 사라지므로 틱 3번 = 무장 뒤 2.25초 - 스킬 안에 무너진다 |
| 27 | E3 | 100시드 × 6맵(하네스): 시드마다 재생성 8회(멤버 1 ~ 4명 · 보스 · 모래 구덩이 0 ~ 3개 무작위) = 4,800회 → **생성 2,896(멤버 발밑 952) · 겹침 0 · 갇힘 0 · 스폰 자리 덮임 0** · 건너뜀 1,904는 전부 상한 | 겹침은 regrowSpot과 따로 계산한 독립 검사로 잰다 |
| 28 | A1 | 지진파(줄넘기 ring) 보스 3종의 찍기 모션: 구간 수호자 = 두 손 내려찍기(주먹 둘이 머리 위 → 앞바닥) · 심해 군주 = 꼬리 휘두르기(꼬리 셋 마디가 뒤로 감겼다가 한 바퀴) · 폭풍 군주 = 지팡이 찍기(치켜들면 끝이 번쩍 → 앞바닥에 꽂음). 서리 거인 · 수정 여왕 · 전갈 여왕은 지진파가 없어 그대로 | 스타일 = `BossFxData.bosses[보스 id]`(데이터) · 빌더 = `BossMotionView` SLAM 표(스타일 이름 키 - 보스 이름 함수 없음). 새 ring 보스는 기본 "점프 착지"(늘어남 · 찌그러짐만) |
| 29 | A1 | 모션 방식 = 서버 보스 모델을 **내 화면에서만** 숨기고(LocalTransparencyModifier) 복제 인형을 서버 모델 피벗에 매 프레임 붙인다 - 늘어남(세로 ×1.25) · 찌그러짐(×0.7) · 앞뒤 기울기 · 팔 · 꼬리 · 지팡이. 모션이 끝나면 인형을 치우고 서버 모델을 되돌린다 | 서버 모델의 위치(= 판정 위치 · 떠오름)는 그대로 따라간다. 서버가 크기를 안 바꾸므로 판정 · 다른 클라에 영향이 없다 |
| 30 | A1 · A2 | 박자 = 서버 hop과 같은 시계(shockTelegraph seconds): 0 ~ 60% 들어 올림 · 60 ~ 85% 꼭대기 멈칫(떨림) · 85 ~ 100% 내려찍기 → 첫 겹 shockwave 순간 납작 + 0.35초 여운(넘쳤다 돌아옴). 다음 파동 예비 동작이 바로 이어지면 0.2초 동안 납작 자세에서 섞어 든다 | 찍는 순간 = 파동이 나가는 순간 = 판정 시계 |
| 31 | A2 | 풍압 = 먼지 공 14(파동보다 느리게 - 초속 9, 파동 18 ~ 36 - 앞서 가는 빨강 띠를 안 덮는다) · 바람 줄기 10(흰 막대 - 몸 높이 1.5 ~ 3.5) · 흰 고리 16. 흔들림 = 45stud 안 · 진폭 0.35 × (1 − 거리/45) · 0.22초. 끄기 = 플레이어 Attribute `SettingBossScreenShake = false`(`BossFx.setShakeEnabled` - 설정창이 생기면 이 함수를 부른다) | "약하게 · 끌 수 있게 준비" |
| 32 | A3 | 땅 파도 = 첫 겹 파동 띠 안에 흙 마루 32조각(맵 바닥색을 18% 밝게) - 높이 ≤ 1.3이 둘레를 따라 굴러간다(솟았다 꺼진다). 마루는 띠 [반경 − 두께, 반경]과 같은 자리이고 띠보다 낮고 좁아 빨강 띠가 겉을 감싼다 | 보이는 파도 = 판정 자리. 겹 2 이상은 옛 빨강 띠만(한 번 찍은 파동 = 흙 마루 하나) |
| 33 | A4 | 돌진: 전조 동안 발 긁기(인형이 뒤로 젖혀 흔들림 + 보스 뒤쪽으로 먼지를 찬다 - 0.28초마다) → 달리는 동안 앞으로 늘어남(×1.28) + 속도선(0.05초마다 2줄) · 잔상(흰 네온 인형 4벌 돌려 쓰기 - 0.09초마다) · 먼지 꼬리 → 도착 = 파편 8(구조물 충돌 12) · 먼지 · 흰 고리 · 흔들림(내가 대상이면 ×1.4). 대상 표식 · 방향선(P3c)은 그대로. 잠행(전갈)은 인형 없이 먼지만 | 다른 멤버가 대상이면 조각 수 × 0.5(`otherPlayerWeight`) |
| 34 | A5 | 조각 = 풀링 파트(`client/BossFx` - 공 · 판 · 막대, 풀 120개) · RenderStepped 하나가 전부 움직인다(조각마다 트윈 없음) · 동시 상한 90(넘으면 연출만 버린다 - 전조 그림은 essential로 안 버린다). ParticleEmitter · 새 에셋 없음 | COMMON "새 파티클 · 새 에셋 금지"를 지키면서 "덩어리감 있는 파티클"을 파트로 만들었다 |
| 35 | A | 새 색: 흙먼지(235, 228, 214) · 지팡이 자루(90, 70, 50) 두 값만 새로 썼다(나머지는 기존 흰색 · 위험색 · 보스 · 맵 색) | 사용자 지시(카툰 연출)에 맞는 기존 색이 없었다 |
| 12 | G-g | 라이브 제외 = 개발 계정 **11595243049**(Studio 로그의 프로필 키 `Player_11595243049`) - 사용자 확인 요청(결정 필요 1). 제외는 라이브(writeMode "live")에서만 건다 | 제외를 모든 모드에 걸면 Studio 검증 모드의 리더보드 기록 검사(개발 계정으로 _verify 저장소에 쓴다)가 전부 막힌다 |

## 결정 필요

| # | 무엇을 | 왜 · 선택지 |
|---|---|---|
| 1 | 라이브 제외 UserId = 11595243049가 개발 계정이 맞는가(G-g) | Studio 로그의 프로필 키에서 읽었다. 운영 계정이 더 있으면 목록에 추가 |
| 2 | 맵 이탈 복귀 보호 0.75초(B2) | 지시 범위 0.5 ~ 1초의 가운데. 시뮬상 연쇄 이탈은 보호 0초로도 0 - 값은 "복귀 직후 맞는 판정을 얼마나 막을지"의 체감 문제다(0.5 = 짧게 · 1.0 = 넉넉히) |
