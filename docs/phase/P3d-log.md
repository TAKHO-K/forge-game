# P3d 로그 - 보스 연출 1차(카툰) · 이탈 복귀 변경 · 단상 균열 · 지형지물 재생성 · 겹침 방지 · 치유사 파티 버프 상향 · P3c 결정 반영

지시 = 사용자 프롬프트(자율 진행 단계, COMMON.md §7). 읽은 문서: README · COMMON · P3c-report · P3a-bossmap-plan · PRD 보스 절(20.75 · 20.118 · 20.120) · docs/perf/baseline.md.
기준선 = `6e0a295`(P3c 보고서). 하네스 = 스크래치패드(P3c 세션에서 복사 - `mk_econ.py` · `mk_p3c.py` · `luau/`).

## 가정 · 결정 로그

| # | 파트 | 결정 | 이유 |
|---|---|---|---|
| 1 | F1 | "처치 속도" = 경제 시뮬 F4(EconSimTables.healer - 파티 딜 ÷ 딜러 4명 딜, 보스 HP는 4인 배율로 같다)의 **치유모드** 값으로 잰다. 딜링모드 값은 참고로만 보고 | 치유사의 파티 역할이 치유모드다. 딜링모드는 보스전 전투 비율 0.26(S13b 모형)이라 어떤 b로도 목표와 치유모드를 같이 맞출 수 없다 |
| 2 | F1 | b = **0.1736**(옛 0.01289). 6조합(장비 없음 · 평균 · 상위 × 도적 · 검사)의 버프 뺀 3+1 속도 0.8526 ~ 0.8941 → 모두 [1.00, 1.05]에 드는 창 [0.17292, 0.17439]의 가운데 | 결과 3+1 = 1.0006 ~ 1.0493 · 2+2 = 0.828 ~ 0.925 · 치유사4 = 0.482 ~ 0.676(가장 느림). 창이 0.0015로 좁다 - 딜러 직업 딜이 바뀌면 다시 풀어야 한다(P3d(가)가 매번 잰다) |
| 3 | F1 | 24-4의 식 값은 `PartyConfig.healerBuffFormulaFraction`으로 남기고, S09(가)의 "b 불변" 검사는 그 값을 본다 | S09 검사의 뜻은 "파티 경험치 코드가 b의 입력을 안 건드렸다" - 사용자 결정으로 올린 b 자체를 옛 값과 대조하면 X가 된다 |
| 4 | F2 | 중첩 없음은 이미 구조로 보장된다(같은 buffId `healerBuff`를 refresh로 덮어쓴다 · 딜 계산이 multiplier를 한 번 곱한다). 코드 변경 없이 P3d(나)에서 치유사 2명이 번갈아 걸어도 배율 = 1 + b인지 잰다 | 값이 하나라 "최댓값 하나"와 같다 |
| 5 | F3 | 파티 칩 = 경험치 칩 아래 "✚ 피해 +17.4%" 칩(내가 버프를 받는 동안 · 모바일 축약형은 "✚ +17.4%"). 값 = PartyConfig, 모양 = `SkillTooltipText.pct`(툴팁과 같은 함수) | 옛 칩은 초록 테두리 · 이름 꼬리 ✚뿐이라 값이 안 보였다 |
| 6 | F | 리더보드 부정 방지 상한(`Leaderboard` memberDpsCap)도 (1 + b)를 곱해 17% 넓어진다 | 같은 소스를 읽는다 - 버프 받은 실제 딜이 상한에 걸리지 않게 |
| 7 | G-a · b · c | 코드 변경 없음: 줄넘기 없는 3종에 추가 안 함 · 폭풍 군주 방전 고리 파동 2 × 1.5 유지 · 심해 군주 발판 11곳(반경 140)은 P3c 그대로(BossData abyssalKitParts · BossArenaMapData RADIUS 140) | 사용자 결정 = 지금 값 확정 |
| 8 | G-d | 견습 7단계를 EconSim 앞에 돈다(`EconSim.tutorialPhase` - 프로필 `tutorial = true`, 지금은 캐주얼만). 게임과 같은 입력: 단계별 tier · killTarget · 견습 보스(`BossRules.buildTutorialInstanceData`) · 대여(무기 등급 + 3부위 itemLevel = 지금 레벨) · 지급(1 ~ 3단계 확정 드랍, 더 좋으면 낀다) · 완료 골드. 처치 · 보스 시간 = 무한 모드와 같은 프로필 파라미터 | 결과(하네스): 견습 11.3분(레벨 7로 끝) → **캐주얼 스테이지 20 = 13.3분(견습 포함)** · 견습 제외 4.4분. P3c 추정 26 ~ 35분은 PRD의 단계당 예산(3 ~ 5분 - 안내 읽기 · 강화대 · 스킬 연습 포함)이었다 - 시뮬은 처치 · 보스의 순수 시간만 잰다(차이는 보고) |
| 9 | G-d | 일반 · 상위 1% 프로필에는 견습을 넣지 않았다 | 지시가 "캐주얼 측정"이다. 다른 프로필의 P3c-after 표가 이유 없이 바뀌지 않게 - 필요하면 프로필 한 줄(tutorial = true) |
| 10 | G-e | 첫 시즌 시작 = `LeaderboardConfig.firstSeasonDateKst = { 년, 월, 일 }`(nil = 아직 - P6에서 확정) → 그날 0시(KST) = `LeaderboardRules.kstMidnightUnix`(달력 공식 - os.time의 시간대 해석에 기대지 않는다). 없으면 옛 seasonStartUnix | 검산: 2026-10-01 · 2027-01-01 · 2028-02-29 · 2026-03-01이 python datetime(KST)과 같다 |
| 11 | G-f | "함께 맞은 수" = 같은 원 안에서 실제로 함께 맞은 사람(BossPatterns circleTarget 판정 - 원 번호별로 센다). 원이 겹친 자리의 사람은 판정과 같이 먼저 찾은 원 하나에만 센다 | P3c(나) A4 기대값을 새 규칙으로 고쳤다(배치상 S1 · S2만 같은 원 → 2명 · 높이 6, 개발 캐릭터 1명) |
| 12 | G-g | 라이브 제외 = 개발 계정 **11595243049**(Studio 로그의 프로필 키 `Player_11595243049`) - 사용자 확인 요청(결정 필요 1). 제외는 라이브(writeMode "live")에서만 건다 | 제외를 모든 모드에 걸면 Studio 검증 모드의 리더보드 기록 검사(개발 계정으로 _verify 저장소에 쓴다)가 전부 막힌다 |

## 결정 필요

| # | 무엇을 | 왜 · 선택지 |
|---|---|---|
| 1 | 라이브 제외 UserId = 11595243049가 개발 계정이 맞는가(G-g) | Studio 로그의 프로필 키에서 읽었다. 운영 계정이 더 있으면 목록에 추가 |
