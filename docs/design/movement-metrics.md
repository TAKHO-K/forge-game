# 이동 수치 · 맵 설계 기준표 v2 (M1-0 · 2026-09-25)

M1 맵 그레이박스와 G2b 보스 난이도가 이 표를 기준으로 삼는다. 수치의 단일 출처는 `roblox/src/shared/data/MovementConfig.lua`이고, 계산식은 `shared/JumpMath.lua`(`maxReachStuds` · `airTrajectory` · `maxAirSeconds`)다. 파생값은 같은 식으로 계산했고(스크래치 `air.py` · 격자 48 = 검증 `M1-0(가)`와 ±0.01 안에서 같다), [실측]은 Studio Play에서 잰 값이다.

**v1(G2a)에서 바뀐 것**: 이단점프 상한형(발 최고 7.2 고정)을 **스택형 공중 점프**로 바꿨다(사용자 결정 - 겐지 · 한조식). 공중대시는 이제 공중 점프와 섞어 쓴다. 보스 아레나도 같은 규칙이다. 그래서 높이 · 간격 · "오를 수 없는 벽"이 전부 커졌다.

## 0. 기준 수치

| 항목 | 값 | 출처 · 비고 |
|---|---|---|
| 중력 | 196.2 | place `Workspace.Gravity`(엔진 기본값) |
| 걷기 | **16 stud/s** · 상한 ×1.5 = **24** | `WorldConfig` · `MovementConfig.moveSpeedMaxMultiplier` |
| 1단 점프 | **7.2** · 체공 0.542초 | place `CharacterJumpHeight` |
| 공중 점프 | 충전 **2** · 한 번에 지금 높이에서 **+6.12**(1단 × **0.85**) | `MovementConfig.airJump`. 바닥을 밟으면 다시 찬다. 오르는 중에 누르면 더 빠른 쪽 속도를 쓴다(정점을 낮추지 않는다) |
| 공중대시 | 16 stud / 0.3초 · 쿨 8초 · **한 체공 1회** | `DashConfig`. 공중 점프 앞 · 사이 · 뒤 어디든. 끝나면 세로 속도 0에서 다시 떨어진다 |
| 점프력 옵션 | "점프 높이 +%" · 상한 **+10%** · 아레나 포함 어디서나 | 공중 점프도 비율이라 같이 커진다 |
| 높이 판정 층 | 높이차 ≤ 8 | `TerrainConfig.heightToleranceStuds`(전투 · 보스 판정 · 어그로) - 그대로 |
| 서버 높이 검증 허용 | 마지막 지면 + **22.38** | 7.92 × 2.7 + 1.0. 두 번 이어 넘으면 되돌린다 |
| 경사 한계 | 45° | 그대로 |

## 1. 최대 도달 높이 (발 · 이륙 지면 기준)

| 조합 | 옵션 없음 | 점프력 +10% |
|---|---|---|
| 1단 | 7.20 | 7.92 |
| 1단 + 공중 점프 1 | **13.32** | 14.65 |
| 1단 + 공중 점프 2 | **19.44** | **21.38** |
| + 공중대시 | 높이는 그대로(대시는 높이를 0.3초 붙잡기만 한다) | 같음 |

- 정점마다 눌렀을 때의 값이다. 적분 대조(`M1-0(가)`): 13.32 · 19.44. [실측] 연타 5번 → 최고 +18.56(충전 2회만 쓰였다 - 3회였다면 20을 넘는다) · 타이밍 조금 늦게 3번 → +16.76.
- 걸어서 오르는 턱 ≤ 2.0 · 경사 45°는 그대로(G2a 실측).

**벽 · 구조물 - 넘을 수 있는가**

| 대상 | 높이 | 결과 |
|---|---|---|
| 오두막 벽 + 지붕 | 6 + 4 = 10 | **공중 점프 1회로 지붕 위**(v1에서는 못 오름) |
| 보스 아레나 벽 | 14 | **공중 점프 2회로 넘는다** - 원 밖으로 나가면 기존 복귀(본인 스폰 + 보호 0.75초) |
| 아레나 단상(3.5) 위에서 | 3.5 + 19.44 = 22.94 | 벽보다 9 높다 |
| 맵 가장자리 장벽 | 36(투명) | 못 넘는다 |
| **오를 수 없는 벽(확실)** | **≥ 23** | 21.38 + 여유 1.6. v1은 ≥ 9 |
| 한 번에 올라서는 단 | ≤ 7(1단) · ≤ 13(공중 1) · ≤ 19(공중 2) | 착지 발판이면 표 값 − 0.3 여유 |

## 2. 한 체공 최대 · 수평 간격 (끝에서 끝, 도움닫기 있음)

간격 = 걷기 × (체공 − 대시 0.3) + 대시 16(대시를 쓰면). 같은 높이에 내려앉을 때:

| 조합 | 한 체공 최대 | 간격 걷기 16 | 간격 걷기 24 |
|---|---|---|---|
| 1단 | 0.542초 | **8.7** | 13.0 |
| 1단 + 공중 1 | 1.042초 | **16.7** | 25.0 |
| 1단 + 공중 2 | 1.541초 | **24.7** | 37.0 |
| 1단 + 대시 | 0.954초 | **26.5** | 31.7 |
| 1단 + 공중 1 + 대시 | 1.459초 | **34.5** | 43.8 |
| 1단 + 공중 2 + 대시 | **1.961초** | **42.6** | **55.9** |

- [실측] 공중 1 → 대시 → 공중 1 순서로 체공 **1.965초**(계산 1.961). 충전 기록 = 점프 1 → 대시 → 점프 0 → 착지(2 · 대시 초기화).
- 체공은 정점이 아니라 **내려오다 늦게 누를수록** 길다(격자 탐색의 최적 = 궤적 아래쪽 10 ~ 40% 높이에서 누름).

높은 단에 내려앉을 때(단 높이 H, 걷기 16 / 24):

| H | 1단 | 공중 1 | 공중 2 | 1단 + 대시 | 공중 1 + 대시 | 공중 2 + 대시 |
|---|---|---|---|---|---|---|
| 2 | 8.0 / 12.0 | 16.0 / 24.0 | 24.0 / 36.0 | 25.5 / 30.3 | 33.7 / 42.6 | 41.8 / 54.7 |
| 4 | 7.2 / 10.8 | 15.2 / 22.8 | 23.2 / 34.8 | 24.4 / 28.6 | 32.9 / 41.3 | 41.0 / 53.5 |
| 7 | 5.1 / 7.6 | 13.0 / 19.6 | 21.0 / 31.6 | 21.4 / 24.0 | 30.7 / 38.1 | 38.8 / 50.3 |
| 10 | - | 11.3 / 16.9 | 19.3 / 28.9 | - | 28.5 / 34.7 | 36.9 / 47.4 |
| 13 | - | 9.2 / 13.9 | 17.2 / 25.9 | - | 25.6 / 30.4 | 34.9 / 44.3 |
| 16 | - | - | 15.3 / 23.0 | - | - | 32.6 / 40.8 |
| 19 | - | - | 13.4 / 20.1 | - | - | 29.8 / 36.8 |

- **못 건너는 틈 ≥ 46**(걷기 16 최대 42.6 + 여유). 이속 상한(24)까지 막으려면 **≥ 58**.
- 공중대시는 쿨 8초라 연속 구간에 두 번 요구하지 않는다. 공중 점프는 착지마다 다시 차므로 발판마다 요구해도 된다.

## 3. 점프맵 난이도 표 v2 (걷기 16 기준)

여유 = 표 값의 약 80%(발판 끝을 겨누지 않게). 발판 폭 ≥ 4(쉬움) · 3(보통) · 2(어려움).

| 단계 | 수평 간격 | 높이 차(오름) | 필요한 기술 |
|---|---|---|---|
| 쉬움 | ≤ 6 | ≤ 5 | 1단만 |
| 보통 | 7 ~ 19 | ≤ 15 | **공중 점프 1 ~ 2회**(7 ~ 13 = 1회 · 14 ~ 19 = 2회 · 높이 6 ~ 10 = 1회 · 11 ~ 15 = 2회) |
| 어려움 | 20 ~ 34 | ≤ 15(대시 구간은 ≤ 7) | **점프 · 대시 조합**(공중 점프로 높이를 벌고 대시로 거리 - 대시는 구간당 1번) |
| (막음) | ≥ 46 | ≥ 23 | 지름길 방지 |

- 이속 상한(24)인 사람은 간격이 약 1.3 ~ 1.5배 쉬워진다. 경쟁형 보상이면 구간 안 걷기를 16으로 묶는 규칙이 필요하다(M1에서 결정).
- 점프력 옵션(+10%)은 높이 차를 1 ~ 2 늘린다 - 어려움의 높이 상한 15는 옵션이 있어도 한 단계 안이다(21.38 − 15 = 6.4 여유).

## 4. 둥지(높은 곳 보상 자리) 난이도 3단계

| 단계 | 가는 길 | 기준 |
|---|---|---|
| 걸어서 | 계단 · 경사로 | 턱 ≤ 2 · 경사 ≤ 45° · 폭 ≥ 4 |
| 연속 점프 | 1단 점프로 오르는 단을 이어서 | 단마다 높이 ≤ 5 · 간격 ≤ 6 · 전체 높이 제한 없음(단 수로 조절). 공중 점프를 쓰면 더 빨리 가는 지름길이 생긴다 - 지름길 허용이 기본 |
| 점프 · 대시 퍼즐 | 공중 점프 2회 + 대시가 모두 필요한 한 번의 도약을 1 ~ 2개 넣는다 | 오름 11 ~ 15 + 간격 20 ~ 30(표 2의 "공중 2 + 대시" 칸 80%) · 실패하면 한 단 아래로 떨어지게(처음부터가 아니게) |

- 둥지를 "공중 점프로도 못 가는" 자리로 두려면 오름 ≥ 23이거나 간격 ≥ 46이어야 한다(= 사실상 길이 없다). 그 대신 길을 막는 것은 높이 · 간격이 아니라 **천장(머리 위 막힘)**이 싸다 - 공중 점프는 천장에 막힌다.

## 5. 잠긴 구역 경계 = 관문 결계

공중 점프 2회면 발이 21.38(옵션 상한)까지 오르고, 대시 16이 더해진다. 높이만으로 막으려면:

| 방식 | 필요 높이 · 조건 | 평가 |
|---|---|---|
| 물리 벽(충돌) | **≥ 23**(발 21.38 + 여유) - 머리까지 막으려면 ≥ 27 | 23 ~ 27 높이 벽이 구역 둘레에 서면 풍경이 막힌다 |
| **투명 충돌 벽 + 보이는 결계(추천)** | 투명 충돌 36(맵 가장자리 장벽과 같은 값) + 발밑 높이 1 ~ 3의 빛나는 결계 선 · 문 | 넘을 길 0 · 풍경 그대로 · 결계 문에 "잠김 조건" 표시 |
| 밀어내기(서버) | 잠긴 구역 안에 있으면 들어온 쪽 경계 밖 + 4로 옮김(아레나 복귀 규칙과 같은 모양) | 순간이동 · 끼임 · 부정 대비 **보조**. 주 수단으로 쓰면 튕김이 잦다 |

- 추천 = 투명 충돌 36 + 결계 표시 + 서버 밀어내기 보조. 대시 광선(`DashEndpoint`)은 충돌 벽에서 멈춘다(기존).

## 6. 통로 · 공간 (v1 그대로)

| 항목 | 값 | 근거 |
|---|---|---|
| 캐릭터 폭 | 약 2(어깨) · 판정 반폭 1 | `BossData.mechanics.dodge.characterHalfWidthStuds` |
| 1인 통로 최소 | 4 | 몸 2 + 양옆 1 |
| 4인 파티 통로 최소 | 16 | 4명 나란히 |
| 4인 파티 전투 가능 폭 | 24 이상 | 지상 대시 16 + 원 장판을 옆으로 피할 폭 |
| 몬스터 격자 · 어그로 | 64 · 25.6 · 리쉬 38.4 | `WorldConfig` |
| 천장(공중 점프를 막을 때) | 바닥 + 12.2 미만이면 1단도 머리를 찧는다 · **바닥 + 13 ~ 18**이면 1단은 그대로 되고 공중 점프는 천장에 막힌다(18.3 = 공중 1 정점 13.32 + 머리 5) | 머리 = 발 + 약 5 |

## 7. 이동 시간 계산식 (v1 그대로)

- 걷기만: **T = d ÷ (16 × m)**, m = 걷기 배율(1 ~ 1.5).
- 대시를 쿨마다 섞을 때: 실효 속도 ≈ 15.4m + 2 → m = 1이면 17.4 · 1.5면 25.1 stud/s. 공중 점프는 수평 속도를 늘리지 않는다(걷기 속도 그대로 떠 있을 뿐).

| 거리 | 16 stud/s | 24 stud/s |
|---|---|---|
| 64(격자 한 칸) | 4.0초 | 2.7초 |
| 256(구역 한 변) | 16초 | 10.7초 |
| 832(지금 맵 한 변) | 52초 | 34.7초 |
| 1,000 | 62.5초 | 41.7초 |

- 포탈 순간이동 전에는 `RequestStreamAroundAsync`로 도착지를 미리 불러와야 한다(M1 재료 - 지금 사용 0건).

## 8. 카메라 (M1-0 - 사용자 결정)

| 방식 | 각도 | 줌 | 누가 |
|---|---|---|---|
| **기본 = 로블록스 기본 카메라** | 자유(엔진 기본 - 스폰 때 약 15°) | 기본 **22** · 범위 **10 ~ 60** | 모두 |
| 탑다운 시점(설정 창) | 55° · 범위 40 ~ 70 | 45 · 25 ~ 70 | 켠 사람만(이번 접속 동안) |

- 기본 카메라 줌 22 · PC 16:9에서 보이는 땅(계산): 각 15 ~ 35°면 앞은 수평선까지 · 좌우(캐릭터 줄) 74 ~ 98 / 45°면 앞 98 · 좌우 71 / 55°면 앞 49 · 좌우 68. 탑다운 표(55° · 45 = 앞 88 · 좌우 126)는 v1 §6 그대로다.
- 맵 설계 기준: 기본 카메라는 **먼 곳이 보인다**(누운 각) - 이정표 · 포탈은 거리보다 **실루엣 · 높이**로 읽히게. 발밑 좌우는 74 안팎이라 "지금 싸우는 자리"는 반경 25 안이 잘 보인다.
- 시점 고정(자체 구현): PC 왼쪽 Ctrl · 폰은 대시 버튼 위 "고정" 버튼. 켜면 마우스가 화면 가운데에 묶이고 캐릭터가 카메라 방향을 본다 · 카메라 오른쪽 어깨 1.75. 기본 Shift Lock은 꺼 둔다(LeftShift = 대시).

## 9. 실측 [Studio 수동 Play · M1-0 · 2026-09-25]

| 항목 | 실측 | 계산 · 기대 |
|---|---|---|
| 1단 | 체공 0.531 · 발 최고 +7.31 | 0.542 · 7.2 |
| 연타 5번(0.2초 간격) | 체공 1.317 · 최고 +18.56 | 충전 2회만 - 19.44 이하 |
| 공중 1 → 대시 → 공중 1 | 체공 **1.965** · 최고 +13.16 · 충전 기록 J1 → 대시 → J0 → 착지 J2 | 1.961 |
| 카메라 시작 | 기본: 줌 22.0 · 범위 10 ~ 60 / 탑다운 켬: 55.0° · 45.0 · 범위 25 ~ 70 / 끔: 22.0 · 10 ~ 60 | 설정값 |
| 시점 고정(Ctrl) | 마우스 LockCenter · AutoRotate 끔 · 오프셋 (1.75, 0, 0) · 캐릭터 방위 = 카메라 방위(차 0.0°) | - |
| 앞돌기 모션 | 관절 회전 18프레임 · 0.284초 · 끝나면 원래 자리 | flipSeconds 0.32(끝 부분은 0.01rad 아래) |
| 높이 검증 | 발 +19.2에 0.6초 → 되돌림 0 · +25.4에 1.5초 → 되돌림 1(`M1-0(나)`) | 허용 22.38 |
| 아레나 공중 | 바닥 + 16(벽 위 + 2)에 1.8초 떠 있음 → 복귀 0 · 벽 위에 서면 offFloor 복귀(`G1-0(나)`) | 서 있을 때만 잰다 |

v1(G2a) 실측 중 걷기 턱(≤ 2.0) · 구조물 무너짐 낙하(0.07초) · 탑다운 카메라 보이는 범위는 그대로 유효하다.
